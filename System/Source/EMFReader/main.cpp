//#include "EMF.h"

#include <QApplication>
#include <QFileInfo>

int main(int argc, char *argv[])
{
    // QApplication a(argc, argv);
    // MainWindow w;
    // w.show();

    // if (argc > 1) {
    //     w.loadEmfFile(QFileInfo(QString::fromLocal8Bit(argv[1])).absoluteFilePath());
    // }

    // return a.exec();
    return 0;
}
