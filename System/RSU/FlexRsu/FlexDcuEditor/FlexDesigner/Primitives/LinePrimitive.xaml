<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:s="clr-namespace:FlexDesigner.Primitives">
  <Style TargetType="{x:Type s:LinePrimitive}">
    <Style.Resources>
      <Style x:Key="LinePrimitiveAdornerThumbStyle" TargetType="{x:Type Thumb}">
        <Setter Property="Width" Value="12"/>
        <Setter Property="Height" Value="12"/>
        <Setter Property="SnapsToDevicePixels" Value="true"/>
        <Setter Property="RenderTransform">
          <Setter.Value>
            <TranslateTransform X="-6" Y="-6"/>
          </Setter.Value>
        </Setter>
        <Setter Property="Template">
          <Setter.Value>
            <ControlTemplate TargetType="{x:Type Thumb}">
              <Grid>
                <Rectangle Fill="#AADCDCDC" RadiusX="0" RadiusY="0"/>
                <Path Data="M 0,6 12,6 M 6,0 6,12" Stroke="Black" StrokeThickness="1"/>
              </Grid>
            </ControlTemplate>
          </Setter.Value>
        </Setter>
      </Style>
      
      <Style x:Key="ArrowGridStyle" TargetType="Grid">
        <Setter Property="Width" Value="10"/>
        <Setter Property="Height" Value="10"/>
        <Setter Property="RenderTransform">
          <Setter.Value>
            <TranslateTransform X="-5" Y="-5"/>
          </Setter.Value>
        </Setter>
      </Style>
      
    </Style.Resources>
    
    <Setter Property="SnapsToDevicePixels" Value="True"/>
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="{x:Type s:LinePrimitive}">
          <Canvas DataContext="{Binding RelativeSource={RelativeSource TemplatedParent}}">
            <Path Name="PART_LinePath"
                  StrokeThickness="{Binding StrokeThickness}"
                  Stroke="{StaticResource SolidBorderBrush}"
                  StrokeStartLineCap="Square"
                  StrokeEndLineCap="Triangle"
                  StrokeLineJoin="Round"
                  StrokeDashArray="{Binding StrokeDashArray}"
                  SnapsToDevicePixels="True"
                  Data="{Binding PathGeometry}">
            </Path>
            <s:MoveThumb Name="moveThumb" Visibility="Collapsed">
              <s:MoveThumb.Template>
                <ControlTemplate TargetType="Thumb">
                  <Path Data="{Binding PathGeometry}" StrokeThickness="{Binding StrokeThickness}" Stroke="Transparent" Cursor="SizeAll"/>
                </ControlTemplate>
              </s:MoveThumb.Template>
            </s:MoveThumb>
            <Grid Style="{StaticResource ArrowGridStyle}"
                  Canvas.Left="{Binding AnchorPositionSource.X}"
                  Canvas.Top="{Binding AnchorPositionSource.Y}">
              <Path Name="PART_SourceAnchorPath"/>
            </Grid>
            <Grid Style="{StaticResource ArrowGridStyle}"
                  Canvas.Left="{Binding AnchorPositionSink.X}"
                  Canvas.Top="{Binding AnchorPositionSink.Y}">
              <Path Name="PART_SinkAnchorPath"/>
            </Grid>
          </Canvas>

          <ControlTemplate.Triggers>
            <MultiDataTrigger>
              <MultiDataTrigger.Conditions>
                <Condition Value="True" Binding="{Binding RelativeSource={RelativeSource Self},Path=IsSelected}"/>
              </MultiDataTrigger.Conditions>
              <Setter TargetName="moveThumb" Property="Visibility" Value="Visible"/>
            </MultiDataTrigger>
            <!--<Trigger Property="IsMouseOver" Value="true">
              <Setter TargetName="moveThumb" Property="Visibility" Value="Visible"/>
            </Trigger>-->
          </ControlTemplate.Triggers>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
    
  </Style>
</ResourceDictionary>