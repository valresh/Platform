#include "stdafx.h"#include <CreateStructHook.h>#include <Names.h>#include <stdio.h>#include <stdlib.h>#include <rsuPaths.h>#include <rsuErr.h>#ifdef _MANAGED#pragma managed(push, off)#endifstatic FILE *pLog = NULL;BOOL APIENTRY DllMain( HMODULE hModule,                       DWORD  ul_reason_for_call,                       LPVOID lpReserved					 ){	switch (ul_reason_for_call)	{	case DLL_PROCESS_ATTACH:    KKK();    break;	case DLL_THREAD_ATTACH:	case DLL_THREAD_DETACH:    break;	case DLL_PROCESS_DETACH:    if( pLog )      fclose( pLog );    pLog = NULL;		break;	}    return TRUE;}#ifdef _MANAGED#pragma managed(pop)#endifCBase* CreateStructHook( LPCSTR ObjName, DWORD TypeID, int number /*= 0*/ ){  if( !pLog )  {    char szPath[MAX_PATH];    _makepath_s( szPath, NULL, rsuGetMemoryPath(), "tegs", "csv" );    fopen_s( &pLog, szPath, "wt" );  }  if( pLog )  {    fprintf_s( pLog, "%s;%d\n", ObjName, TypeID );    fflush( pLog );  }  return CreateStruct( ObjName, TypeID, number );}