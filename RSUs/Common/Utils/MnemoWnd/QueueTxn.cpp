#include "stdafx.h"#include "MnemoWnd.h"#include "Function.h"#include "QueueTxn.h"//~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~CQueueTexno::CQueueTexno(HWND hWnd): CQueueWnd(hWnd,NULL)  {  m_pClient      = CPipeClient::ms_pMainPipe;  mQueue.m_pPipe = CPipeClient::ms_pMainPipe;  mQueue.m_pSocket=CSocketClient::ms_pMainSocket;  }//~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~CQueueTexno::~CQueueTexno()  {  }//~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~LRESULT CQueueTexno::OnBuildQueue( const char* pszName )  {  m_nMorg = 0;  //  SetSelect( NULL );  //  CharMP szBuffer;  GetPathScn( szBuffer, pszName );  //  ::ShowWindow( m_hWnd, SW_HIDE );  //  //m_pClient->Resize();  SQueue szQueue = ::BuildQueue(this,szBuffer,pszName);  szQueue.hWnd = NULL;  ::FreeQueue(mQueue);  //  mQueue = szQueue;  if ( !(mQueue.Stat.szQueue == NULL && mQueue.Dyna.szQueue == NULL) )    mQueue.hWnd = m_hWnd;  //  //m_pClient->AddEnd();  //  Fine();  //  return 0;  }//~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~void CQueueTexno::MainTimer(DWORD dwMainTimer)  {  mQueue.m_dwMainTimer = dwMainTimer;  if ( !::IsWindowVisible( m_hWnd ) )    return;  HDC hDC = ::GetDC(m_hWnd);  if ( hDC )    {    ::PaintQueue( hDC, mQueue, this );    ::ReleaseDC( m_hWnd, hDC );    }  }//~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~LRESULT CALLBACK TexnoProc( HWND hWnd, UINT uMsg, WPARAM wParam, LPARAM lParam )  {  //  static CQueueTexno* obj;  //  if ( uMsg == WM_CREATE )    {    LRESULT lResult = ::DefWindowProc( hWnd, uMsg, wParam, lParam );    if ( lResult == 0 )      {      obj = new CQueueTexno( hWnd );      ::SetWindowLongPtr( hWnd, GWLP_USERDATA, (LONG_PTR)obj );      }    return lResult;    }  //  obj = (CQueueTexno*)(LONG_PTR)::GetWindowLongPtr( hWnd, GWLP_USERDATA );  if ( obj == NULL )  return ::DefWindowProc( hWnd, uMsg, wParam, lParam );  //  //switch ( uMsg )  //  {  //  case WM_CONTEXTMENU:  //    {  //    PT  //    if ( obj->OnContextMenu( point ) == 0 )  //      return 0;  //    }  //    break;  //  };  //  return obj->WindowProc( uMsg, wParam, lParam );  }//~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~