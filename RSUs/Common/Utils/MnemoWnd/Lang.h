#pragma once//#include "FormatScn.h"#include "UniLogic.h"#include "SocketClient.h"//~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~#undef IN_DLL#ifdef QUEUEAPM_EXPORTS#define IN_DLL Q_DECL_DLLEXPORT     #else#define IN_DLL Q_DECL_DLLIMPORT     #endif////~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~// Функции отрисовки очереди#define DefinePaint  HDC hDC,LPBYTE& szQueue,struct SQueue& q,LPBYTE beginQ, SUniLogic* utypedef int (*LPFuncPaint)(DefinePaint);//~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~// Функции нажатия на элементах очереди#define DefinePress SQueue& q,void* s,POINT point,int nCodetypedef int (*LPFuncPress)(DefinePress);// nCode - 0-Down, 1-Move, 2-Up//~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~// Функция удаления памяти ( если она заказана )typedef void (*LPFuncClear)(SQueue& q,void* s);//~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~// Функции создания очереди#define DefineTags   class CLang& def,struct STegScn* teg, struct SBuffer& Buffer#define DefineTegs   DefineTags, SUniLogic& rLogtypedef void (*LPFuncsTegs)(DefineTegs);//~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~#undef  PROLOG // Объекты, имеющие ограничивающий прямоугольник#define PROLOG  BYTE btType;\                BYTE btSendType;\                WORD wdSizeOf;\                DWORD dwShift;\                DWORD dwSizeT;\                LPFuncPaint paint;/* Функция отрисовки объекта*/\                LPFuncPress press;/* Функция нажатия на объекте*/\                LPFuncClear clear;/* Функция удаления памяти */\                RECT  mRect;typedef struct {PROLOG} SPROLOG,*LPSPROLOG;//~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~typedef struct // Объекты входящие в список, запрашиваемый у сервера  {  PROLOG  int  nNumber;  int nSocketNumber;  int  nFloat;  bool mbSelect;  LONG_PTR nRegulator;  int Cx() { return (mRect.left + mRect.right)/2;}  int Cy() { return (mRect.top + mRect.bottom)/2;}  int W () { return (mRect.right - mRect.left);}  int H () { return (mRect.bottom - mRect.top);}  int X( int w, int x )    {    return int(double(mRect.left*(w-x)+mRect.right*x)/w+0.5);    }  int Y( int h, int y )    {    return int(double(mRect.top*(h-y)+mRect.bottom*y)/h+0.5);    }  } SPROLOZ,*LPSPROLOZ;//~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~//#undef  MAX_IF#define MAX_IF 32//class CLang  {  public:    CLang(SQueue& queue);    virtual ~CLang();    void NextTeg();    SQueue& m_queue;    // Для логических выражений    int  nIf,kIf;    bool bIf[MAX_IF];    //    int  AddKind  (UINT type,const char* name,LONG_PTR* pnRegul = NULL);    int  AddKindEx(UINT type,const char* name,LONG_PTR* pnRegul = NULL);	int  AddKindPipe  (UINT type,const char* name,LONG_PTR* pnRegul = NULL);    int  AddKindExPipe(UINT type,const char* name,LONG_PTR* pnRegul = NULL);    int  AddPribor(UINT type,char* name,LPSPROLOZ p,HPRIBOR hPribor,LONG_PTR* pnRegul = NULL);	int  AddPriborPipe(UINT type,char* name,LPSPROLOZ p,HPRIBOR hPribor,LONG_PTR* pnRegul = NULL);    void AddPribor(HPRIBOR hPribor);    //    int GetActiveObj();	int GetActiveObjSocket();    int GetCountsObj();    int GetMnemoTags( int nNumber );    UINT        GetMnemoType( int nNumber );    const char* GetModelName( int nNumber );	const char* GetModelNamePipe( int nNumber );    //    static bool Memory(SBuffer& mem,unsigned long iDelta);    LPSPROLOG MemoryEx(SBuffer& mem,unsigned long iDelta);    //    class CPipeClient* Pipe()      {      return m_queue.m_pPipe;      }    //    static bool ms_bAddQueue;    static int  ms_nStack;    char* szPtr;    char* szBeg;    char* szEnd;    //    char* m_szStr;    //    COLORREF bkColor;// Цвет фона    COLORREF lgPipe ;// Цвет края    COLORREF fnPipe ;// Цвет центра    COLORREF dkPipe ;// Цвет края    //    char szComm[128];    //    int penCount;    int fntCount;    int brsCount;    DWORD m_dwShift;    //    LONG_PTR nCurrReg;    //    #define PAR0 0x0001    #define PAR1 0x0002    #define PAR2 0x0004    #define PAR3 0x0008    #define PAR4 0x0010    #define PAR5 0x0020    #define PAR6 0x0040    #define PAR7 0x0080    #define PAR8 0x0100    #define PAR9 0x0200    //    DWORD dwPar;    char szParam[10][256];    //    static char szCommonParam[4][256];  };//inline bool IsCommonParamVersions(){    return  ((AOP_TOBOLSK == Version) || (MK2_YAR == Version)||(Version==UPV20_UNPZ)||(Version==UPV_NOVOIL));}//struct SQueueTagEx  {  const char* name;  LPFuncsTegs func;  };//~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~// Структура работы с задвижкойtypedef struct : public SPROLOZ  {  SSendToModel send;    // Новое  struct CShBase* pSH;  class  CPipeClient* pPipe;  class  CSocketClient* pSocket;  bool m_bPressed;  } Queue_PRS;//~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~struct SBtnDataEx :  public SBtnData  {  bool m_bPressed;  int  m_nChild;  };//~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~typedef void (*LPFuncAddP)( struct SPriborBase* d, void* p );//~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~void SelectFocus( HDC hDC, SQueue& q, RECT rc, COLORREF fon = 0x809daa, int bRamka = 1 );bool SelectRgn( SQueue& q, HDC hDC, RECT& rc, LONG_PTR nRegul, LPBYTE beginQ, COLORREF fon = 0x809daa );HFONT GetFont( char* font, COLORREF* color = NULL );bool BadTag( HDC hDC, SQueue& q, LPSPROLOZ p, LPRECT prc = NULL );void Krug( HDC hDC, RECT rc, COLORREF frg, COLORREF bkg, int nPen );HPRIBOR AddPribors( DefineTags, Queue_PRS* p, LPFuncAddP func, EDataTypes );HPRIBOR AddPriborsPipe( DefineTags, Queue_PRS* p, LPFuncAddP func, EDataTypes );void AddPRSH( class CLang& def, enum EDataTypes type, char*& name, LPSPROLOZ p, int& nMess );void AddPRSHPipe( class CLang& def, enum EDataTypes type, char*& name, LPSPROLOZ p, int& nMess );void DrawValue( HDC hDC, LPSPROLOZ p, double dValue, const char* ext = NULL );bool ParserQueue(struct SQueue& q, char* ptr, SUniLogic& rLog );RECT Calc( char* font, const char* text, int* w = NULL, int* h = NULL );//~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~inline int X( RECT& rc, int W, int x )  {  return int(double(rc.left*(W-x)+rc.right*x)/W+0.5);  }//~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~inline int Y( RECT& rc, int H, int y )  {  return int(double(rc.top*(H-y)+rc.bottom*y)/H+0.5);  }//~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~inline bool PulsTimer(SQueue& q)  {  return (q.m_dwMainTimer % 2) == 1;  }//~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~inline bool MorgTimer(SQueue& q,bool bNoAck)  {  return ( bNoAck && PulsTimer(q) ) || !bNoAck;  }//~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~inline COLORREF Rgb(RGBTRIPLE& rgb)  {  return RGB(rgb.rgbtRed,rgb.rgbtGreen,rgb.rgbtBlue);  }inline BYTE Add(BYTE a,BYTE b,int n,int p)  {  return (unsigned char)((int)a+MulDiv((int)b-(int)a,n,p));  }inline COLORREF Col(RGBTRIPLE& a,RGBTRIPLE& c,int n,int p)  {  BYTE r = Add(a.rgbtRed  ,c.rgbtRed  ,n,p);  BYTE g = Add(a.rgbtGreen,c.rgbtGreen,n,p);  BYTE b = Add(a.rgbtBlue ,c.rgbtBlue ,n,p);  return RGB(r,g,b);  }//~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~inline bool PtInCircle(RECT& rect, POINT& point)  {  double a = (rect.right - rect.left) / 2;  double b = (rect.bottom - rect.top) / 2;  //  double x = point.x - (rect.left + rect.right) / 2;  double y = point.y - (rect.top + rect.bottom) / 2;  //  return ((x * x) / (a * a) + (y * y) / (b * b) <= 1);  }//~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~#define FLG( a ) ( ( sh.Type_Flags & a ) == a )#define MYWT CMyWT wt( hDC, q )#undef  TEG_BUTTON#define TEG_BUTTON \    { 'A', "fon"   , &p->bkgNorma, NULL },\    { 'A', "dark"  , &p->bkgPress, NULL },\    { 'A', "color" , &p->txtNorma, NULL },\    { 'A', "light" , &p->txtPress, NULL },\    { 'P', "state" , &p->ptBorder, NULL },\    { 'I', "child" , &p->m_nChild, NULL },\    { 'I', "offset", &p->nOffsetTxt, NULL },\    { 'S', "font"     ,  p->lf.lfFaceName,31 },\    { 'I', "height"   , &p->lf.lfHeight      },\    { 'I', "r"        , &p->lf.lfHeight      },\    { 'I', "bold"     , &p->lf.lfWeight      },\    { 'I', "angle"    , &p->lf.lfEscapement  },\    { 'I', "orient"   , &p->lf.lfOrientation },\    { 'B', "italic"   , &p->lf.lfItalic      },\    { 'B', "underline", &p->lf.lfUnderline   },//~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~void DefaultButton( SBtnDataEx* p, char* value );void PostfixButton( SBtnDataEx* p );bool PressedButton( SBtnDataEx* p, RECT& rc, SQueue& q );void PaintBtn( HDC hDC, RECT rc, SBtnDataEx* p, bool bPressed, int nType = 0 );bool PaintSelBtn( SQueue& q, POINT point, RECT rc, SBtnDataEx* p, int nType = 0 );UINT TextToType( char* text );//~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~#undef  QUEUE_FUNC#define QUEUE_FUNC( Text, Func ) void Func(DefineTegs);#include "Function.hpp"#undef  PAINT#define PAINT HDC hDC, SQueue& q#undef  FAINT#define FAINT CMyFont& font, SQueue& q//#define COLOR_E 0x000000ff  // EMERGNCY#define COLOR_H 0x00ff00ff  // HIGH#define COLOR_L 0x0000ffff  // LOW#define COLOR_N 0xffffffff  // NORMAL#define COLOR_A 0xfeffffff  // NORMAL+NOACK//~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~// Получение элемента из очереди#define QueueP(A) \  A* p = (A*)szQueue;\  szQueue += p->wdSizeOf;\  if ( u && u->InVis() ) return 1//#define QueueM(A) \  A* p = (A*)szQueue;\  szQueue += p->wdSizeOf//~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~// Добавление элемента в очередь#define AddQueueEx(A) A* p = (A*)def.MemoryEx(Buffer,sizeof(A));\                      if ( p == NULL ) return;\                      p->nRegulator = -1;\                      p->nNumber    = -1;#define AddQueueEX(A) A* p = (A*)def.MemoryEx(Buffer,sizeof(A));\                      if ( p == NULL ) return p;\                      p->nRegulator = -1;\                      p->nNumber    = -1;//~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~// Общие тэги для всех#define EXT_QUEUE  \    { 'R', "rect"   , &p->mRect }, \    { 'E', "rc"     , &p->mRect }, \    { 'I', "float"  , &p->nFloat}, \    { 'S', "comment", def.szComm, sizeof( def.szComm ) },//~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~inline bool IsBlink( CShBase* sh, SQueue& q, BYTE& btEHLN )  {  /*  btEHLN = 0;  // Не обнаружен в модели - рисуем  if ( !IsTypeOk           ) return true;  // Квитированный объект  - рисуем  CShAICHANNEL* hs = (CShAICHANNEL*)sh;  CAlarmBase* al = dynamic_cast<CAlarmBase*>(hs);  if ( al ) btEHLN = al->btEHLN;  if ( al && !al->notACK   ) return true;  // Не квитированный обект - по таймеру  if ( q.m_dwMainTimer % 2 ) return true;  */  return false;  }//~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~