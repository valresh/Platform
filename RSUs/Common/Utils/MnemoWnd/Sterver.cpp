#include "stdafx.h"#include "MnemoWnd.h"#include "PipeClient.h"#include "CommProc.h"#include "LogFile.h"#ifdef LINUX#define MULTITHREAD#endif // LINUXtGetShFromChild g_pExtenders[5] = { NULL };CSterver::CSterver(): SUniBuffer( sizeof(SSterver) ), mData(1), m_nActive(0)  {  ActObj("");  //#ifdef MULTITHREAD  InitializeCriticalSection(&m_lock);  //#endif //  MULTITHREAD  }//CSterver::~CSterver()  {  mData.Clear();  }//const char* CSterver::GetMnemoName( UINT nNumber )  {  static char* szDefault = "Ф¸дор";  #ifdef MULTITHREAD::EnterCriticalSection(&m_lock);#endif //  MULTITHREAD  if ( nNumber >= Count() )  {	  #ifdef MULTITHREAD::LeaveCriticalSection(&m_lock);#endif //  MULTITHREAD    return szDefault;  }  SSterver* obj = Item();  #ifdef MULTITHREAD::LeaveCriticalSection(&m_lock);#endif //  MULTITHREAD  return obj[nNumber].szName+1;  }//const char* CSterver::GetModelName( UINT nNumber )  {  static char* szDefault = "Дима";  #ifdef MULTITHREAD::EnterCriticalSection(&m_lock);#endif //  MULTITHREAD  if ( nNumber >= Count() )  {	  #ifdef MULTITHREAD::LeaveCriticalSection(&m_lock);#endif //  MULTITHREAD    return szDefault;  }  SSterver* obj = Item();  #ifdef MULTITHREAD::LeaveCriticalSection(&m_lock);#endif //  MULTITHREAD  return obj[nNumber].szServ+1;  }//UINT CSterver::GetMnemoType( UINT nNumber )  {  static EDataTypes mtDefault = id_Unknown;  #ifdef MULTITHREAD::EnterCriticalSection(&m_lock);#endif //  MULTITHREAD  if ( nNumber >= Count() )  {	  #ifdef MULTITHREAD::LeaveCriticalSection(&m_lock);#endif //  MULTITHREAD    return mtDefault;  }  SSterver* obj = Item();  #ifdef MULTITHREAD::LeaveCriticalSection(&m_lock);#endif //  MULTITHREAD  return obj[nNumber].nType;  }//int CSterver::GetMnemoTags( UINT nNumber )  {	#ifdef MULTITHREAD::EnterCriticalSection(&m_lock);#endif //  MULTITHREAD	if ( nNumber >= Count() )	{		#ifdef MULTITHREAD::LeaveCriticalSection(&m_lock);#endif //  MULTITHREAD		return -1;	}	SSterver* obj = Item();	#ifdef MULTITHREAD::LeaveCriticalSection(&m_lock);#endif //  MULTITHREAD	return obj[nNumber].nTeg;  }//int CSterver::GetAlarmTags( UINT nNumber )  {	  #ifdef MULTITHREAD::EnterCriticalSection(&m_lock);#endif //  MULTITHREAD  if ( nNumber >= Count() )  {	  #ifdef MULTITHREAD::LeaveCriticalSection(&m_lock);#endif //  MULTITHREAD    return 0;  }  SSterver* obj = Item();  #ifdef MULTITHREAD::LeaveCriticalSection(&m_lock);#endif //  MULTITHREAD  return (int)obj[nNumber].nEala;  }//bool CSterver::IsTrueSh( UINT nNumber, UINT nType )  {	#ifdef MULTITHREAD::EnterCriticalSection(&m_lock);#endif //  MULTITHREAD	SSterver* obj = Item(nNumber);	if ( obj == NULL )	{		#ifdef MULTITHREAD::LeaveCriticalSection(&m_lock);#endif //  MULTITHREAD		return false;	}	if ( obj->nType == nType )    {		CShBase* sh = Base(*obj);		if ( sh == NULL )		{			#ifdef MULTITHREAD::LeaveCriticalSection(&m_lock);#endif //  MULTITHREAD			return false;		}		#ifdef MULTITHREAD::LeaveCriticalSection(&m_lock);#endif //  MULTITHREAD  		return sh->RetCode == CShBase::rcOK;    }	#ifdef MULTITHREAD::LeaveCriticalSection(&m_lock);#endif //  MULTITHREAD	return false;  }CShBase* CSterver::Base ( SSterver& st ){	CShBase* sh = (CShBase*)mData.Obj( st.nShift );	return sh;}//CShBase* CSterver::Base( UINT nNumber )  {  SSterver* obj = Item(nNumber);  if ( obj == NULL ) return NULL;  CShBase* sh = Base(*obj);  return sh;  }//CShBase& CSterver::SH( UINT nNumber )  {	  BOOL bMainPipe = (CPipeClient::ms_pMainPipe == this);	#ifdef MULTITHREAD::EnterCriticalSection(&m_lock);#endif //  MULTITHREAD	SSterver* obj = Item(nNumber);	int nTest = 0;	if (nNumber == 0)	{		bool bMainPipe = (CPipeClient::ms_pMainPipe == this);		CLogFile::LogEx (LOGLEVEL_HIGH, "CSterver::SH_1: obj %16lx, bMainPipe %d, mData %16lx, m_nCurrent %d, m_dwSizeST %d", obj, bMainPipe, &mData, mData.m_nCurrent, mData.m_dwSizeST);		//CLogFile::LogEx (LOGLEVEL_HIGH, "CSterver::SH_1: obj %16lx, mData %16lx", obj, &mData);	}	static CShBase mbDefault(sizeof(CShBase));	if ( obj == NULL )	{		#ifdef MULTITHREAD::LeaveCriticalSection(&m_lock);#endif //  MULTITHREAD		return mbDefault;	}	//	CShBase* sh = Base(*obj);	if (nNumber == 0)		CLogFile::LogEx (LOGLEVEL_HIGH, "CSterver::SH_2: sh %16lx, m_szBuffer %16lx, nShift %d", sh, mData.m_szBuffer, obj->nShift);	if ( sh == NULL )	{		#ifdef MULTITHREAD::LeaveCriticalSection(&m_lock);#endif //  MULTITHREAD		return mbDefault;	}	if (nNumber == 0)	{		if ( sh->m_nSize != 104 )			nTest++;		CLogFile::LogEx (LOGLEVEL_HIGH, "CSterver::SH_3: m_nSize %d, Busy %d, m_bAOFS %d, RetCode %d", sh->m_nSize, sh->Busy, sh->m_bAOFS, sh->RetCode);	}	#ifdef MULTITHREAD::LeaveCriticalSection(&m_lock);#endif //  MULTITHREAD	return *sh;  }//CShBase* CSterver::Sh( UINT nNumber )  {	  CLogFile::LogEx (LOGLEVEL_HIGH, "CSterver::Sh_1: nNumber %d", nNumber);	#ifdef MULTITHREAD::EnterCriticalSection(&m_lock);#endif //  MULTITHREAD	SSterver* obj = Item(nNumber);	static CShBase mbDefault(sizeof(CShBase));	if ( obj == NULL )	{		CLogFile::LogEx (LOGLEVEL_HIGH, "CSterver::Sh_11");		#ifdef MULTITHREAD::LeaveCriticalSection(&m_lock);#endif //  MULTITHREAD		return &mbDefault;	}	//	CShBase* sh = Base(*obj);	if ( sh == NULL )	{		CLogFile::LogEx (LOGLEVEL_HIGH, "CSterver::Sh_12");		#ifdef MULTITHREAD::LeaveCriticalSection(&m_lock);#endif //  MULTITHREAD		return &mbDefault;	}	#ifdef MULTITHREAD::LeaveCriticalSection(&m_lock);#endif //  MULTITHREAD	CLogFile::LogEx (LOGLEVEL_HIGH, "CSterver::Sh_2");	return sh;}void* CSterver::ParamValue( SParamValue& var )  {	#ifdef MULTITHREAD::EnterCriticalSection(&m_lock);#endif //  MULTITHREAD	if ( var.def )	{		CShBase* sh = Sh( var.nNumber );		if ( IsTypeOk  )		{			char* ch = ((char*)sh)+var.def->dwShiftSh;			#ifdef MULTITHREAD::LeaveCriticalSection(&m_lock);#endif //  MULTITHREAD			return ch;		}/*		else		{			const char* name = this->GetMnemoName(var.nNumber);			int yy = 90;		}*/	}	#ifdef MULTITHREAD::LeaveCriticalSection(&m_lock);#endif //  MULTITHREAD	return NULL;  }// Чтобы не получать вязанку ASSERT-овinline void CSterver::Assert( SParamValue& var, EValueType a2 )  {  if ( var.def->eVal == a2 ) return;  const char* name = GetMnemoName(var.nNumber);  static bool init;  if ( init ) return;  init = true;  ASSERT( 0 );  }//double CSterver::ValueD( SParamValue& var, double dDef )  {  void* val = ParamValue( var );  if ( val )    {    Assert( var, enumValueDbl );    return *(double*)val;    }  return dDef;  }//double CSterver::ValueF( SParamValue& var, double dDef )  {  void* val = ParamValue( var );  if ( val )    {    if ( var.def->eVal == enumValueChr )      return (double)*(BYTE*)val;    if ( var.def->eVal == enumValueInt )      return (double)*(int*)val;    Assert( var, enumValueDbl );    return *(double*)val;    }  return dDef;  }BYTE CSterver::ValueB( SParamValue& var, BYTE bDef )  {  void* val = ParamValue( var );  if ( val )    {    Assert( var, enumValueChr );    return *(BYTE*)val;    }  return bDef;  }//int CSterver::ValueI( SParamValue& var, int nDef )  {  void* val = ParamValue( var );  if ( val )    {    Assert( var, enumValueInt );    return *(int*)val;    }  return nDef;  }//char* CSterver::ValueS( SParamValue& var, char* sDef )  {  void* val = ParamValue( var );  if ( val )    {    Assert( var, enumValueStr );    return (char*)val;    }  return sDef;  }//bool CSterver::ValueL( SParamValue& var, bool bDef )  {  void* val = ParamValue( var );  if ( val )    {    Assert( var, enumValueBol );    return *(bool*)val;    }  return bDef;  }//bool CSterver::FillObj(SSterver& st,const char* szServ,const char* szName){	#ifdef MULTITHREAD::EnterCriticalSection(&m_lock);#endif //  MULTITHREAD	int n = lstrlen( szServ );	if( !(n < (_countof(st.szServ)-2)) )	{		#ifdef MULTITHREAD::LeaveCriticalSection(&m_lock);#endif //  MULTITHREAD		return false;	}	strncpy_s( st.szServ+1, _countof(st.szServ)-1, szServ, _TRUNCATE );	st.szServ[0] = lstrlen( st.szServ+1 )+1;	//	st.nEala =  0;	st.nTeg  = -1;	if ( szName == NULL )    {		// Сюда попадаем при чтении crd-файла		// Здесь не надо копаться в tegs.scn		szName = szServ;    }	else    {		STeg teg;		st.nTeg = FindTegEx( szServ, teg, st.nType );		if ( st.nTeg == -1 )		{			st.nTeg = FindTeg( szName, teg, st.nType );			if ( st.nTeg != -1 )			{				n = lstrlen( teg.szModel );				st.szServ[0] = (char)n+1;				lstrcpy( st.szServ+1, teg.szModel );			}			else			{				char* find = (char*)strrchr( szServ, '.' );				if ( find )				{					*find = 0;					st.nTeg = FindTeg( szServ, teg, st.nType );					*find = '.';				}			}		}		else		{			szName = teg.szName;		}	}	n = lstrlen( szName );	if( !(n < (_countof(st.szName)-2) ))	{		#ifdef MULTITHREAD::LeaveCriticalSection(&m_lock);#endif //  MULTITHREAD		return false;	}	strncpy_s( st.szName+1, _countof(st.szName)-1, szName, _TRUNCATE );	st.szName[0] = lstrlen( st.szName+1 )+1;  // Имена мнемосхем переводим в верхний регистр  /*if ( st.nType == id_OneMnemo )    {    ::CharUpper( st.szServ+1 );    ::CharUpper( st.szName+1 );    }*/	#ifdef MULTITHREAD::LeaveCriticalSection(&m_lock);#endif //  MULTITHREAD	return true;}//int CSterver::AddObj(UINT nType,const char* szServ,const char* szName)  {	#ifdef MULTITHREAD::EnterCriticalSection(&m_lock);#endif //  MULTITHREAD	CLogFile::LogEx (LOGLEVEL_LOW, "CSterver::AddObj_0: nType %d, szServ %s, szName %s", nType, szServ, szName);	CShBase* sh = GetSh(nType);	if( !sh )	{		#ifdef MULTITHREAD::LeaveCriticalSection(&m_lock);#endif //  MULTITHREAD		return -1;	}	//	SSterver st;	st.nType  = nType;	st.nShift = mData.Count();// Устанавливаем смещение	if( !FillObj( st, szServ, szName ) )	{		#ifdef MULTITHREAD::LeaveCriticalSection(&m_lock);#endif //  MULTITHREAD		return -1;	}	CLogFile::LogEx (LOGLEVEL_LOW, "CSterver::AddObj_1");	mData.AddObj( sh, sh->m_nSize );// Добавляем объект	//	m_nActive = SUniBuffer::AddObj( &st );	#ifdef MULTITHREAD::LeaveCriticalSection(&m_lock);#endif //  MULTITHREAD	return m_nActive;  }//int CSterver::AddAcy(UINT nType,LPCTSTR pszName,BYTE nEALA)  {	#ifdef MULTITHREAD::EnterCriticalSection(&m_lock);#endif //  MULTITHREAD	int nReturn = FindObj( nType, pszName, NULL );	SSterver* item = Item(nReturn);	if ( item )		item->nEala = nEALA;	#ifdef MULTITHREAD::LeaveCriticalSection(&m_lock);#endif //  MULTITHREAD	return nReturn;  }//int CSterver::AddObj(int nTag)  {	#ifdef MULTITHREAD::EnterCriticalSection(&m_lock);#endif //  MULTITHREAD	STeg tag;	if ( ::GetTag( nTag, tag ) != nTag )	{		#ifdef MULTITHREAD::LeaveCriticalSection(&m_lock);#endif //  MULTITHREAD		return -1;	}	#ifdef MULTITHREAD::LeaveCriticalSection(&m_lock);#endif //  MULTITHREAD	return FindObj( tag.tegType, tag.szModel, tag.szName );  }//int CSterver::AddSxema( LPCTSTR pszName, UINT nType )  {  return FindObj( nType, pszName, NULL );  }//int CSterver::FindObj(UINT nType,const char* szServ,const char* szName)  {	#ifdef MULTITHREAD::EnterCriticalSection(&m_lock);#endif //  MULTITHREAD	int dl = lstrlen( szServ )+1;	SSterver* obj = Item();	UINT   nCount = Count();	for ( UINT n = 0; n < nCount; n++ )	{		if  ( obj[n].nType == nType )		{			if  ( obj[n].szServ[0] == dl )			{				if  ( lstrcmp( obj[n].szServ+1, szServ) == 0 )				{					m_nActive = n;					#ifdef MULTITHREAD::LeaveCriticalSection(&m_lock);#endif //  MULTITHREAD					return n;				}			}		}	}	#ifdef MULTITHREAD::LeaveCriticalSection(&m_lock);#endif //  MULTITHREAD	return AddObj( nType, szServ, szName );  }//CShBase* CSterver::GetSh(UINT eType){  switch ( eType )    {    case id_Forsunka  : {static CShForsunka   hs; return &hs;} break;    case id_KVO       : {static CShKVO        hs; return &hs;} break;    case id_Klapan    : {static CShKlapan     hs; return &hs;} break;    case id_Zadv      : {static CShZadv       hs; return &hs;} break;    case id_HS        : {static CShHS         hs; return &hs;} break;	case id_ModelData : {static CShModelData  hs; return &hs;} break;    case id_PHS       : {static CShPolyHS         hs; return &hs;} break;    case id_Pump      : {static CShPump       hs; return &hs;} break;    case id_GasAnalyz : {static CShGasAnalyz  hs; return &hs;} break;    case id_System    : {static CShSystem     hs; return &hs;} break;    case id_Value     : {static CShValue      hs; return &hs;} break;    case id_Edg       : {static CShEdg       hs; return &hs;} break;    case id_Sensor    : {static CShSensor     hs; return &hs;} break;    case id_Level     : {static CShSensor     hs; return &hs;} break;    case id_Alien     : {static CShSensor     hs; return &hs;} break;    } //switch  for( int i=0; i<_countof(g_pExtenders); i++ )  {    if( !g_pExtenders[i] )      break;    CShBase *psh = g_pExtenders[i]( eType );    if( psh )      return psh;  }  ASSERT(0);  return NULL;}//~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~inline int Vn( double d, int n )  {  double v = fabs(d);  while ( v > 1 )    {    v *= 0.1,n++;    if ( n == 0 ) break;    }  return -n;  }//~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~bool CSterver::FillValue( char* text, SParamValue& var, int nDot, const char* formt, double dScale )  {	if ( var.def == NULL )		return false;	#ifdef MULTITHREAD::EnterCriticalSection(&m_lock);#endif //  MULTITHREAD	//	if ( var.def->eVal == enumValueDbl )    {		double d = ValueD( var, NaN );		if ( IsNaN(d) )			lstrcpy( text, "NaN" );		else		{			if (dScale!=1)				d=d*dScale;			if ( formt && *formt )				sprintf_s( text, 512, formt, d );			else			{				if ( nDot < 0 )// Количество значащих цифр				nDot = Vn(d,nDot);				//				nDot = min( nDot, 8 );				static char fmt[8] = "%.0f";				fmt[2] = '0'+nDot;				sprintf_s( text, 512, fmt, d );			}		}    }	else    {		lstrcpy( text, "XYZ" );    }	#ifdef MULTITHREAD::LeaveCriticalSection(&m_lock);#endif //  MULTITHREAD	return true;  }//~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~bool CSterver::IsTrueUnit( UINT nNumber )  {	#ifdef MULTITHREAD::EnterCriticalSection(&m_lock);#endif //  MULTITHREAD	if ( nNumber >= Count() )	{		#ifdef MULTITHREAD::LeaveCriticalSection(&m_lock);#endif //  MULTITHREAD		return false;	}	#ifdef MULTITHREAD::LeaveCriticalSection(&m_lock);#endif //  MULTITHREAD	return true;  /* Sergej  SSterver* obj = Item();  if ( obj[nNumber].btUnit == 0xff )    return false;  CShSystem& sh = ::GiveSystem();  if ( !IsTypeOK ) return false;  SUnitInfo& info = sh.unit[obj[nNumber].btUnit];  return info.area[AreaNumb()];  */  }