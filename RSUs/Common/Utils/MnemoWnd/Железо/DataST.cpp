#include "stdafx.h"#include "..\MnemoWnd.h"#include "Queue.h"#include "..\Lang.h"#include <string>#include <sstream>//  enum eDataType  {  DataBool,   DataInt,  DataDouble  };//~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~// Структура работы с задвижкойtypedef struct : public Queue_PRS  {  bool bValue;  int nValue;  double dValue;  eDataType dataType;  //  bool bCheckZacepka;  CShKlapan sh;  bool mZacepka;      //текущее состояние зацепления  bool  m_PrevZacepka; //предыдущее состояние зацепления  double dDelta;      //между значением положения дублера и реальным положнием - вычисляется в момент зацепления   SPriborBase::MINMAX m_MinMax; //значения пределов для PribDouble c учетом dDelta для коррекции пределов вращния  //  char szName[64];  char szParam[64];  //  } Queue_DataST;const char HookOn[] = "#Зацепление";const char PositionDubl[] = "#Положение колеса дублера";// Функция отображенияint PaintDataST(DefinePaint)  {  QueueP(Queue_DataST);  //  if ( !p->m_bPressed )  {	  if (q.m_pSocket->IsConnected())	  {		  switch (p->dataType)		  {			case DataBool:                q.m_pSocket->GetParam(p->szName, p->szParam,p->bValue);                p->send.bOld = p->bValue;                break;			case DataInt:q.m_pSocket->GetParam(p->szName, p->szParam,p->nValue);				p->send.nOld = p->nValue;				break;			case DataDouble:                q.m_pSocket->GetParam(p->szName, p->szParam,p->dValue);                p->send.dOld = p->dValue;                break;		  }	  }     }  if (q.m_pSocket->IsConnected())  {       if(p->bCheckZacepka)       {         p->m_PrevZacepka = p->mZacepka;         q.m_pSocket->GetParam(p->szName, HookOn, p->mZacepka);          CShKlapan *sh=(CShKlapan*)q.m_pSocket->Sh(p->nSocketNumber);         if ( IsTypeOk )            memcpy( &p->sh, sh, sizeof( CShKlapan ) );         //if(p->mZacepka != p->m_PrevZacepka)         //{           q.m_pSocket->GetParam(p->szName, PositionDubl, p->dValue);           p->dDelta =  p->dValue - p->sh.Vent;         //}         if(p->mZacepka)           {           p->m_MinMax.min = 0. + p->dDelta;           p->m_MinMax.max = 100. + p->dDelta;           }           else           {             p->m_MinMax.min = -1000000.;             p->m_MinMax.max =  1000000.;           }       }  }  //  return 1;  }//~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~static void fnSendData(void* base,void* pVoid,bool bMessage)  {	  ASS( base )	  Queue_DataST* p = (Queue_DataST*)base;	  if (p->pSocket->IsConnected())	  {		  if ( pVoid == &p->bValue)		  {                std::string strOld = p->send.bOld? "1":"0";			  if (p->bValue)                  p->pSocket->SetParam(p->szName, p->szParam, p->szParam, "1", strOld.c_str(), bMessage, id_ModelData, enumValueInt);			  else                  p->pSocket->SetParam(p->szName, p->szParam, p->szParam, "0", strOld.c_str(), bMessage, id_ModelData, enumValueInt);              if(bMessage)                   p->send.bOld = p->bValue;		  }		  else		  if ( pVoid == &p->nValue)		  {		  		std::ostringstream strs;				strs <<p->nValue;				std::string strValue = strs.str();                strs.str("");				strs << p->send.nOld;				std::string strOldValue = strs.str();				p->pSocket->SetParam(p->szName, p->szParam, p->szParam, strValue.c_str(), strOldValue.c_str(), bMessage, id_ModelData, enumValueInt);                if(bMessage)                   p->send.nOld = p->nValue;		  }		  else		  if ( pVoid == &p->dValue)		  {		  		std::ostringstream strs;				strs <<p->dValue;				std::string strValue = strs.str();                strs.str("");				strs << p->send.dOld;				std::string strOldValue = strs.str();#ifdef _DEBUG  char buf[512];  sprintf_s(buf, "mess= %1d, Value= %s  OldValue= %s\n", bMessage, strValue.c_str(), strOldValue.c_str());   OutputDebugString(buf);#endif                 bool bSend = true;                 if(p->bCheckZacepka)                 {                 bSend = p->mZacepka;                 p->send.eType = enumValueDbl;                 if ( bSend )                   {                   p->m_MinMax.min = 0. + p->dDelta;                   p->m_MinMax.max = 100. + p->dDelta;                   p->send.SetNew( pVoid );                   if( ((p->sh.Vent >= 100.0) && (p->send.dNew > p->send.dOld))                       || ((p->sh.Vent <= 0.0) && (p->send.dNew < p->send.dOld))                     )                      bSend =false;                   }                 else                   {                     p->m_MinMax.min = -1000000.;                   p->m_MinMax.max =  1000000.;                   if(p->sh.Vent != p->send.dOld)                     {                       *((double*)pVoid) = p->send.dNew = p->sh.Vent;                     bSend = true;                     bMessage = false;                     }                   }                     if(bSend)                    {                          //здесь в каждой из переменных szValue и szValueOld посылаем по ДВА значения разделенных ;                          //первое для изменения положения дублера в симуляторе, второе для записи в журнал (с коррецией                           //на dDelta для для приведения к требуемому положению клапана)						 double dValue=*(double*)pVoid;                            char szValue[32], szValueOld[32];                         double dtrueNew = dValue - p->dDelta;                         double dOld = p->send.dOld - p->dDelta;                         if(dtrueNew < 1.)                            dtrueNew = 0.;                         if(dtrueNew > 99.)                            dtrueNew = 100.;                         sprintf_s(szValue, "%0.5f;%0.5f", dValue, dtrueNew);                         sprintf_s(szValueOld, "%0.5f;%0.5f", p->send.dOld, dOld);						 //const char* szField = p->pSocket->GetModelPseudonym(id_Klapan, p->send.szValue);						 //p->pSocket->SetParam(p->send.szName, szField, p->send.szValue, szValue, szValueOld, bMessage, id_Klapan, p->send.eType);                          p->pSocket->SetParam(p->szName, p->szParam, p->szParam, szValue, szValueOld, bMessage, id_ModelData, p->send.eType);                         bSend = false; //здесь послали - не посылать ниже                         p->send.dOld = dValue;					}                 }                  if(bSend)                {				p->pSocket->SetParam(p->szName, p->szParam, p->szParam, strValue.c_str(), strOldValue.c_str(), bMessage, id_ModelData, enumValueDbl);                if(bMessage)                   p->send.dOld = p->dValue;                }		  }	  }     }//~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~void FillData( SPriborBase* d, void* pp )  {	  d->fnData = fnSendData;	  //	  Queue_DataST* p = (Queue_DataST*)pp;	  if ((lstrcmp(p->szParam,"#Относительные обороты")==0)&&(d->nType == 14))	  {		  	p->dataType=DataDouble;			d->pVoid =  &p->dValue;	  }	  else	  if ((d->nType == 1)||(d->nType == 14))	  {		    p->dataType=DataBool;			SPriborBool* a = (SPriborBool*)d;			d->pVoid =  &p->bValue;	  }		  else	  if (d->nType == 10)	  {		    p->dataType=DataInt;			SPriborInt* a = (SPriborInt*)d;			d->pVoid =  &p->nValue;	  }	  else	  if ((d->nType == 0)||(d->nType == 11)||(d->nType == 15))	  {		    p->dataType=DataDouble;			//SPriborDbl* a = (SPriborDbl*)d;//11			//SPriborDouble* a = (SPriborDouble*)d;//0			d->pVoid =  &p->dValue;	  }	  if ((d->nType == 3)||(d->nType == 4))	  {		    p->dataType=DataBool;			d->eType = enumValueBol;			d->pVoid =  &p->bValue;	  }      if(p->bCheckZacepka)         d->pMinMax = &p->m_MinMax;  }//~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~void AddDataST(DefineTegs)  {  //  AddQueueEx(Queue_DataST);  //  //  // Код, посылаемый в АРМ  p->btSendType = QUEUE_NO;  // Определение функции вывода  p->paint = &PaintDataST;  p->bValue=false;  p->nValue=0;  p->dValue=0;  p->dataType=DataBool;  p->dDelta = 0.;  p->m_MinMax.min = 0.;  p->m_MinMax.max = 100.;  char szShift[64] = "\0";  //  STegObj obj[] =    {    { 'S', "shift", szShift, 63},    EXT_QUEUE    };::ParserObjScn( teg, obj, sizeof(obj)/sizeof(STegObj) );  //  lstrcpy(p->szName,teg->value);  lstrcpy(p->szParam,szShift);  if(strcmp(szShift, PositionDubl)==0)    p->bCheckZacepka = true;  else p->bCheckZacepka = false;  if (lstrcmp(p->szName,".")==0)  {	  int nActive=def.GetActiveObjSocket();	  const char* name = def.GetModelName(nActive);	  lstrcpy(p->szName,name);  }  AddPribors( def, teg, Buffer, p, &FillData, id_ModelData );  //  }//~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~