#include "stdafx.h"#include "Queue.h"#include "..\Lang.h"#include "ForsLogic.h"//#include "MyAvi.h"#pragma comment( lib, "MyAvi.lib" )////~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~// Структура работы с задвижкойtypedef struct : public SPROLOZ  {  int nValue;  int nType;  int nDlg;  COLORREF color;  // Для типа 2 и 3  bool bPN;  //  } Queue_ForsSH;//~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~char* PrevNexT( CPipeClient* pipe, int nNumber, int d )  {  bool bPresent = false;  const char* name = pipe->GetModelName(nNumber);  char szName[256];  lstrcpy( szName, name );  char* sp = strrchr( szName, ' ' );  if ( sp )    {    int n = atoi(sp);    n += d;    *sp = 0;    static char szPN[256];    sprintf_s( szPN, sizeof(szPN), "%s %d", szName, n );    return szPN;    }  return "";  }//~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~bool PrevNext( CPipeClient* pipe, int nNumber, int d )  {  CShForsunka sh;  pipe->GetData( id_Forsunka, PrevNexT( pipe, nNumber, d ), &sh );  return IsTypeOK;  }//~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~// Функция нажатия на объектеint PressFors(DefinePress)  {  Queue_ForsSH* p = (Queue_ForsSH*)s;  if ( nCode == 0 )    { }  else  if ( nCode == 1 )    { }  else  if ( nCode == 2 )    { }  //  if ( nCode == 2 && p->nDlg > 0 )    {    const char* name = q.m_pPipe->GetModelName( p->nNumber );    if ( p->nType == 2 )      {      if ( !p->bPN ) return 0;      name = PrevNexT( q.m_pPipe, p->nNumber, -1 );	  //MISHA чтоб пропадало окно при нажатии на стрелку Дима попросил	  WPARAM wParam = MAKELONG( p->btSendType, p->nDlg );      ::PostMsg( q.hWnd, wParam, (LPARAM)name );	  ShowWindow(q.hWnd,SW_HIDE);	  return 0;      }    else    if ( p->nType == 3 )      {      if ( !p->bPN ) return 0;      name = PrevNexT( q.m_pPipe, p->nNumber,  1 );	  //MISHA чтоб пропадало окно при нажатии на стрелку Дима попросил	  WPARAM wParam = MAKELONG( p->btSendType, p->nDlg );      ::PostMsg( q.hWnd, wParam, (LPARAM)name );	  ShowWindow(q.hWnd,SW_HIDE);	  return 0;      }	  else	  if ( p->nType == 8)      {      if ( !p->bPN ) return 0;      name = PrevNexT( q.m_pPipe, p->nNumber,  -6 );	  //MISHA чтоб пропадало окно при нажатии на стрелку Дима попросил	  WPARAM wParam = MAKELONG( p->btSendType, p->nDlg );      ::PostMsg( q.hWnd, wParam, (LPARAM)name );	  ShowWindow(q.hWnd,SW_HIDE);	  return 0;      }	  else	  if ( p->nType == 9)      {      if ( !p->bPN ) return 0;      name = PrevNexT( q.m_pPipe, p->nNumber,  6 );	  //MISHA чтоб пропадало окно при нажатии на стрелку Дима попросил	  WPARAM wParam = MAKELONG( p->btSendType, p->nDlg );      ::PostMsg( q.hWnd, wParam, (LPARAM)name );	  ShowWindow(q.hWnd,SW_HIDE);	  return 0;      }	  else	  if ( p->nType == 11)      {	  WPARAM wParam = MAKELONG( p->btSendType, p->nDlg );      ::PostMsg( q.hWnd, wParam, (LPARAM)name );	  ShowWindow(q.hWnd,SW_HIDE);	  return 0;      }	  else	  if ( p->nType == 12 )      {      if ( !p->bPN ) return 0;      name = PrevNexT( q.m_pPipe, p->nNumber, -1 );	  WPARAM wParam = MAKELONG( p->btSendType, p->nDlg );	  //MISHA хитрая логика если, для второй горелки то другой номер диалога	  if ((lstrcmp(name, "П-2к_1 Форсунка 2")==0)||((lstrcmp(name, "П-2к_2 Форсунка 2")==0)))		  wParam = MAKELONG( p->btSendType, p->nDlg+1 );      ::PostMsg( q.hWnd, wParam, (LPARAM)name );	  ShowWindow(q.hWnd,SW_HIDE);	  return 0;      }    else    if ( p->nType == 13 )      {      if ( !p->bPN ) return 0;      name = PrevNexT( q.m_pPipe, p->nNumber,  1 );	  WPARAM wParam = MAKELONG( p->btSendType, p->nDlg );	  //MISHA хитрая логика если, для второй горелки то другой номер диалога	  if ((lstrcmp(name, "П-2к_1 Форсунка 2")==0)||((lstrcmp(name, "П-2к_2 Форсунка 2")==0)))		  wParam = MAKELONG( p->btSendType, p->nDlg+1 );      ::PostMsg( q.hWnd, wParam, (LPARAM)name );	  ShowWindow(q.hWnd,SW_HIDE);	  return 0;      }    //    WPARAM wParam = MAKELONG( p->btSendType, p->nDlg );    ::PostMsg( q.hWnd, wParam, (LPARAM)name );    }  return 0;  }//~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~// Функция отображенияint PaintForsSH(DefinePaint)  {  QueueP(Queue_ForsSH);  //  double dValue = 0;  CShForsunka& sh = (CShForsunka&)PipeSH;  //  if ( IsTypeOK )    {    }  //  switch ( p->nValue )    {    case 1:dValue = sh.VentGas  ;break;    case 2:dValue = sh.ShibGas  ;break;    case 3:dValue = sh.VentMazut;break;    case 4:dValue = sh.ShibMazut;break;    case 5:dValue = sh.VentSteam;break;    case 6:dValue = sh.VentPilot;break;    case 11:dValue = sh.VentGas2  ;break;	  case 12:dValue = sh.ShibGas2  ;break;    case 13:dValue = sh.VentMazut2;break;    case 15:dValue = sh.VentSteam2;break;    case 16:dValue = sh.VentPilot2;break;    }  //  if ( p->nType == 0 )    DrawValue( hDC, p, dValue, "%" );  else  if ( p->nType == 1 || p->nType == 10 )    {    COLORREF color = 0;    if ( IsTypeOK )      {      if ( sh.isFire ) color = 0xff;      }    else      color = 0x80ff;    if ( color != 0 )    TransparentRect( hDC, p->mRect, color, p->color );    if ( p->nType == 10 )      {      //char txt[32];      //sprintf_s( txt, 31, "%d", 23 );//sh.Etap );      color = ::SetTextColor( hDC, sh.isFire ? 0xc0c0c0 : 0xffff );      //::TextOut( hDC, p->mRect.right+2, p->mRect.top, txt, lstrlen(txt) );      ::SetTextColor( hDC, color );      }    }  else  if ( p->nType == -1 ) // Разные цвета    {    COLORREF color = 0;    if ( IsTypeOK )      {      if ( sh.isFire )        {        if ( sh.VentGas > 0 && sh.VentMazut > 0 )          color = 0x117ae9;        else        if ( sh.VentGas > 0 )          color = 0xe89307;        else        if ( sh.VentMazut > 0 )          color = 0x1329d5;        }      }    else      color = 0xff00ff;    if ( color != 0 )    TransparentRect( hDC, p->mRect, color, p->color );    }  else  if ( p->nType == -2 ) // Пилотные горелки    {    if ( IsTypeOK )      {        if (sh.Is_State(IS_ZAPAL_FIRE))        {        CMyRgn rgn( hDC, 0xb68e1e );        rgn.Ellipse( p->mRect );        }      }    }  else  if ( p->nType == -3 ) // горелки котловые    {    COLORREF color = 0;    if ( IsTypeOK )      {		  if (sh.Eq_State(IS_WORK_FIRE)) color = 0xff;      }    else      color = 0x80ff;    if ( color != 0 )    TransparentRect( hDC, p->mRect, color, p->color );    }  else        if ( p->nType == 2 && p->bPN )    {    //CMyPen pen( hDC, RGB(0,0x80,0x80) );    //pen.Rect( p->mRect );    }  else  if ( p->nType == 3 && p->bPN )    {    //CMyPen pen( hDC, RGB(0,0x80,0x80) );    //pen.Rect( p->mRect );    }  else  if ( p->nType == 5 )    {    //CMyPen pen( hDC, RGB(0,0x80,0x80) );    //pen.Rect( p->mRect );    }  //  else  if ( p->nType == 6 ) // Разноцветная горелка  {	   COLORREF color = 0;	   if ( IsTypeOK )	   {		  if ( sh.isFire )			if (sh.Eq_State(IS_WORK_FIRE))			{					if ( sh.VentGas > 0 && sh.VentMazut > 0 )					  color = 0x117ae9;					else					if ( sh.VentGas > 0 )					  color = 0xe89307;					else					if ( sh.VentMazut > 0 )					  color = 0x1329d5;			}		}		else		  color = 0xff00ff;	   CMyRgn rgn( hDC, color );	   rgn.Ellipse( p->mRect );  }  else  if ( p->nType == 7 ) //номер горелки на диалоге  {		const char* name = q.m_pPipe->GetModelName(p->nNumber);		char szName[256];		lstrcpy( szName, name );		char* sp = strrchr( szName, ' ' );		if ( sp )		{			int n = atoi(sp);			char txt[32];			sprintf_s( txt, 31, "%d", n );			if (strstr(name," A Форсунка")!=NULL)				sprintf_s( txt, 31, "%dA", n );			else				if (strstr(name," B Форсунка")!=NULL)					sprintf_s( txt, 31, "%dБ", n );			::DrawText( hDC, txt, lstrlen(txt), &p->mRect, DT_SINGLELINE|DT_VCENTER|DT_CENTER );		}  }  else  if ( p->nType == 4 ) //номер секции на диалоге  {		const char* name = q.m_pPipe->GetModelName(p->nNumber);		char szName[256];		lstrcpy( szName, name );		char* sp = strrchr( szName, ' ' );		if ( sp )		{			int n = atoi(sp);			int nSection=(n-1)/6+1;			char txt[32];			sprintf_s( txt, 31, "%d", nSection );			//::DrawText( hDC, txt, lstrlen(txt), &p->mRect, DT_SINGLELINE|DT_VCENTER|DT_CENTER );		}  }  return 1;  }//~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~void AddForsSH(DefineTegs)  {  //  AddQueueEx(Queue_ForsSH);  //  // Код, посылаемый в АРМ  p->btSendType = QUEUE_FORSUNKA;  // Определение функции вывода  p->paint = &PaintForsSH;  char szImage[64];  char szDirect[64] = "up";  static int returnd = 0;  //  STegObj obj[] =    {    { 'I', "type" , &p->nType },    { 'I', "dlg"  , &p->nDlg },    { 'I', "ret"  , &returnd },    { 'A', "color", &p->color  },    { 'S', "dir"  , szDirect,63},    { 'S', "image", szImage ,63},	{ 'I', "value"  , &p->nValue },    // Дополнительная информация    EXT_QUEUE    };  //  ::ParserObjScn( teg, obj, sizeof(obj)/sizeof(STegObj) );  AddPRSHPipe( def, id_Forsunka, teg[0].value, p, p->nValue );  //  p->press = &PressFors;  //  if (( p->nType == 2 )||( p->nType == 12 ))    {	//MISHA Дима попросил, чтоб на обычных диалогах отображались стрелки, а не только на сенсорной панели    //DWORD dwStyle = ::GetWindowLong( def.m_queue.hWnd, GWL_STYLE );    //if ( dwStyle & WS_CHILD )    p->bPN = PrevNext( def.m_queue.m_pPipe, p->nNumber, -1 );    if ( !p->bPN ) ::SetRectEmpty( &p->mRect );    else      {      sprintf_s( def.m_szStr, 256, "<image=\"%s\" rect=%d,%d,%d,%d dir=%s>", szImage,        p->mRect.left,p->mRect.top,p->mRect.right,p->mRect.bottom,szDirect);      }    }  else  if (( p->nType == 3 )||( p->nType == 13 ))    {	//MISHA Дима попросил, чтоб на обычных диалогах отображались стрелки, а не только на сенсорной панели    //DWORD dwStyle = ::GetWindowLong( def.m_queue.hWnd, GWL_STYLE );    //if ( dwStyle & WS_CHILD )    p->bPN = PrevNext( def.m_queue.m_pPipe, p->nNumber,  1 );    if ( !p->bPN ) ::SetRectEmpty( &p->mRect );    else      {      sprintf_s( def.m_szStr, 256, "<image=\"%s\" rect=%d,%d,%d,%d dir=%s>", szImage,        p->mRect.left,p->mRect.top,p->mRect.right,p->mRect.bottom,szDirect);      }    }  else  if ( p->nType == 5 )    {    DWORD dwStyle = ::GetWindowLong( def.m_queue.hWnd, GWL_STYLE );    if ( dwStyle & WS_CHILD )      p->nDlg  = returnd;    if ( p->nDlg > 0 )      {      p->press = &PressFors;      sprintf_s( def.m_szStr, 256, "<image=\"%s\" rect=%d,%d,%d,%d dir=%s>", szImage,        p->mRect.left,p->mRect.top,p->mRect.right,p->mRect.bottom,szDirect);      }    }    if ( p->nType == 8 )    {    p->bPN = PrevNext( def.m_queue.m_pPipe, p->nNumber,  -6 );    if ( !p->bPN ) ::SetRectEmpty( &p->mRect );    else      {      sprintf_s( def.m_szStr, 256, "<image=\"%s\" rect=%d,%d,%d,%d dir=%s>", szImage,        p->mRect.left,p->mRect.top,p->mRect.right,p->mRect.bottom,szDirect);      }    }  else	if ( p->nType == 9 )    {    p->bPN = PrevNext( def.m_queue.m_pPipe, p->nNumber,  6 );    if ( !p->bPN ) ::SetRectEmpty( &p->mRect );    else      {      sprintf_s( def.m_szStr, 256, "<image=\"%s\" rect=%d,%d,%d,%d dir=%s>", szImage,        p->mRect.left,p->mRect.top,p->mRect.right,p->mRect.bottom,szDirect);      }    }	  else	if ( p->nType == 11 )    {      sprintf_s( def.m_szStr, 256, "<image=\"%s\" rect=%d,%d,%d,%d dir=%s>", szImage,        p->mRect.left,p->mRect.top,p->mRect.right,p->mRect.bottom,szDirect);    }  //  }//~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~