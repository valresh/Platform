#include "stdafx.h"#include "Queue.h"#include "..\Lang.h"////~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~// Структура работы с задвижкойtypedef struct : public Queue_PRS  {  CShKlapan sh;  bool mPress;  bool mAutoMode;  int  mZacepka;  bool m_bOpen;  bool m_bStopor;  bool m_bStopBaipas;  bool m_bStopIn;  bool m_bStopOut;  bool m_bIsOpen;  bool m_bIsClose;  bool m_bIsMufta;  int nHS;  bool bState;  int nSend;  bool bSend;  bool m_bStoporKoleso;  bool m_bIsManual;  bool m_bIsSend;  bool m_bStopControlSP;  int nResetOpenClose;  int nStopOpenClose;  int nKlapanStop;  bool m_b0Lamp;  bool m_b100Lamp;  bool m_b1_99Lamp;  bool  m_bOpen5Sec;  bool  m_bZadaniePlus;  bool  m_bZadanieMinus;  double m_dStep;  bool bPipe;  bool bViewInvert;  int  m_PrevZacepka; //предыдущее состояние зацепления  double dDelta;      //между значением положения дублера и реальным положнием - вычисляется в момент зацепления   SPriborBase::MINMAX m_MinMax; //значения пределов для PribDouble c учетом dDelta для коррекции пределов вращния  } Queue_KlapST;////struct CReg//  {//  enum// -> charMode//    {//    Man     = 'M',//    Auto    = 'A',//    Cascade = 'C',//    PRD     = 'P',//    Off     = ' ' //    };//  };//~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~static char cszZacepl_Dubler[] = "Зацепленный дубл¸р";// Функция отображенияint PaintKlapST(DefinePaint)  {  QueueP(Queue_KlapST);  //  if ( p->nHS >= 0 )    {		  CShHS& Sh = (CShHS&)q.m_pPipe->SH(p->nHS);		  if ( Sh.RetCode == CShBase::rcOK )		  {				p->bState = p->bViewInvert? !Sh.mOn : Sh.mOn;		  }	}    if ( p->nKlapanStop>=0)  {		CShKlapan& Sh = (CShKlapan&)q.m_pPipe->SH(p->nKlapanStop);		if (Sh.VentZ<=0)			p->m_bStopControlSP=true;		else			p->m_bStopControlSP=false;  }	if ( p->nSend >= 0 )	{	  CShHS& Sh = (CShHS&)q.m_pPipe->SH(p->nSend);      if ( Sh.RetCode == CShBase::rcOK )			p->bSend = Sh.mOn;	}  CShKlapan* sh = (CShKlapan*)PipeSh;  if ( !p->m_bPressed )    {	if (p->nSocketNumber!=-1)		sh=(CShKlapan*)q.m_pSocket->Sh(p->nSocketNumber);    if ( IsTypeOk )      {      memcpy( &p->sh, sh, sizeof( CShKlapan ) );			p->mAutoMode = false;//p->sh.charMode == SModePole::AUT;      p->m_bStopor=((!p->sh.Is_Ctrl(CKlapan::KLAPAN_MANUAL))||(p->sh.Is_Ctrl(CKlapan::KLAPAN_HYDRAVLIC)));			p->m_bStopBaipas=p->sh.Is_State(CKlapan::BAIPAS_BROKEN);			p->m_bStopIn=p->sh.Is_State(CKlapan::IN_BROKEN);			p->m_bStoporKoleso=!p->sh.Is_Ctrl(CKlapan::KLAPAN_MANUALKOLESO);      p->m_bStopOut=p->sh.Is_State(CKlapan::OUT_BROKEN);	  p->m_bIsOpen=p->sh.Is_State(CKlapan::IS_OPEN);	  p->m_bIsClose=p->sh.Is_State(CKlapan::IS_CLOSE);	  p->m_bIsMufta=p->sh.Is_State(CKlapan::IS_MUFTA);	  p->m_b0Lamp=(p->sh.Vent==0);      p->m_b100Lamp=(p->sh.Vent==100);      p->m_b1_99Lamp=(!p->m_b0Lamp)&&(!p->m_b100Lamp);    p->mZacepka=p->sh.m_bZacepka;	p->m_bOpen5Sec=p->sh.Is_Ctrl(CKlapan::OPEN_5SEC_TIMER);	  //if(!p->m_bIsSend)	  //{			p->m_bIsManual=p->sh.Is_Ctrl(CKlapan::KLAPAN_MANUALKOLESO);			//p->m_bIsSend=true;	  //}      }    }  if ( lstrcmp( p->send.szValue, cszZacepl_Dubler ) == 0 )  {   p->mZacepka=p->sh.m_bZacepka;   if(p->mZacepka)     {  //здесь при зацеплении вычисляется Delta между положением дублера и реальным положением         if(p->mZacepka != p->m_PrevZacepka)          {          p->dDelta =  p->sh.Vent_Dubler - p->sh.Vent;          }        p->m_MinMax.min = 0. + p->dDelta;        p->m_MinMax.max = 100. + p->dDelta;     }   else     {        p->m_MinMax.min = -1000000.;        p->m_MinMax.max =  1000000.;     }     p->m_PrevZacepka = p->mZacepka;  }  //  return 1;  }//~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~static void fnSendData(void* base,void* pVoid,bool bMessage)  {  ASS( base )  Queue_KlapST* p = (Queue_KlapST*)base;  if ( !ISTypeOK ) return;  //  bool bSend = true;  //  // Работа ручной помпы  if ( pVoid == &p->mPress && lstrcmp( p->send.szValue,  "Гидравлическая помпа" ) == 0 )    {    if ( p->sh.ePumpCode == CKlapan::enumNeitral || p->sh.Vent_OilB >= 100. )      bSend = false;    else      {      double Old = p->sh.Vent;      double New = Old;      double step = 1.-0.01*p->sh.Vent_OilB;      if ( ( GetAsyncKeyState( VK_CONTROL ) & 0x8000 ) )        step *= 10;      if ( p->sh.ePumpCode == CKlapan::enumOpen )        New = min( 100., Old+step );      else        New = max( 0.00, Old-step );      p->send.Set( enumValueDbl, &Old, &New );      }    }  else  // Если нет зацепления крутилка не работает  if ( pVoid == &p->sh.Vent_Dubler && lstrcmp( p->send.szValue, cszZacepl_Dubler ) == 0 )    {    bSend = p->sh.m_bZacepka;    if ( bSend )      {      p->m_MinMax.min = 0. + p->dDelta;      p->m_MinMax.max = 100. + p->dDelta;      p->send.SetNew( pVoid );      if( ((p->sh.Vent >= 100.0) && (p->send.dNew > p->send.dOld))          || ((p->sh.Vent <= 0.0) && (p->send.dNew < p->send.dOld))        )         bSend =false;      }    else      {        p->m_MinMax.min = -1000000.;      p->m_MinMax.max =  1000000.;      if(p->sh.Vent != p->send.dOld)        {          *((double*)pVoid) = p->send.dNew = p->sh.Vent;        bSend = true;        bMessage = false;        }      }      if (( bSend )&&(p->bSend))      {	  if (p->nSocketNumber!=-1)	    {			if (p->send.eType==enumValueDbl)			{//здесь в каждой из переменных szValue и szValueOld посылаем по ДВА значения разделенных ;             //первое для изменения положения дублера в симуляторе, второе для записи в журнал (с коррецией              //на dDelta для для приведения к требуемому положению клапана)		    double dValue=*(double*)pVoid;               char szValue[32], szValueOld[32];            double dtrueNew = dValue - p->dDelta;            double dOld = p->send.dOld - p->dDelta;            if(dtrueNew < 1.)               dtrueNew = 0.;            if(dtrueNew > 99.)               dtrueNew = 100.;            sprintf_s(szValue, "%0.5f;%0.5f", dValue, dtrueNew);            sprintf_s(szValueOld, "%0.5f;%0.5f", p->send.dOld, dOld);			const char* szField = p->pSocket->GetModelPseudonym(id_Klapan, p->send.szValue);		    p->pSocket->SetParam(p->send.szName, szField, p->send.szValue, szValue, szValueOld, bMessage, id_Klapan, p->send.eType);             bSend = false; //здесь послали - не посылать ниже			}                  }      }    }  else  if ( pVoid == &p->mZacepka && lstrcmp( p->send.szValue, "Зацепление" ) == 0 )    {    bool Old = p->sh.m_bZacepka;    bool New = p->mZacepka != 0;    p->send.Set( enumValueBol, &Old, &New );       bSend = bMessage;    bMessage = true; //для сброса p->m_bPressed в конце - т.к. в PaintKlapST() по !p->m_bPressed заполняется реальное положение    }   else   if ( p->send.eType==enumValueInt && lstrcmp( p->send.szValue, "Дистанц." ) == 0 )    {    bSend = bMessage;    bMessage = true; //для сброса p->m_bPressed в конце - т.к. в PaintKlapST() по !p->m_bPressed заполняется реальное положение    }   else  if ( pVoid == &p->m_bIsManual )    {    bool Old = p->sh.Is_Ctrl(CKlapan::KLAPAN_MANUALKOLESO);    bool New = p->m_bIsManual;    p->send.Set( enumValueBol, &Old, &New );    }  else  if ( pVoid == &p->m_bZadaniePlus )    {	  if (p->m_bZadaniePlus)	  {		  double Old = p->sh.VentZ;		  double New = p->sh.VentZ+p->m_dStep;		  if (New>100)			  New=100;		  p->send.Set( enumValueDbl, &Old, &New );	  }    }  else  if ( pVoid == &p->m_bZadanieMinus )    {	  if (p->m_bZadanieMinus)	  {		  double Old = p->sh.VentZ;		  double New = p->sh.VentZ-p->m_dStep;		  if (New<0)			  New=0;		  p->send.Set( enumValueDbl, &Old, &New );	  }    }  else  if ( pVoid == &p->mAutoMode && lstrcmp( p->send.szValue, "Мода" ) == 0 )    {    char Old = 0;//p->sh.charMode;		BYTE New = ( p->mAutoMode ) ? SModePole::AUT : SModePole::MAN;    p->send.Set( enumValueChr, &Old, &New );    }  else  if ( pVoid == &p->m_bOpen && lstrcmp( p->send.szValue, "Откр/Закр" ) == 0 )    {		if (p->nSocketNumber!=-1)		{			double dValue=1;			char szValue[128];			if (p->m_bOpen)				lstrcpy( szValue,"Открыть");			else				lstrcpy( szValue,"Закрыть");			p->pSocket->SetSocketValue(id_Klapan, p->send.szName, szValue, dValue, p->send.dOld, bMessage, p->send.eType);			bSend = false; 		}		else		{			bool Old= false;			bool New= true;			p->send.Set( &Old, &New );			if ( p->m_bOpen ) 			lstrcpy( p->send.szValue, "Открыть" ); 			else 			lstrcpy( p->send.szValue, "Закрыть" ); 			p->pPipe->SendData( p->send, bMessage ); 			lstrcpy( p->send.szValue, "Откр/Закр" ); 			bSend = false; 		}    }  else  if ( pVoid == &p->nResetOpenClose && lstrcmp( p->send.szValue, "СбросОткр/Закр" ) == 0 )    {    bool Old= false;    bool New= true;    p->send.Set( &Old, &New );    if ( p->nResetOpenClose==1 ) 	{    lstrcpy( p->send.szValue, "Открыть" ); 	if (p->nSocketNumber!=-1)	{			if (p->send.eType==enumValueInt)			{				int nValue=1;				p->send.nOld=0;				p->pSocket->SetSocketValue(id_Klapan, p->send.szName, p->send.szValue, nValue, p->send.nOld, bMessage, p->send.eType);			}	}	else		p->pPipe->SendData( p->send, bMessage ); 	}    else     if ( p->nResetOpenClose==2 ) 	{        lstrcpy( p->send.szValue, "Закрыть" ); 		if (p->nSocketNumber!=-1)		{			if (p->send.eType==enumValueInt)			{				int nValue=1;				p->send.nOld=0;				p->pSocket->SetSocketValue(id_Klapan, p->send.szName, p->send.szValue, nValue, p->send.nOld, bMessage, p->send.eType);			}		}		else			p->pPipe->SendData( p->send, bMessage ); 	}    lstrcpy( p->send.szValue, "СбросОткр/Закр" );     bSend = false;     }  else  if ( pVoid == &p->nStopOpenClose && lstrcmp( p->send.szValue, "СтопОткр/Закр" ) == 0 )    {    bool Old= false;    bool New= true;    p->send.Set( &Old, &New );	if ( p->nStopOpenClose==0 ) 	{		lstrcpy( p->send.szValue, "Остановить" ); 		if (p->nSocketNumber!=-1)		{				if (p->send.eType==enumValueInt)				{					int nValue=1;					p->send.nOld=0;					p->pSocket->SetSocketValue(id_Klapan, p->send.szName, p->send.szValue, nValue, p->send.nOld, bMessage, p->send.eType);				}		}		else			p->pPipe->SendData( p->send, bMessage ); 	}    else     if ( p->nStopOpenClose==1 ) 	{    lstrcpy( p->send.szValue, "Открыть" ); 	if (p->nSocketNumber!=-1)	{			if (p->send.eType==enumValueInt)			{				int nValue=1;				p->send.nOld=0;				p->pSocket->SetSocketValue(id_Klapan, p->send.szName, p->send.szValue, nValue, p->send.nOld, bMessage, p->send.eType);			}	}	else		p->pPipe->SendData( p->send, bMessage ); 	}    else     if ( p->nStopOpenClose==2 ) 	{        lstrcpy( p->send.szValue, "Закрыть" ); 		if (p->nSocketNumber!=-1)		{			if (p->send.eType==enumValueInt)			{				int nValue=1;				p->send.nOld=0;				p->pSocket->SetSocketValue(id_Klapan, p->send.szName, p->send.szValue, nValue, p->send.nOld, bMessage, p->send.eType);			}		}		else			p->pPipe->SendData( p->send, bMessage ); 	}    lstrcpy( p->send.szValue, "СтопОткр/Закр" );     bSend = false;     }  else  if ( lstrcmp( p->send.szValue, "Открыть"    ) == 0  ||   lstrcmp( p->send.szValue, "Закрыть"    ) == 0  ||   lstrcmp( p->send.szValue, "Остановить" ) == 0 )    {    bool Old= false;    bool New= true;    p->send.Set( &Old, &New );    p->send.SetNew( pVoid );    }  else    // Если нет зацепления крутилка не работает  if ( pVoid == &p->sh.Vent && lstrcmp( p->send.szValue, "Зацепленный шибер" ) == 0 )    {	bSend = p->sh.m_bZacepka&&p->sh.Is_Ctrl(CKlapan::KLAPAN_MANUALKOLESO);    if ( bSend )      p->send.SetNew( pVoid );    }  else  if ( pVoid == &p->sh.m_bZacepka && lstrcmp( p->send.szValue, "Зацепление шибера" ) == 0 )    {	bSend = p->sh.Is_Ctrl(CKlapan::KLAPAN_MANUALKOLESO);    if ( bSend )      p->send.SetNew( pVoid );    }  else  if ( lstrcmp( p->send.szValue, "Открыть5сек" ) == 0 )  {	        int New =*(int*)pVoid;			int Old = (New==0);	  p->send.Set( enumValueInt, &Old, &New );  }  else    p->send.SetNew( pVoid );    if ((lstrcmp( p->send.szValue,  "Открыть" ) == 0 )||(lstrcmp( p->send.szValue,  "Закрыть" ) == 0 )||(lstrcmp( p->send.szValue,  "Остановить" ) == 0 ))	{		//if (p->sh.LocalRemoteDouble!=0)		//	bSend=false;	}  //  if ( pVoid == &p->sh.Vent_Dubler && lstrcmp( p->send.szValue, "Дубл¸р Автозацеп" ) == 0 )  {	  double dublerNewValue =*(double*)pVoid;	  SSendToModel sendZacep;      bool New = true;	  if ((bMessage)&&(dublerNewValue<=0.9))		  New=false;	  bool Old =!New;	  sendZacep.Set( enumValueBol, &Old, &New );	  lstrcpy(sendZacep.szValue,"Зацепление");	  lstrcpy(sendZacep.szName,p->send.szName);	  sendZacep.eData=id_Klapan;	  p->pPipe->SendData( sendZacep, false );  }  if (( bSend )&&(p->bSend))  {	  if (p->nSocketNumber!=-1)	  {			if (p->send.eType==enumValueDbl)			{				double dValue=*(double*)pVoid;				p->pSocket->SetSocketValue(id_Klapan, p->send.szName, p->send.szValue, dValue, p->send.dOld, bMessage, p->send.eType);			}			else			if (p->send.eType==enumValueInt)			{				int nValue=*(int*)pVoid;				p->pSocket->SetSocketValue(id_Klapan, p->send.szName, p->send.szValue, nValue, p->send.nOld, bMessage, p->send.eType);			}						else			if (p->send.eType==enumValueBol)			{				bool bValue=*(bool*)pVoid;                p->pSocket->SetSocketValue(id_Klapan, p->send.szName, p->send.szValue, bValue, p->send.bOld, bMessage, p->send.eType);			}	  }	  else          p->pPipe->SendData( p->send, bMessage );  }  if  ( lstrcmp( p->send.szValue, "КонтрольСостояния" ) == 0 )  {	  int Old =CKlapan::Ctrl_State;      int New = CKlapan::Ctrl_State;	  p->send.Set( enumValueInt, &Old, &New );	  p->pPipe->SendData( p->send, false );  }  p->m_bPressed = !bMessage;  }//~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~void FillKlapan( SPriborBase* d, void* pp )  {  d->fnData = fnSendData;  Queue_KlapST* p = (Queue_KlapST*)pp;  if ( p->nHS >= 0 && d->nType == 1 )  {    SPriborBool* a = (SPriborBool*)d;    a->pbOther = &p->bState;	if ((lstrcmp( p->send.szValue, "Задание+" ) == 0 )||(lstrcmp( p->send.szValue, "Задание-" ) == 0 ))		a->pbBlock=&p->bState;  }  // Маленькие нюансы  if ( d->pVoid == &p->sh.Vent && lstrcmp( p->send.szValue, "Гидравлическая помпа" ) == 0 )       d->pVoid =  &p->mPress;  else  if ( /*d->pVoid == &p->sh.charMode &&*/ lstrcmp( p->send.szValue, "Мода" ) == 0 )       d->pVoid =  &p->mAutoMode;  else  if ( d->pVoid == &p->sh.m_bZacepka && lstrcmp( p->send.szValue, "Зацепление" ) == 0 )       d->pVoid =  &p->mZacepka;  else  if ( d->pVoid == &p->sh.Ctrl_Flags && lstrcmp( p->send.szValue, "Откр/Закр" ) == 0 )       d->pVoid =  &p->m_bOpen;  else  if ( d->pVoid == &p->sh.Ctrl_Flags && lstrcmp( p->send.szValue, "СбросОткр/Закр" ) == 0 )       d->pVoid =  &p->nResetOpenClose;  else  if ( d->pVoid == &p->sh.Ctrl_Flags && lstrcmp( p->send.szValue, "СтопОткр/Закр" ) == 0 )       d->pVoid =  &p->nStopOpenClose;  else  if ( d->pVoid == &p->sh.State_Flags && lstrcmp( p->send.szValue, "Открыт" ) == 0 )       d->pVoid =  &p->m_bIsOpen;  else  if ( d->pVoid == &p->sh.State_Flags && lstrcmp( p->send.szValue, "Закрыт" ) == 0 )       d->pVoid =  &p->m_bIsClose;  else  if ( d->pVoid == &p->sh.Vent && lstrcmp( p->send.szValue, "0Лампа" ) == 0 )       d->pVoid =  &p->m_b0Lamp;  else  if ( d->pVoid == &p->sh.Vent && lstrcmp( p->send.szValue, "100Лампа" ) == 0 )       d->pVoid =  &p->m_b100Lamp;  else  if ( d->pVoid == &p->sh.Vent && lstrcmp( p->send.szValue, "1_99Лампа" ) == 0 )       d->pVoid =  &p->m_b1_99Lamp;  else  if ( d->pVoid == &p->sh.State_Flags && lstrcmp( p->send.szValue, "Муфта" ) == 0 )       d->pVoid =  &p->m_bIsMufta;  else  if ( d->pVoid == &p->sh.Ctrl_Flags && lstrcmp( p->send.szValue, "ManualKoleso" ) == 0 )       d->pVoid =  &p->m_bIsManual;  else  if ( d->pVoid == &p->sh.Ctrl_Flags && lstrcmp( p->send.szValue, "Открыть5сек" ) == 0 )		d->pVoid =  &p->m_bOpen5Sec;  else  if ( d->pVoid == &p->sh.VentZ && lstrcmp( p->send.szValue, "Задание+" ) == 0 )		d->pVoid =  &p->m_bZadaniePlus;  else  if ( d->pVoid == &p->sh.VentZ && lstrcmp( p->send.szValue, "Задание-" ) == 0 )		d->pVoid =  &p->m_bZadanieMinus;  //Показания HC  if ( d->nType == 4 && lstrcmp( p->send.szValue, "Показания" ) == 0 )    {    SPriborArrow* a = (SPriborArrow*)d;    a->pdVal        = &p->sh.VentZ;    }  if ( lstrcmp( p->send.szName, "ПоворотБлок" ) == 0 )  {    d->pbBlock = &p->m_bStopor;  }  if ( lstrcmp( p->send.szValue, "ЗаданиеБлок" ) == 0 )  {    d->pbBlock = &p->m_bStoporKoleso;  }  if ( lstrcmp( p->send.szName, "Байпас" ) == 0 )  {    d->pbBlock = &p->m_bStopBaipas;  }    if ( lstrcmp( p->send.szName, "Вход" ) == 0 )  {    d->pbBlock = &p->m_bStopIn;  }  if ( lstrcmp( p->send.szName, "Выход" ) == 0 )  {    d->pbBlock = &p->m_bStopOut;  }  if ( lstrcmp( p->send.szValue, "Поворот_С_Блокировкой" ) == 0 )       d->pbBlock = &p->m_bStopControlSP;  if ( lstrcmp( p->send.szValue, cszZacepl_Dubler ) == 0 )  {       d->pMinMax = &p->m_MinMax;  }  }//~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~void AddKlapST(DefineTegs)  {  AddQueueEx(Queue_KlapST);  //  CShKlapan sh;  memcpy( &p->sh, &sh, sizeof(CShKlapan) );  p->pSH = &p->sh;  //  // Код, посылаемый в АРМ  p->btSendType = QUEUE_NO;  // Определение функции вывода  p->paint = &PaintKlapST;  p->nHS=-1;  char szHS[64] = "";  char szSF[64] = "";  p->nSend=-1;  p->bSend=true;  p->m_bIsSend=false;  char szSend[64] = "";  p->m_bStopControlSP=false;  char szKlapanStop[64] = "";  p->nKlapanStop=-1;  p->m_b0Lamp=false;  p->m_b100Lamp=false;  p->m_b1_99Lamp=false;  p->m_bOpen5Sec=false;  p->m_bZadaniePlus=false;  p->m_bZadanieMinus=false;  p->m_dStep=0;  p->bViewInvert=false;  p->nSocketNumber=-1;  p->m_MinMax.min = 0.;  p->m_MinMax.max = 100.;  p->dDelta = 0.;  p->bPipe=false;  //  STegObj obj[] =    {    { 'S', "view", szHS, 63 },	{ 'S', "send", szSend, 63 },	{ 'S', "stop", szKlapanStop, 63 },      { 'S', "suff", szSF, 63 },	  { 'D', "step" ,  &p->m_dStep },	  { 'B', "viewinvert", &p->bViewInvert},	  { 'B', "pipe", &p->bPipe},    // Дополнительная информация    EXT_QUEUE    };::ParserObjScn( teg, obj, sizeof(obj)/sizeof(STegObj) );  //    if ( *szSF )    {    int nActive = def.GetActiveObjSocket();    const char* name = def.GetModelName(nActive);    char szName[128];    sprintf_s( szName, sizeof(szName), "%s%s", name, szSF );    def.AddKind(id_Klapan,szName);    def.m_queue.m_pSocket->Refresh();    }	  if ( *szHS )    p->nHS = def.AddKind(id_HS,szHS);  if ( *szSend )    p->nSend = def.AddKind(id_HS,szSend);  if ( *szKlapanStop )	  p->nKlapanStop=def.AddKind(id_Klapan,szKlapanStop);  //  //  HPRIBOR hPribor;  if (p->bPipe)	  hPribor= AddPriborsPipe( def, teg, Buffer, p, &FillKlapan, id_Klapan );  else        hPribor= AddPribors( def, teg, Buffer, p, &FillKlapan, id_Klapan );  if ( hPribor == NULL )    {    Buffer.nPtr -= sizeof(Queue_KlapST);    memset( Buffer.szQueue+Buffer.nPtr, 0, sizeof(Queue_KlapST) );    }  //  }//~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~