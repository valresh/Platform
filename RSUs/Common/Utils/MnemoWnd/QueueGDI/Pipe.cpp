#include "stdafx.h"#include "Queue.h"#include "..\Lang.h"////~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~void AddPipeV(DefineTegs);void AddPipeH(DefineTegs);void AddPipeF(DefineTegs);//~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~void AddPipe(DefineTegs)  {  EDirect eDir = ParserDirect(teg[0].value);  switch ( eDir )    {    case dirVert:AddPipeV(def,teg,Buffer,rLog);break;    case dirHorz:AddPipeH(def,teg,Buffer,rLog);break;    case dirFree:AddPipeF(def,teg,Buffer,rLog);break;    };  }//~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~// Определение типов трубstruct STypePipe  {  char name[64];// Имя  COLORREF   lg;// Цвет края  COLORREF   fn;// Цвет центра  COLORREF   dk;// Цвет края  };//class CTypePipe //: public SUniBuffer  {  public:	  CTypePipe();	  virtual ~CTypePipe();    void AddPipeColor(STypePipe& pipe);    STypePipe&  SetPipeColor(char* value);  };//~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~CTypePipe thePipe;//// Определение типов трубCTypePipe::CTypePipe()//: SUniBuffer(sizeof(STypePipe))  {  STypePipe add;  lstrcpy( add.name, "   " );  add.lg = 0x000000;  add.fn = 0xffffff;  add.dk = 0x000000;  //AddObj( &add );  }CTypePipe::~CTypePipe()  {  }void CTypePipe::AddPipeColor(STypePipe& pipe)  {  /*  UINT nCnt = Count();  STypePipe* obj = (STypePipe*)Obj(0);	for ( UINT n = 0; n < nCnt; n++)    if ( !lstrcmpi(pipe.name,obj[n].name) )      {      obj[n].lg = pipe.lg;      obj[n].fn = pipe.fn;      obj[n].dk = pipe.dk;      return;      }  AddObj(&pipe);  */  }STypePipe& CTypePipe::SetPipeColor(char* value)  {#if 0  STypePipe* obj = (STypePipe*)Obj(0);  if ( value && *value )    {    ::CharLower(value);    UINT nCnt   = Count();    STypePipe* obj = (STypePipe*)Obj(0);	  for ( UINT n = 0; n < nCnt; n++)      if ( !strcmp(value,obj[n].name) )        return obj[n];    }  return obj[0];#else  static STypePipe ss;  return ss;#endif  }//~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~void AddPipesC    (DefineTegs)  {  STypePipe& obj = thePipe.SetPipeColor(teg[0].value);  def.lgPipe = obj.lg;// Цвет края  def.fnPipe = obj.fn;// Цвет центра  def.dkPipe = obj.dk;// Цвет края  }//~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~void AddPipesD    (DefineTegs)  {  STypePipe& obj = thePipe.SetPipeColor(teg[0].value);  def.lgPipe = obj.lg;// Цвет края  def.fnPipe = obj.fn;// Цвет центра  def.dkPipe = obj.dk;// Цвет края  }//~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~void AddPipeColor (DefineTegs)  {  STypePipe pipe;  lstrcpy( pipe.name, teg[0].value );  //  pipe.fn = 0xfffffe;  pipe.lg = 0x000001;  pipe.dk = 0x000001;  //  STegObj obj[] =    {    { 'A', "fon"   , &pipe.fn },    { 'A', "light" , &pipe.lg },    { 'A', "dark"  , &pipe.dk },    { 'A', "f"     , &pipe.fn },    { 'A', "l"     , &pipe.lg },    { 'A', "d"     , &pipe.dk },    };  ::ParserObjScn( teg, obj, sizeof(obj)/sizeof(STegObj) );  //  if ( pipe.lg == 0x000001 ) pipe.lg = pipe.dk;  if ( pipe.dk == 0x000001 ) pipe.dk = pipe.lg;  //  thePipe.AddPipeColor(pipe);  }//~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~