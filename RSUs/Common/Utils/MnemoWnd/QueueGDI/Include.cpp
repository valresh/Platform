#include "stdafx.h"#include "Queue.h"#include "..\Lang.h"////~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~// Структураtypedef struct : public SPROLOZ  {  } Queue_Include;//~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~int PaintInclude(DefinePaint)  {  QueueP(Queue_Include);  return 1;  }//~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~void AddInclude(DefineTegs)  {  AddQueueEx(Queue_Include);  // Код, посылаемый в АРМ  p->btSendType = QUEUE_NO;  // Определение функции вывода  p->paint = &PaintInclude;  //  bool bExt = true;  //  STegObj obj[] =    {    { 'B', "ext" , &bExt },    };  ::ParserObjScn( teg, obj, sizeof(obj)/sizeof(STegObj) );  //  // Подгружаем файл  char szPath[_MAX_PATH];char szDrv[_MAX_DRIVE];char szDir[_MAX_DIR];  _splitpath_s( def.m_queue.szName, szDrv, sizeof(szDrv), szDir, sizeof(szDir),0,0,0,0);  _makepath_s ( szPath, sizeof(szPath), szDrv, szDir, teg[0].value, NULL );  //  char szBuffer[_MAX_PATH];  GetPathScn( szBuffer, szPath, bExt );  //  char* ptr = ::FileToBuff(szBuffer);  if ( ptr )    {    CLang::ms_nStack++;    ::ParserQueue( def.m_queue, ptr, rLog );    CLang::ms_nStack--;    ::BuffFree(ptr);    }  //  }//~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~