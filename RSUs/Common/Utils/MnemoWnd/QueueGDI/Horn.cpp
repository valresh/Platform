#include "stdafx.h"#include "Queue.h"#include "..\Lang.h"#include "KeybCOM.h"#include "Sound.h"////~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~// Структураtypedef struct : public SPROLOZ  {  int nCom1;  bool bWork;  } Queue_Horn;//~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~void ClearHorn(SQueue& q,void* s)  {  //Queue_Horn* p = (Queue_Horn*)s;  SoundStop();  }//~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~// Функция отображенияint PaintHorn(DefinePaint)  {  QueueP(Queue_Horn);  //  if ( ::IsWorkModel() )    {	  // Звук может быть только при работающей модели.    CShSystem& sh = GiveSystem();    if ( IsTypeOK )      {//это уже специфика РСУ      /*bool bSound = sh.bSound && !::IsNoPCY();  		if ( bSound )	  	  SoundPlay();		  else			  SoundStop();*/      }    p->bWork = true;    }  else // Модель не работает    {    SoundStop();    //    //if ( p->nCom1 && p->bWork )    //  {    //  HWND hWnd;    //  if ( IsKeyCOM1( hWnd ) )    //  ::vMessageCOM1( hWnd, 0x11ff );    //  }    p->bWork = false;    }  return 1;  }//~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~void AddHorn(DefineTegs)  {  AddQueueEx(Queue_Horn);  // Код, посылаемый в АРМ  p->btSendType = QUEUE_NO;  // Определение функции вывода  p->paint = &PaintHorn;  p->clear = &ClearHorn;  p->bWork = true;  //  STegObj obj[] =    {    { 'R', "rect" , &p->mRect },    { 'E', "rc"   , &p->mRect },    { 'I', "com1" , &p->nCom1 },    };  //  ::ParserObjScn( teg, obj, sizeof(obj)/sizeof(STegObj) );  //  }//~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~