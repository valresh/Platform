#include "stdafx.h"#include "..\MnemoWnd.h"#include "Queue.h"#include "..\Lang.h"#include "..\Resource.h"////~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~//Структура задана в Lang.h, нужно для вызова схем //при нажатии на клавиатуру Honeywelltypedef struct : public SPROLOZ  {  char name[64];  int    nCode;  WPARAM dwAdd1;  WPARAM dwAdd2;  bool    bReal;  POINT yel;  POINT red;  int nLampNumber;  } Queue_Alarm;//~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~int PressAlarm(DefinePress)  {  static char szAlarm[256];  Queue_Alarm* p = (Queue_Alarm*)s;  if ( nCode == 0 )    {    if ( p->bReal )      {        lstrcpy( szAlarm, p->name );		if ((lstrcmp(szAlarm,"")!=0)&&(lstrcmp(szAlarm,"NULL")!=0))			::PostMsg( q.hWnd, p->btSendType, (LPARAM)szAlarm);      }    return 0;    }  //  if ( nCode == 1 )    return 0;  //  if ( *p->name == '.' || *p->name == '\0'  ) return 1;  //  if ( point.x == -1 && point.y == -1 )    {    if ( ( p->nCode & 0xff ) != nCode )      return 1;    }  else  if ( !::PtInRect( &p->mRect, point ) )    return 0;  //  if ( !p->bReal )    {    lstrcpy( szAlarm, p->name );	if ((lstrcmp(szAlarm,"")!=0)&&(lstrcmp(szAlarm,"NULL")!=0))		::PostMsg( q.hWnd, p->btSendType, (LPARAM)szAlarm);    }  return 0;  }//~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~DWORD DwAdd(int d1,int d2)  {  DWORD wAdd = 0x00;  if ( d1==0 && d2 > 0 )     wAdd = 0x100;  else  if ( d1 > 0 )    wAdd = 0x200;  return wAdd;  }//~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~//int MnemoAlarm( bool* b, int* i )  {  int M = 0;  CShSystem& sh = ::GiveSystem();  if ( IsTypeOK )    {    //int N = AreaNumb();    //for ( int n = 0; n < COUNT_UNIT; n++ )    //if  ( sh.unit[n].area[N] )    //  {    //  if ( i[n] > 0 ) M = 1;//Горение    //  if ( b[n] )  return 2;//Моргание    //  }    }  return M;  }//// Для работы с клавиатурой, подключенной к COM-портуvoid Com1(SQueue& q,Queue_Alarm* p)  {  //Sergej13  /*  CShOneMnemo& sh = (CShOneMnemo&)PipeSH;  if ( !(IsTypeOK) ) return;  HWND hWnd;  if ( q.nLedTest == 0 && IsKeyCOM1( hWnd ) )    {    WPARAM wPar1 = (p->nCode >>  8) & 0xff;    if ( wPar1 )      {	   int y = ::MnemoAlarm( sh.state1, sh.count1 );      WPARAM wAdd = 0x100*y;      if ( p->dwAdd1 != wAdd )        {        p->dwAdd1 = wAdd; wAdd += 0x100;        ::MessageCOM1( hWnd, wPar1 | wAdd );        }      }    WPARAM wPar2 = (p->nCode >> 16) & 0xff;    if ( wPar2 )      {	  int r = ::MnemoAlarm( sh.state2, sh.count2 );      WPARAM wAdd = 0x100*r;      if ( p->dwAdd2 != wAdd )        {        p->dwAdd2 = wAdd; wAdd += 0x100;        ::MessageCOM1( hWnd, wPar2 | wAdd );        }      }    }    */  }//~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~void Lampa( HDC hDC, HDC hdc, int x, int y, HBITMAP& hBmp, UINT nID )  {  if ( hBmp == NULL )    hBmp = ::LoadBitmap(g_hModule,MAKEINTRESOURCE(nID));  BITMAP info;  ::GetObject( hBmp, sizeof(BITMAP), &info);  HBITMAP oldBmp = (HBITMAP)::SelectObject( hdc, hBmp );  ::BitBlt( hDC, x, y, info.bmWidth, info.bmHeight, hdc, 0, 0, SRCCOPY );  ::SelectObject( hdc, oldBmp );  }//~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~// Функция отображенияint PaintAlarm(DefinePaint)  {  QueueP(Queue_Alarm);  //  if ( p->nLampNumber == -1 )  if ( p->nNumber == -1 )    if ( !(*p->name == '.' || *p->name == '\0') )      return 1;  //  if (p->nLampNumber!=-1)	  KKK();  bool bTest = (q.nLedTest%2)==1;  //  // Для работы с клавиатурой, подключенной к COM-порту  if ( p->nCode ) Com1( q, p );  //  bool red	= bTest;  bool yel	= bTest;  if ( q.nLedTest == 0 )    {    //if ( p->nNumber == -1 ) return 1;    //#pragma message( "убрал для собираемости\n")    /*CShOneMnemo& sh = (CShOneMnemo&)PipeSH;    if ( !(IsTypeOK) ) return 1;*/    //    //Sergej13    /*    bool bPuls = (q.m_dwMainTimer%2)==0;    // Квитированное горение лампочек или Неквитированное мерцание    int y = ::MnemoAlarm( sh.state1, sh.count1 );	  yel = ( y == 1 ) || ( y == 2 && bPuls );    int r = ::MnemoAlarm( sh.state2, sh.count2 );    red = ( r == 1 ) || ( r == 2 && bPuls );    */    }  //  if(p->nLampNumber>=0)  {       red = yel =false;       bool bPuls = (q.m_dwMainTimer%2)==0;       BYTE nValue=(BYTE)::SendMsg( q.hWnd, QUEUE_SENSGETLAMPSTATE, (LPARAM)p->nLampNumber);       if(BYTE rr = (nValue & 0x3))         {          if(rr&1)            red = bPuls? true : false;          else red = true;         }       BYTE yy = (nValue>>2) & 0x3;       if(yy)         {          if(yy&1)            yel = bPuls? true : false;          else yel = true;         }  }  //  if ( yel || red )    {    HDC hdc = ::CreateCompatibleDC( hDC );    if ( hdc != NULL )      {      if ( p->bReal )        {        static HBITMAP hBmp[2];        if ( yel ) Lampa( hDC, hdc, p->yel.x, p->yel.y, hBmp[0], IDB_1280_ONR );        if ( red ) Lampa( hDC, hdc, p->red.x, p->red.y, hBmp[1], IDB_1280_ON  );        }      else        {        static HBITMAP hBmp[2];        if ( yel ) Lampa( hDC, hdc, p->yel.x, p->yel.y, hBmp[0], IDB_BMP_ONR );        if ( red ) Lampa( hDC, hdc, p->red.x, p->red.y, hBmp[1], IDB_BMP_ON  );        }      ::DeleteDC(hdc);      }    }  return 1;  }//~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~void AddAlarm(DefineTegs)  {  AddQueueEx(Queue_Alarm);  // Код, посылаемый в АРМ  p->btSendType = QUEUE_GOTO;  // Определение функции вывода  p->paint = &PaintAlarm;  p->press = &PressAlarm;  p->dwAdd1=0x111;//иначе не сбивает в 0 при первом запуске  p->dwAdd2=0x111;//иначе не сбивает в 0 при первом запуске  //  p->yel.x = -12; p->yel.y = -12;  p->red.x =   6; p->red.y = -12;  p->nLampNumber=-1;  //  STegObj obj[] =    {    { 'R', "rect", &p->mRect },    { 'E', "rc"  , &p->mRect },    { 'H', "code", &p->nCode },    { 'L', "real", &p->bReal },    { 'P', "yel" , &p->yel },    { 'P', "red" , &p->red },	{ 'I', "lampNumber", &p->nLampNumber },    };  //  ::ParserObjScn( teg, obj, sizeof(obj)/sizeof(STegObj) );  //  p->yel.x += p->mRect.right; p->yel.y += p->mRect.top;  p->red.x += p->mRect.left ; p->red.y += p->mRect.top;  //  StrCPY( p->name, teg[0].value, sizeof(p->name) );  //  if ( !(*teg[0].value == '.' || *teg[0].value == '\0') )    {    ::CharUpper( teg[0].value );#pragma message( "обрал для собираемости\n")    //p->nNumber = def.AddKind( id_OneMnemo, teg[0].value );    }  }//~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~