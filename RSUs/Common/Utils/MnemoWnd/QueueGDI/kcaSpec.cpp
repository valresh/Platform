#include "stdafx.h"#include "..\MnemoWnd.h"#include "Queue.h"#include "..\Lang.h"//// Обработка кнопок//~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~// Структураtypedef struct : public SPROLOZ  {  char szName[64];  int nSpec;  //  } Queue_KcaSpec;//~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~// Все, кроме DD_SWOV_MODE, в Clear надо обнулитьstatic int UA_NUMBER_VESSELS_ONLINE;//Количество работающих адсорберовstatic int DD_SWOV_MODE ;// Режим переключения циклаstatic int STATE_PSA    ;// Признак работающих адсорберовstatic int nDA_PROC_REQ ;// Нажатие на адсорберах//static int nDD_ONLINE_REQ ;// нажатие ПОДКЛЮЧИТЬstatic int nDD_OFFLINE_REQ;// нажатие ИЗОЛИРОВАТЬstatic int nDD_PROC_ENABLE;// нажатие ПРИНЯТЬstatic int nDD_PROC_CANCEL;// нажатие ОТМЕНИТЬstatic bool bInit = true;//~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~void ClearKcaSpec(SQueue& q,void* s)  {  bInit = true;  }//~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~int PressKcaSpec(DefinePress)  {	  //MISHA  //Queue_KcaSpec* p = (Queue_KcaSpec*)s;  //if ( p->nRegulator != -2 )  //  return 0;  ////  //if ( nCode == 0 )  //  return 0;  //else  //if ( nCode == 1 )  //  return 0;  //else  //if ( nCode == 3 )  //  return 0;  //else  //if ( nCode == 2 )  //  {  //  bool bPtInRect = ::PtInRect( &p->mRect, point ) == TRUE;  //  if ( !bPtInRect )  //    return 0;  //  }  ////  //if (  p->nSpec == 1 )//ПОДКЛЮЧИТЬ  //  {  //  nDD_ONLINE_REQ  = 1;  //  nDD_OFFLINE_REQ = 0;  //  }  //else  //if (  p->nSpec == 2 )//ИЗОЛИРОВАТЬ  //  {  //  nDD_ONLINE_REQ  = 0;  //  nDD_OFFLINE_REQ = 1;  //  }  //else  //if (  p->nSpec == 3 )//ПРИНЯТЬ  //  {  //  nDD_PROC_ENABLE = 1;  //  //  //  SSendToModel send;  //  send.eData = id_PHS;  //  if ( nDD_ONLINE_REQ  )  //    {  //    lstrcpy( send.szName , "N_WORK_PSA" );  //    lstrcpy( send.szValue, "Включение" );  //    }  //  else  //  if ( nDD_OFFLINE_REQ  )  //    {  //    lstrcpy( send.szName , "N_DEFECT_PSA" );  //    lstrcpy( send.szValue, "Изоляция" );  //    }  //  else  //    {  //    // Здесь не должны быть!!!  //    return 0;  //    }  //  int Old = 0;  //  int New = nDA_PROC_REQ;  //  send.Set( enumValueInt, &Old, &New );  //  q.m_pPipe->SendData( send, true );  //  }  //else  //if (  p->nSpec == 4 )//ОТМЕНИТЬ  //  {  //  //nDD_PROC_CANCEL = 1;  //  nDA_PROC_REQ    = 0;  //  nDD_ONLINE_REQ  = 0;  //  nDD_OFFLINE_REQ = 0;  //  }  //else  //  {  //  nDA_PROC_REQ = p->nSpec-10;  //  }  //  return 0;  }//~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~// Функция отображенияint PaintKcaSpec(DefinePaint)  {  QueueP(Queue_KcaSpec);  //  //MISHA  //CShPolyHS& ads = (CShPolyHS&)q.m_pPipe->SH(UA_NUMBER_VESSELS_ONLINE);//Количество работающих адсорберов  //CShHS    & swv = (CShHS    &)q.m_pPipe->SH(DD_SWOV_MODE); // Режим переключения цикла  //CShPolyHS& psa = (CShPolyHS&)q.m_pPipe->SH(STATE_PSA);// Признак работающих адсорберов  //if ( ads.RetCode != CShBase::rcOK || swv.RetCode != CShBase::rcOK || psa.RetCode != CShBase::rcOK )  //  {  //  CMyPen pen( hDC, 0x80ff, 2 );  //  pen.Romb( p->mRect );  //  return 1;  //  }  ////  //RECT rc = p->mRect;  //COLORREF colorTxt = 0x80ff;  //COLORREF colorBkg = 0x80ff;  //if (  p->nSpec == 1 )//ПОДКЛЮЧИТЬ  //  {// DD_ONLINE_REQ - нажатие  //  rc.right -= 30;  //  // UA_NUMBER_VESSELS_ONLINE == 12 - неактивна  //  p->nRegulator = ( ads.m_currPos == 12 ) ? -1 : -2;  //  // UD_ONLINE_PENDING - мерцание зел¸ным квадрата  //  if ( nDD_ONLINE_REQ && q.m_dwMainTimer % 2 )  //    colorBkg = 0x00ff00;  //  // Цвет текста синий  //  colorTxt = 0xff0000;  //  }  //else  //if (  p->nSpec == 2 )//ИЗОЛИРОВАТЬ  //  {// DD_OFFLINE_REQ - нажатие  //  rc.right -= 30;  //  // UA_NUMBER_VESSELS_ONLINE == 8 - неактивна  //  p->nRegulator = ( ads.m_currPos == 8 ) ? -1 : -2;  //  // UD_OFFLINE_PENDING - мерцание зел¸ным квадрата  //  if ( nDD_OFFLINE_REQ && q.m_dwMainTimer % 2 )  //    colorBkg = 0x00ff00;  //  // Цвет текста синий  //  colorTxt = 0xff0000;  //  }  //else  //if (  p->nSpec == 3 )//ПРИНЯТЬ  //  {  //  rc.right -= 30;  //  // DD_PROC_ENABLE - нажатие  //  // DA_PROC_REQ == 0 - неактивна  //  p->nRegulator = ( nDA_PROC_REQ == 0 || swv.mOn ) ? -1 : -2;  //  // Цвет текста UD_ONLINE_PENDING OR UD_OFFLINE_PENDING == 1 - 0ч008000  //  colorTxt = ( nDD_ONLINE_REQ || nDD_OFFLINE_REQ ) ? 0x008000 : 0x828282;  //  // DD_PROC_ENABLE - мерцание текста  //  if ( nDD_PROC_ENABLE )  //    colorTxt = ( q.m_dwMainTimer % 2 ) ? 0x008000 : 0x828282;  //  // UD_SWOV_PENDING - мерцание зел¸ным квадрата (при DD_PROC_ENABLE)  //  if ( !swv.mOn )  //    {  //    if ( nDD_PROC_ENABLE )  //      colorBkg =  ( q.m_dwMainTimer % 2 ) ? 0x00ff00 : 0x828282;  //    else  //      colorBkg = 0x00ff00;  //    }  //  }  //else  //if (  p->nSpec == 4 )//ОТМЕНИТЬ  //  { // DD_PROC_CANCEL - нажатие  //  // UD_OFFLINE_PENDING OR UD_ONLINE_PENDING OR UD_SWOV_PENDING - отмена выключения  //  p->nRegulator = ( nDD_ONLINE_REQ || nDD_OFFLINE_REQ || swv.mOn ) ? -2 : -1;  //  // UD_ONLINE_PENDING OR UD_OFFLINE_PENDING == 1 красный цвет текста  //  colorTxt = ( nDD_ONLINE_REQ || nDD_OFFLINE_REQ ) ? 0x0000ff : 0x828282;  //  }  //else  //  {  //  int n = p->nSpec-10;  //  int N = 1 << n;  //  int work = (psa.m_currPos & N) != 0;  //  // Нажатие DA_PROC_REQ = n;  //  // Зел¸ный квадрат ( DA_PROC_REQ == n )  //  if ( nDA_PROC_REQ == n ) colorBkg = 0x00ff00;  //  // Красный цвет текста  UA_DOWN_PAIR_1 == n OR UA_DOWN_PAIR_2 == n  //  colorTxt = ( work ) ? 0x00 : 0xff;  //  // (UD_ONLINE_PENDING AND VESSEL_1_DOWN) OR (UD_OFFLINE_PENDING AND NOT (VESSEL_1_DOWN)) отмена выключения  //  bool a1 = nDD_ONLINE_REQ  && !work;// Подключить неработающий  //  bool a2 = nDD_OFFLINE_REQ &&  work;// Изолировать  работающий  //  p->nRegulator = (( a1 || a2 ) && !swv.mOn) ? -2 : -1;  //  //  //  }  ////  //if ( colorBkg != 0x80ff )  //  TransparentRect( hDC, p->mRect, colorBkg, 0xc0c0c0 );  ////  //COLORREF old = ::SetTextColor( hDC, colorTxt );  //::DrawText( hDC, p->szName, lstrlen(p->szName), &rc, DT_SINGLELINE|DT_VCENTER|DT_CENTER );  //::SetTextColor( hDC, old );  ////  //if ( ::GetMnemoStyle() & MNEMO_EDIT )  //  {  //  CMyPen pen( hDC, 0x80ff );  //  pen.Rect( p->mRect );  //  }  //  return 1;  }//~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~void AddKcaSpec(DefineTegs)  {  //  AddQueueEx(Queue_KcaSpec);  // Код, посылаемый в АРМ  p->btSendType = QUEUE_BUTTON;  // Определение функции вывода  p->paint = &PaintKcaSpec;  p->press = &PressKcaSpec;  p->clear = &ClearKcaSpec;  //  //MISHA  //lstrcpy( p->szName, teg[0].value );  //     if ( lstrcmpi( p->szName, "ПОДКЛЮЧИТЬ" ) == 0 ) p->nSpec = 1;  //else if ( lstrcmpi( p->szName, "ИЗОЛИРОВАТЬ") == 0 ) p->nSpec = 2;  //else if ( lstrcmpi( p->szName, "ПРИНЯТЬ"    ) == 0 ) p->nSpec = 3;  //else if ( lstrcmpi( p->szName, "ОТМЕНИТЬ"   ) == 0 ) p->nSpec = 4;  ////  //p->nRegulator = -2;  ////  //STegObj obj[] =  //  {  //  { 'R', "rect"   , &p->mRect  },  //  { 'E', "rc"     , &p->mRect  },  //  { 'I', "spec"   , &p->nSpec },  //  EXT_QUEUE  // Дополнительная информация  //  //  //  };  //::ParserObjScn( teg, obj, sizeof(obj)/sizeof(STegObj) );  //p->nNumber  = def.GetActiveObj();  //if ( bInit )  //  {  //  bInit = false;  //  UA_NUMBER_VESSELS_ONLINE= def.AddKind(id_PHS,"K_ADS"        );// Количество работающих адсорберов  //  DD_SWOV_MODE            = def.AddKind(id_HS ,"DD_SWOV_MODE" );// Режим переключения цикла  //  STATE_PSA               = def.AddKind(id_PHS,"STATE_PSA"    );// Признак работающих адсорберов  //  nDA_PROC_REQ   =0;// Нажатие на адсорберах  //  nDD_ONLINE_REQ =0;// нажатие ПОДКЛЮЧИТЬ  //  nDD_OFFLINE_REQ=0;// нажатие ИЗОЛИРОВАТЬ  //  nDD_PROC_ENABLE=0;// нажатие ПРИНЯТЬ  //  nDD_PROC_CANCEL=0;// нажатие ОТМЕНИТЬ  //  }  }//~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~