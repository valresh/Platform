#pragma oncetypedef void OnReadFunc(int, BYTE[3]);class CKbdDriver{protected:	HANDLE hdevice;	 // по прерыванию	HANDLE hdevice0; // для управления	HANDLE hThread;	DWORD dwThread;	OnReadFunc* pOnReadFunc;	void Clear();	void Read();public:	CKbdDriver(void);	~CKbdDriver(void);	void Close();	BOOL Open();	BOOL IsOpened();	BOOL SetReadFunction(OnReadFunc* function);	BOOL Write(BYTE packet[8]);	static DWORD _ReadThread(LPVOID lpParam);};