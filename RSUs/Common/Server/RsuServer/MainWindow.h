#pragma once#include <shellapi.h>#include <macros/NonCopyable.h>#include "IModelHandler.h"#include <map>#include <string>class KMainWindow : n_RSUs::NonCopyable{  enum  {    MAX_LOADSTRING = 100,    cmdExit = 102,  };public:  KMainWindow(HINSTANCE hInstance, HINSTANCE hPrevInstance, LPTSTR lpCmdLine, int nCmdShow);  ~KMainWindow(void);  BOOL Initialize();  int Work();protected:  ATOM MyRegisterClass(HINSTANCE hInstance);  BOOL InitInstance(HINSTANCE hInstance, int nCmdShow);protected:  static LRESULT WINAPI s_WndProc( HWND hWnd, UINT msg, WPARAM wParam, LPARAM lParam );  LRESULT WndProcImpl( HWND hWnd, UINT msg, WPARAM wParam, LPARAM lParam );protected:  LRESULT OnCopyData( WPARAM wParam, LPARAM lParam );protected:  LRESULT OnInitialize( LPCSTR pszDll, LPCSTR pszModelName );protected:  NOTIFYICONDATA m_NI;  HWND m_hExecWnd;  UINT m_nIconMessage;  HICON hIcon;  HINSTANCE hInst;								// current instance  char szTitle[MAX_LOADSTRING];  char szWindowClass[MAX_LOADSTRING];protected:  typedef std::map<std::string, tModelHandlerPtr_> tModelsCont_;  tModelsCont_ m_Servers;};