#include "stdafx.h"#include "SMessage.h"#undef  STDARG#define STDARG szFiltr, send.szName#undef  STDVAL#define STDVAL szFiltr, send.szName, send.szValue#undef  STDCOM#define STDCOM szFiltr, szName, szCommbool SMessage::PutMdl( SSendToModel& send, CBase* pBase, CEventString& str ){  bool bFlag = def->dwFlag != 0 /*&& pBase->ID_CLASS < id__GS*/;  //  if ( def->eVal != send.eType )  {    if ( !(bFlag && send.eType == enumValueBol) )    {      if ( send.bMessage )        str.Event( "Не совпадают типы переменной '%s'.'%s'", send.szName, send.szValue );      return false;    }  }  //  // В сообщении присутствует только имя объекта  if ( nOnlyName == 1 && send.bMessage )    send.bMessage = false, str.Event( STDARG );  // Просто сообщение без параметров  if ( nOnlyName == 2 && send.bMessage )    send.bMessage = false, str.Event( szFiltr );  //  void* v = (void*)((char*)(pBase) + def->dwShift);  //  // Обработка флагов  if ( bFlag )    return PutFlg(send,(int*)v,str);  else    switch ( send.eType )  {    case enumValueDbl: return PutDbl(send,(double*)v,str);    case enumValueInt: return PutInt(send,(int   *)v,str);    case enumValueChr: return PutChr(send,(char  *)v,str);    case enumValueBol: return PutBol(send,(bool  *)v,str);    case enumValueFlt: return PutFlt(send,(float *)v,str);  }  return false;}bool SMessage::PutDbl( SSendToModel& send, double* v, CEventString& str ){  *v = send.dNew;  if ( !(send.bMessage && send.dOld != send.dNew) ) return true;  //  if ( nAsString )    str.Double2( STDARG, send.dOld, send.dNew );  else    str.Event  ( STDARG, send.dOld, send.dNew );  //  return true;}bool SMessage::PutFlt( SSendToModel& send, float* v, CEventString& str ){  *v = send.fNew;  if ( !(send.bMessage && send.fOld != send.fNew) ) return true;  //  if ( nAsString )    str.Double2( STDARG, send.fOld, send.fNew );  else    str.Event  ( STDARG, send.fOld, send.fNew );  //  return true;}bool SMessage::PutInt( SSendToModel& send, int* v, CEventString& str ){  // Частный случай  if ( lstrcmp( this->def->name, "ON/OFF" ) == 0 )  {    *v |= send.nNew;    send.nNew = send.nOld;    send.nOld++;  }  else    *v  = send.nNew;  //  if ( !(send.bMessage && send.nOld != send.nNew) ) return true;  //  if ( nAsString == 0 )    str.Event( STDARG, send.nOld, send.nNew );  else    if ( nAsString == 1 )    {      int nOld = min(send.nOld,15); nOld = max(nOld,0);      int nNew = min(send.nNew,15); nNew = max(nNew,0);      str.Event( STDARG, szTxt[nOld], szTxt[nNew] );      if ( send.nOld < 0 || send.nOld > 15 )        return false;      if ( send.nNew < 0 || send.nNew > 15 )        return false;    }    else      if ( nAsString == 2 )      {        int nNew = min(send.nNew,15); nNew = max(nNew,0);        str.Event( STDARG, szTxt[nNew] );        if ( send.nNew < 0 || send.nNew > 15 )          return false;      }      else        if ( nAsString == 3 )        {          str.Event( szFiltr );        }        //        return true;}bool SMessage::PutChr( SSendToModel& send, char* v, CEventString& str ){  *v = send.cNew;  if ( !(send.bMessage && send.cOld != send.cNew) ) return true;  //  if ( nAsString == 0 )    str.Event( STDARG, send.cOld, send.cNew );  else    if ( nAsString == 1 )    {      char* unk = "Unknown";      char* szOld = NULL;      char* szNew = NULL;      for ( int n = 0; n < 16; n++ )      {        if ( *szTxt[n] == send.cOld ) szOld = szTxt[n], szOld++;        if ( *szTxt[n] == send.cNew ) szNew = szTxt[n], szNew++;      }      //      if ( szOld == NULL ) szOld = unk;      if ( szNew == NULL ) szNew = unk;      //      str.Event( STDARG, szOld, szNew );      if ( szOld == unk || szNew == unk )        return false;    }    else      if ( nAsString == 2 )      {        char* unk = "Unknown";        char* szNew = NULL;        for ( int n = 0; n < 16; n++ )          if ( *szTxt[n] == send.cNew ) szNew = szTxt[n], szNew++;        //        if ( szNew == NULL ) szNew = unk;        //        str.Event( STDARG, szNew );        if ( szNew == unk )          return false;      }      //      return true;}bool SMessage::PutBol( SSendToModel& send, bool* v, CEventString& str ){  *v = send.bNew;  if ( !(send.bMessage && send.bOld != send.bNew) ) return true;  //  int nOld = int(send.bOld);  int nNew = int(send.bNew);  if ( nAsString == 0 )    str.Event( STDARG, nOld, nNew );  else    if ( nAsString == 1 )      str.Event( STDARG, szTxt[nOld], szTxt[nNew] );    else      if ( nAsString == 2 )// Только новое значение        str.Event( STDARG, szTxt[nNew] );  //  return true;}bool SMessage::PutFlg( SSendToModel& send, int* v, CEventString& str ){  bool bFlag = ( *v & int(def->dwFlag) ) == def->dwFlag;  if ( send.bNew )  {    if ( bFlag ) return true;    *v ^= def->dwFlag;  }  else  {    if ( !bFlag ) return true;    *v ^= def->dwFlag;  }  //  if ( !send.bMessage ) return true;  //  int nOld = int(send.bOld);  int nNew = int(send.bNew);  if ( nAsString == 0 )    str.Event( STDARG, nOld, nNew );  else    if ( nAsString == 1 )      str.Event( STDARG, szTxt[nOld], szTxt[nNew] );    else      if ( nAsString == 2 )// Только новое значение        str.Event( STDARG, szTxt[nNew] );  //  return true;}