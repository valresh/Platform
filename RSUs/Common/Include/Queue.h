#pragma once//#undef QUEUEAPM_API#ifdef QUEUEAPM_EXPORTS#define QUEUEAPM_API Q_DECL_DLLEXPORT     #else#define QUEUEAPM_API Q_DECL_DLLIMPORT     #endif//#include <SoftGDI.h>#include <common.h>#include "..\Utils\MnemoWnd\SQueue.h"//struct SCmdLine  {  bool bInstructor;   // I  bool bSound;   // A  char szHIS[16];// H  bool bOnlyField;//F  bool bCAMS;//C  bool bUserFromCmd;  bool bFixFacePlate; //R  bool bAllowBigSchemas;//B  bool bDisableKirishiView;//K - отключаем расположение 9 окон на одном мониторе  bool bWindowModeFlex;//W для 139 позволяет окну сворачиваться  bool bDebugKirishiMonitorsInfo;//D для 140 выводит информацию о мониторах.  bool bUsualDisplay;//U 178 неширокий экран   int nMonitor0SpecNumber;//-m2 2-ой монитор  int nProjectNumber;//-p1 1-ый проект  bool bLocalPlaceFromCmd;  };//struct SChangeValue  {  char text[128];  double dMin,dMax;  double* pValue;  class CPipeClient* pipe;  int nNumber;  struct SValueDef* def;  DWORD  dwFlags;  };//struct SQueuePopup  {  CharMP szFile,szTitl,szName,szText;  int x,y,w,h,Z;  DWORD StyleAdd;  };//~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~typedef HGLOBAL (WINAPI* GsrvR)(char*);//~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~typedef UINT (*LPGetWriteInt)( LPCTSTR key, LPCTSTR entry, int nDefault);extern  QUEUEAPM_API LPGetWriteInt fnReadInt;extern  QUEUEAPM_API LPGetWriteInt fnSaveInt;typedef HWND (*LPLoadPult)( HWND hParent, HWND hPostM, LPCTSTR pszPult, bool bPopup );extern  QUEUEAPM_API LPLoadPult fnLoadPult;//~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~extern bool QUEUEAPM_API g_bFastTime;extern bool QUEUEAPM_API g_bAlphaShift;//~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~void    QUEUEAPM_API MainTimer(HWND hWnd,DWORD dwMainTimer);void    QUEUEAPM_API OnTimer(HWND hWnd, UINT_PTR nIDEvent);void    QUEUEAPM_API PaintDlg(HWND hWnd,HDC hDC,DWORD dwMainTimer);void    QUEUEAPM_API SetMessageWnd(HWND hWnd,HWND hMessageWnd,bool obj = false);BOOL    QUEUEAPM_API ParserCmdLine(char* ptr,SCmdLine& cmd);bool    QUEUEAPM_API IsPriborPresent(HWND hWnd);int     QUEUEAPM_API Mouse(HWND hWnd,UINT uMsg,WPARAM wParam,POINT point);bool    QUEUEAPM_API IsCurrentTex(HWND hWnd,const char* pszName);void    QUEUEAPM_API SetCurrentTex(HWND hWnd,const char* pszName);void    QUEUEAPM_API RegionPressed( HWND hWnd );LRESULT QUEUEAPM_API PostMsg( HWND hWnd, WPARAM wParam, LPARAM lParam );LRESULT QUEUEAPM_API SendMsg( HWND hWnd, WPARAM wParam, LPARAM lParam );GsrvR   QUEUEAPM_API GSRV( const char* pszExt );HGLOBAL QUEUEAPM_API ImageFile( const char* pszFile );void    QUEUEAPM_API PaintRegimChar( HDC hDC, int x, int y, int n );void    QUEUEAPM_API OpenMnemoWnd( HWND hParent, HWND hMessage, HWND& hWnd, char* pszWnd, bool bCaption, POINT initialPosition, bool bShow );void    QUEUEAPM_API OpenMnemoWnd( HWND hParent, HWND hMessage, HWND& hWnd, char* pszWnd, bool bCaption = true, bool bShow = true );HWND    QUEUEAPM_API OpenMnemoWnd( HWND hWnd, char* pszWnd, bool bCaption = true);HWND    QUEUEAPM_API FindMnemoWnd( HWND hWnd, char* pszWnd );void	QUEUEAPM_API GetStaffFilePath (char *szFile, int nSize);void    QUEUEAPM_API CheckPhotoCapture ();//void  QUEUEAPM_API mvaluef( char* dst, int dim, double src );DWORD QUEUEAPM_API GetMnemoStyle();void  QUEUEAPM_API SetMnemoStyle(DWORD dwMnemoStyle);DWORD QUEUEAPM_API MnemoStyleXOR(DWORD dwFlags);bool  QUEUEAPM_API IsMnemoStyle(DWORD dwFlag);//void QUEUEAPM_API Time( char* dst, __time64_t nTime );void QUEUEAPM_API Data( char* dst, __time64_t nData );void QUEUEAPM_API Data2( char* dst, __time64_t nData );void QUEUEAPM_API Data3( char* dst, __time64_t nData );//~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~// Параметры, которые можно передать в командной строке (-qX)#define MNEMO_EDIT      0x0000001 // Редактирование мнемосхем#define MNEMO_TEST      0x0000002 // Настройка PID-ов#define MNEMO_ONLY      0x0000004 // При настройке не вызывать стандартный диалог#define MNEMO_COMMENT   0x0000008 // Отладочный режим#define MNEMO_HIDE_CTRL 0x0000010 // Невидимость контролей ToolBar & StatusBar#define MNEMO_KCA       0x0000020 // АРМ КЦА#define MNEMO_HIDE_SCN  0x0000040 // Показ скрытой мнемосхемы#define MNEMO_PRIBOR    0x0000100 // Показывать регионы нажатия#define MNEMO_METAFILE  0x0000200 // Отображать тех. схему метафайлом#define MNEMO_BITMAPS   0x0000400 // Отображать битмапы как прямоугольники#define MNEMO_WORK_1    0x0000800 // Выводить значения со стандартных мнемосхем 1=#define MNEMO_WORK_2    0x0001000 // Выводить значения со стандартных мнемосхем 2=#define MNEMO_WORK_3    0x0002000 // Выводить значения со стандартных мнемосхем 3=#define MNEMO_BBOX      0x0004000 // Выводить рамки устройств на технологической схеме#define MNEMO_TIME      0x0008000 // Выводить время отрисовки и связи с сервером#define MNEMO_ANCHOR    0x0010000 // Прокручивать изображение рывками#define MNEMO_NOTIME    0x0020000 // Отключить вывод по таймеру#define MNEMO_NAMES     0x0040000 // Вывод имен датчиков на схеме (для компрессоров)//~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~// Для сообщения  nPressQueue:#define QUEUE_NO         0#define QUEUE_GOTO       1 //Переход на другую мнемосхему#define QUEUE_FACEPLATE  2 //Нажатие на объекте#define QUEUE_PUMP       3 //Нажатие на объекте#define QUEUE_KLAP       4 //Нажатие на объекте#define QUEUE_POPUP      5 //Создание окна с мнемосхемой#define QUEUE_FORSUNKA  14#define QUEUE_HWKBD     16 //Нажатие на клавиатуре Honeywell#define QUEUE_OLE       17#define QUEUE_TIMER     18#define QUEUE_REZINA    19#define QUEUE_LOGIC     20#define QUEUE_ACK       21#define QUEUE_ISNOACK   22#define QUEUE_TIMESELECT  23 //для управления ole кнопками#define QUEUE_BUTTONSTATE 24 //для управления ole кнопками#define QUEUE_SENSKEYBOARD 25 //для управления с сенсорной клавиатруры#define QUEUE_SENSGETLAMPSTATE 26 //для получения значения состояния лампы сенс. клав.#define QUEUE_SENSKEY_STR_OFF 27 //для управления с сенсорной клавиатруры <<>> стрелкой#define QUEUE_SPBTN     28 //Нажатие на специфичных кнопках#define QUEUE_BTNRECTDLG  29#define QUEUE_BTNRECTFACE 30//#define QUEUE_GROUP     50 // Переход на группу#define QUEUE_BUTTON    51 // Кнопка#define QUEUE_TREND_W   52 // Посылка сообщения окну#define QUEUE_ALARM_W   53 // Посылка сообщения окну#define QUEUE_CHILD_NEW 54 // Посылка сообщения окну#define QUEUE_CHILD_NXT 55 // Посылка сообщения окну#define QUEUE_SCALE     56 // Посылка сообщения окну#define QUEUE_TAB       57#define QUEUE_EDIT_W    58#define QUEUE_POINTNAME 59#define QUEUE_FLOAT     60#define QUEUE_COMBO_W	61#define QUEUE_GET_COMBO 62#define QUEUE_SELCHANGE 63#define QUEUE_SETEDITGREEN 64#define QUEUE_GETEDITGREEN 65#define QUEUE_FACEPLATEWATER 66#define QUEUE_FACEPLATE_WND 67#define QUEUE_SELRECT       68#define QUEUE_SELPOINT      69#define QUEUE_HIDEEDIT      70#define QUEUE_PASSWORD_ENTERED 71#define QUEUE_EXCHANGE_MONITORS 72 //Обмен схемами между окнами#define QUEUE_HSCONTROLNAME 73#define IS_USED_REMOTE_CONTROL 0////~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~// Для сообщения  nBuildQueue:#define BUILD_MNEMO      0 // Открытие scn-файла #define BUILD_SXEMA      1 // Получение имени технологического файла#define BUILD_ALARMS     2 // Массив с указателями сигнализаций#define BUILD_SELECT     5 // Выделение объекта#define BUILD_SETOBJ     6 // Установка имени объекта до построения очереди#define BUILD_COM1	     7 // Переход на схему при нажатии клавиатуры Honeywell#define BUILD_MNEMOBAR   8 // Сохранение имени схемы в окне DownBar#define BUILD_SELRECT    9 // Сохранение select rectа #define BUILD_MNEMO_EX   10 // Открытие scn-файла с сохранением фокуса при клике вне клавиатуры#define BUILD_MNEMO_TSW  11 // Открытие scn-файла в том же окне на сенсороной клавиатуре//~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~#ifdef __MAIN_FILE__  UINT nPressQueue = ::RegisterWindowMessageA("Объект мнемосхемы");  UINT nBuildQueue = ::RegisterWindowMessageA("Открыть мнемосхему");#else  extern UINT nPressQueue;  extern UINT nBuildQueue;#endif//~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~class CMyWT  {  public:    //    CMyWT( HDC hDC, FLOAT fScale, POINT& ptOffset )    : m_hDC( hDC )      {      m_nGraph = ::SetGraphicsMode( hDC, GM_ADVANCED );      ::GetWorldTransform( hDC, &xT );      FLOAT dx = -FLOAT(ptOffset.x);      FLOAT dy = -FLOAT(ptOffset.y);      XFORM xf = {fScale,0,0,fScale,dx,dy};      ::SetWorldTransform( hDC, &xf );      }    //    CMyWT( HDC hDC, FLOAT fScaleX, FLOAT fScaleY, POINT& ptOffset )    : m_hDC( hDC )      {      m_nGraph = ::SetGraphicsMode( hDC, GM_ADVANCED );      ::GetWorldTransform( hDC, &xT );      FLOAT dx = -FLOAT(ptOffset.x);      FLOAT dy = -FLOAT(ptOffset.y);      XFORM xf = {fScaleX,0,0,fScaleY,dx,dy};      ::SetWorldTransform( hDC, &xf );      }    //    CMyWT( HDC hDC, double fScaleX, double fScaleY )    : m_hDC( hDC )      {      m_nGraph = ::SetGraphicsMode( hDC, GM_ADVANCED );      ::GetWorldTransform( hDC, &xT );      XFORM xf = {FLOAT(fScaleX),0,0,FLOAT(fScaleY),0,0};      ::SetWorldTransform( hDC, &xf );      }    //    CMyWT( HDC hDC, SQueue& q )    : m_hDC( hDC )      {      m_nGraph = ::SetGraphicsMode( hDC, GM_ADVANCED );      ::GetWorldTransform( hDC, &xT );      FLOAT dx = -FLOAT(q.setShift.x);      FLOAT dy = -FLOAT(q.setShift.y);      XFORM xf = {q.setScale,0,0,q.setScale,dx,dy};      ::SetWorldTransform( hDC, &xf );      }    //    virtual ~CMyWT()      {      ::SetWorldTransform( m_hDC, &xT );      ::SetGraphicsMode( m_hDC, m_nGraph );      }  private:    HDC      m_hDC;    int   m_nGraph;    XFORM xT;  };//~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~// Трансформация точкиinline void PtWT( POINT& point, SQueue& q )  {  point.x += q.setShift.x;  point.y += q.setShift.y;  point.x = int(double(point.x)/q.setScale);  point.y = int(double(point.y)/q.setScale);  }inline void PtWt( POINT& point, SQueue& q )  {  ::GetCursorPos( &point );  ::ScreenToClient( q.hWnd, &point );  PtWT( point, q );  }//~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~//#include "InlineGDI.h"#include "DataTypes.h"#include "PipeClient.h"#include "FormatScn.h"//~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~#define CLASS_MNEMO "MnemoSxema29"#define CLASS_TEXNO "TexnoSxema29"//~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~enum AlarmWindowType{   A_Alarms = 1,   A_History =2};struct QUEUE_ALARM_W_STRUCT{   RECT rect;   DWORD Tag55556666;   AlarmWindowType Type;   bool bHeader;};//~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~typedef struct{    HWND  hWnd;    POINT DownPoint;    POINT UpPoint;    POINT CurrentPoint;    int   MaxDelta;    bool bMouseDown;    void (*pDrawSlider)(HWND hwnd);    Bitmap* SliderBmp;} _QueueWndMouseState;extern _QueueWndMouseState QueueWndMouseState;//~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~