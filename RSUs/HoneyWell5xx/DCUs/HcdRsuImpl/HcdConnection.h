#pragma once#include <HcdMemoryService.h>#include <HcdVarTypes.h>struct SHcdConnection{  struct SPin  {    eHcdVType type;    union    {      tDigital32* pdigVal;      tAnalog32*  panaVal;      tBool*      pbVal;    };    tHcdVarSizeType_ size;  };  NEWHCD  int sourceID, sinkID;  char szSourcePin[32], szSinkPin[32];  char szDataType[32];  char szBlkSource[32], szBlkSink[32];  SPin sourcePin, sinkPin;  void Transfer();};