#include "stdafx.h"#include "Translator.h"#include <algorithm>namespace ns_HcdTranslator{  /*bool KHcdTranslator::IsVar( LPCSTR pszName, LPCSTR pszBlock, SExecVariable &Var )  {    if( !m_pResponser )      return false;    return m_pResponser->IsVar( pszName, pszBlock, Var );  }*/  int KHcdTranslator::AddVar( LPCSTR pszName, KVar::eType Type )//, SExecVariable &Var )  {    tVarCont_::iterator it = std::find( m_Vars.begin(), m_Vars.end(), pszName );    if( m_Vars.end()!=it )    {      return (int)std::distance( m_Vars.begin(), it );    }    m_Vars.push_back( KVar(pszName, Type/*, Var*/) );    int N = (int)m_Vars.size()-1;    return N;  }}