#include "stdafx.h"#include <RsuX.h>#include "SM_Server.h"#include <ConnectSM.h>#include "SMsPool.h"extern nRSUx::SParamInfo RsuConnectParamInfo( LPCSTR pszPointName, LPSTR pszIOtype, LPCSTR pSuffix );KSMsPool g_Pool;nRSUx::SParamInfo GetSourceFromSMImpl(LPCSTR pszController, int nPlcAddress, bool Direction ){  nRSUx::SParamInfo pnt;  SDotValue *pV = NULL;  void **pIO = NULL;  bool r = g_Pool.FindPoint( pszController, nPlcAddress, &pV, &pIO );  if( r && pV )  {    switch( pV->eType )    {    case enumValueDbl:      pnt << &pV->dVal;      if ( Direction )        *(double**)pIO = &pV->dVal;      break;    case enumValueBol:      pnt << &pV->bVal;      if ( Direction )        *(bool**)pIO = &pV->bVal;      break;    default:      ASS(0);    }  }  return pnt;}LPCSTR g_szPntsFilePrefix = "РСУ_SMH_";KServer::KServer( LPCSTR pszModelName ): m_hWndThread( 0 ), m_hCmdWnd( 0 ), m_timeAccum( 0 ), m_nCalls( 0 ){  size_t prefixLen = strlen(g_szPntsFilePrefix);  LPCSTR pszRealName = pszModelName;  if( !_strnicmp( pszModelName, g_szPntsFilePrefix, prefixLen) )    pszRealName += prefixLen;  strcpy_s( m_szRealName, pszRealName );  m_bInited = m_bStep0 = m_bStep1 = false;  g_Pool.RegisterSM( this );  pGetSourceFromSM = GetSourceFromSMImpl;}LPCSTR KServer::GetCsvPrefix(){  return g_szPntsFilePrefix;}int KServer::Initialize(){  if( m_bInited )    return 0;  m_bInited = true;  if( pRegisterRsuConnection )    pRegisterRsuConnection( GetCsvPrefix(), RsuConnectParamInfo, NULL );  DWORD ThreadId;  m_hWndThread = ::CreateThread( NULL, 0, s_WndThread, (void*)this, 0, &ThreadId );  while( !m_hCmdWnd )    Sleep( 100 );  if( m_hCmdWnd )    SendMessage( m_hCmdWnd, WM_USER+1, 2, 0 );  return KSafetyManager::Initialize( m_szRealName );}int KServer::AfterInit(){  if( m_bStep0 )  {  }  m_bStep0 = true;  return 0;}int KServer::AfterRestored(){  m_timeAccum = 0;  m_nCalls = 0;  return 0;}int KServer::CalcStep( int dtMs ){  double dtS = dtMs / 1000.;  m_timeAccum += dtS;  ++m_nCalls;  return __super::StepTL( dtS );}double KServer::GetTimeAccum(){  return m_timeAccum;}__int64 KServer::GetNSteps(){  return m_nCalls;}DWORD KServer::s_WndThread( LPVOID lpParam ){  KServer* p = (KServer*)lpParam;  p->WndThread();  return 0;}namespace ns_details{  static LPCSTR s_szWndClassName = "SM5xxServer_ModelWindowClass";  static int s_flagRegisteredWindow = 0;}extern HINSTANCE g_hInst;void KServer::WndThread(){  if( !ns_details::s_flagRegisteredWindow )  {    WNDCLASSEX wc;    ::memset ( &wc, 0, sizeof( wc ) );    wc.cbSize = sizeof(WNDCLASSEX);    wc.lpfnWndProc = s_WndProc;    wc.hInstance     = g_hInst;    wc.lpszClassName = ns_details::s_szWndClassName;    ::RegisterClassEx( &wc );  }  ++ns_details::s_flagRegisteredWindow;  HWND hCmdWnd = ::CreateWindow( ns_details::s_szWndClassName, 0, WS_DISABLED, 0, 0, 0, 0, 0, 0, g_hInst, this );  if( !hCmdWnd )  {    return;  }  ::SetWindowLongPtr( hCmdWnd, GWLP_USERDATA, (LONG_PTR)this );  MSG msg;  while (GetMessage(&msg, NULL, 0, 0))  {    TranslateMessage(&msg);    DispatchMessage(&msg);  }}LRESULT KServer::s_WndProc( HWND hWnd, UINT msg, WPARAM wParam, LPARAM lParam ){  switch( msg )  {  case WM_CREATE:    {      CREATESTRUCT *pCreate = (CREATESTRUCT*)lParam;      if( pCreate->lpCreateParams )      {        KServer* p = reinterpret_cast<KServer*>(pCreate->lpCreateParams);        if( p )          return p->WndProcImpl( hWnd, msg, wParam, lParam );      }    }    break;  case WM_DESTROY:    break;  }  LONG_PTR lg = GetWindowLongPtr( hWnd, GWLP_USERDATA);  if( lg )  {    KServer* p = reinterpret_cast<KServer*>(lg);    if( p )      return p->WndProcImpl( hWnd, msg, wParam, lParam );  }  return ::DefWindowProc( hWnd, msg, wParam, lParam );}LRESULT KServer::WndProcImpl( HWND hWnd, UINT msg, WPARAM wParam, LPARAM lParam ){  switch( msg )  {  case WM_CREATE:    m_hCmdWnd = hWnd;    return 0;  case WM_USER+1:    if( 1==wParam )    {      size_t prefixLen = strlen(g_szPntsFilePrefix);      LPCSTR pszRealName = m_szShowObject;      if( !_strnicmp( pszRealName, g_szPntsFilePrefix, prefixLen) )        pszRealName += prefixLen;      if( _stricmp(pszRealName, m_szRealName) )        break;      ShowACSObject( NULL, m_szRealName, NULL );    }    /*else if( 2==wParam )      Ks::InitL();*/    break;  }  return ::DefWindowProc( hWnd, msg, wParam, lParam );}void KServer::ShowObject( LPCSTR pszName ){  if( m_hCmdWnd )  {    strcpy_s( m_szShowObject, pszName );    SendMessage( m_hCmdWnd, WM_USER+1, 1, (LPARAM)m_szShowObject );  }}LPCSTR KServer::GetName(){  return m_szRealName;}static LPCSTR s_ExperionServer = "EPKS_CCB"; // для 319_VSB_KFbool KServer::FindPoint( int nPlcAddress, SDotValue** ppV, void ***ppIO ){  int nItem = 0;  for( LFscBase* base = mList.Item( nItem ); base; ++nItem, base = mList.Item( nItem ) )  {    int nName = -1;    switch( base->mT )    {    case 0x0B:    case 0x0C:    case 0x46:      {        SPointIn* data = (SPointIn*)mBuff.Data( base->nBuff );        *ppV = DotV(data->nOut);        *ppIO = &data->In;        nName = data->nDbf;      }      break;    case 0x19:    case 0x1A:    case 0x47:      {        SPointOut* data = (SPointOut*)mBuff.Data( base->nBuff );        *ppV = DotV(data->nIn);        *ppIO = &data->Out;        nName = data->nDbf;      }      break;    default:      continue;    }    if( nName>=m_kFSC_Info )      continue;    CFSC_Info &Info = m_FSC_Info[nName];    for ( int i=0; i<6; ++i )    {      if( nPlcAddress==Info.PLCADDR[i] && !_strcmpi(Info.szMaster[i], s_ExperionServer) )      {        if( ppV )        {          ASSD( !_strcmpi(Info.szSlave[i], m_szRealName) );          return true;        }      }    }    *ppV = NULL;    *ppIO = NULL;  }  return false;}