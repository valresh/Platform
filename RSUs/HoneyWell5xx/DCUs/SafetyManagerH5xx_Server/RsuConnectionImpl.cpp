#include "StdAfx.h"#include <RsuX.h>#include <MemoryServiceSM5xx.h>#include <rsuNoNames.h>#include <BaseType.h>#include <UtilsSM.h>#include <SafetyManagerStruct.h>#include <macros/StrHelps.h>#pragma comment( lib, "UtilsSM5xx.lib" )using namespace nRSUx;extern KNoName g_IOs;SParamInfo RsuConnectParamInfo( LPCSTR pszPointName, LPSTR pszIOtype, LPCSTR pSuffix ){  LPSTR p = strrchr( (LPSTR)pszPointName, '.');  SParamInfo info;  const char* pszName;  DWORD ID = 0;  CBase* pBase = NULL;  int nFcs = 0;  bool bF = false;  TStringTerminator st( p );  while( g_IOs.WhileBase( ID, -1/*id_Unknown*/, &pBase, &pszName, &nFcs ) )  {    if( _strcmpi(pszName, pszPointName) || pBase->ID_CLASS == W_SMFLD::TypeID )      continue;    if( !p )    {      switch( pBase->ID_CLASS )      {      case W_SMDISCRET::TypeID:        p = " PVFL";        break;      case W_SMANALOG::TypeID:        p = " PV";        break;      case W_SMAI::TypeID:        p = " PV";        break;      case W_SMAO::TypeID:        p = " OP";        break;      case W_SMDOCOM::TypeID:        p = " PVFL";        break;      case W_SMDI::TypeID:        p = " PVFL";        break;      case W_SMDO::TypeID:        p = " OPFL";        break;      case W_SMDICOM::TypeID:        p = " PVFL";        break;      default:        ASSD(0);        break;      }    }    if( !p )      continue;    SValueDef* def = NameToValueSM( pBase->ID_CLASS, p+1 );    if( !def )      continue;    //++p;    BYTE *pMem = (BYTE*)pBase;    pMem += def->dwShift;    switch( def->eVal )    {    case enumValueDbl:      info << (double*)pMem;      break;    case enumValueBol:      info << (bool*)pMem;      break;    default:      ASSD(0);      return info;    }    break;  }  return info;}