#include "StdAfx.h"#include "FscList.h"static char* sid = "BUFFER INVERTER b:=NOT(a)";//struct SLogic27  {  UINT I;// Вход  UINT O;// Выход  };//bool SFscList::FuncParser27( VAR_PARSER ){  FuncParserdo(fsc,p,dat,tmp);  //  SLogic27 io;  // Выходные точки  SDotValue O(enumValueBol);  io.O = fsc->AddFscDots(&O);  // Входные точки  io.I = -1;  // Заказываем память  p.nBuff = fsc->AddFscBuff( &io, sizeof(io) );  //  ns_UT::SLocalTmp* item = tmp.Item();  item->nCount = 1;  item->dim[0] = io.O;  return true;}////void FuncDebugs27( VAR_DEBUGS )//  {//  strcat_s( str, sid );//  strcat_s( str, "\r\n------------------------------------------------------------\r\n" );//  FuncDebugsdo( p, str );//  }//void SFscList::FuncPaints27( VAR_PAINTS ){  SLogic27* dat = (SLogic27*)fsc->Data(p.nBuff);  //  RECT rcl = p.rect; rcl.right -= 6;  RECT rcr = p.rect;  rcr.left   = rcl.right;  rcr.top    = (rcl.top+rcl.bottom)/2-3;  rcr.bottom = (rcl.top+rcl.bottom)/2+3;  //  CMyPen pen( font, p.Color( nSelect, dat->I, dat->O ) );  pen.Rect( rcl );  font.Draw( rcl, "1" );  //  CMyBrush brush( font, 0x101010, true );  pen.Ellipse( rcr );}//void SFscList::FuncSetLnk27( VAR_SETLNK ){  // Вход только справа  if ( x == p.rect.right )     return;  SLogic27* dat = (SLogic27*)fsc->Data(p.nBuff);  dat->I = nLink;}////-----------------------------------------------------------------------------//UINT FuncCursor27( VAR_CURSOR )//  {//  SLogic27* dat = (SLogic27*)fsc->Data(p.nBuff);//  int x = (p.rect.left+p.rect.right)/2;//  return ( pt.x < x ) ? dat->I : dat->O;//  }////-----------------------------------------------------------------------------UINT* SFscList::FuncOutput27( VAR_OUTPUT ){  SLogic27* dat = (SLogic27*)fsc->Data(p.nBuff);  n = 1;  return &dat->O;}////-----------------------------------------------------------------------------void SFscList::FuncTimers27( VAR_TIMERS ){  SLogic27* dat = (SLogic27*)fsc->Data(p.nBuff);  SDotValue* O = fsc->DotV(dat->O);  if ( O->dwFlags & 1 )    return;  SDotValue* I = fsc->DotV(dat->I);  bool i = I->Bol();  O->Set( !i );}////-----------------------------------------------------------------------------static LPCSTR s_pName[] = { "O", "I" };int SFscList::FuncQuickWatch27( IFscStorage* fsc, LFscBase *obj, IFscStorage::SVarInfo *pvi, int cVI, LPCSTR *ppPntName ){  SLogic27* dat = (SLogic27*)fsc->Data(obj->nBuff);  SDotValue* vals[] = { fsc->DotV(dat->O), fsc->DotV(dat->I) };  int n = 0;  for( n; n<_countof(vals) && n<cVI; ++n )  {    pvi[n].val = vals[n];    strcpy_s( pvi[n].szName, s_pName[n] );  }  return n;}int SFscList::SaveState27( IStateSer *psaver, IFscStorage* fsc, LFscBase *obj ){  SLogic27* dat = (SLogic27*)fsc->Data(obj->nBuff);  SDotValue* vals[] = { fsc->DotV(dat->O) };  for( int i=0; i<_countof(vals); ++i )  {    psaver->WriteDotV( vals[i], s_pName[i] );  }  return 0;}int SFscList::RestoreState27( IStateSer *prest, IFscStorage* fsc, LFscBase *obj ){  SLogic27* dat = (SLogic27*)fsc->Data(obj->nBuff);  SDotValue* vals[] = { fsc->DotV(dat->O) };  for( int i=0; i<_countof(vals); ++i )  {    prest->ReadDotV( vals[i], s_pName[i] );  }  SDotValue* I = fsc->DotV(dat->I);  SDotValue* O = fsc->DotV(dat->O);  bool o = O->Bol();  I->Set( !o );  return 0;}