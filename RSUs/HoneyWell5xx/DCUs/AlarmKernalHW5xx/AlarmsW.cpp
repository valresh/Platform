#include "stdafx.h"#include "AlarmsW.h"#include <rsuPaths.h>#include <macros/AutoCloser.h>#include <UtilsH.h>#include <rsuNoNames.h>#include <macros/AutoCloser.h>#include <macros/StrHelps.h>KNoName g_Objs, g_qbIOs;bool SAlarmsW::BuildAll(){  g_Objs = KNoName::Create( "H5xx_Objs", false );  g_qbIOs = KNoName::Create( "QB5xx_IOs", false );  const char g_chVer = 3;  char szXml[_MAX_PATH],szBin[_MAX_PATH];  _makepath_s( szXml, NULL, rsuGetAcyPath(), "mnemo", "xml" );  ::GetBinFile( szBin, szXml, "alarm" );  char szAlmGrXml[_MAX_PATH];  _makepath_s( szAlmGrXml, NULL, rsuGetAcyPath(), "Alarm Groups.cnf", "xml" );  //  _WIN32_FILE_ATTRIBUTE_DATA file[5] = {};  //  bool bRead = true, bReturn = false;  if ( ::GetAttr( szXml, file[0] ) || ::GetAttr( szAlmGrXml, file[4] ) )  {    DWORD dwSize = 0;    KAutoCloser<char*> szBuffer( ::FileToBuff( szBin, dwSize ), ::BuffFree );    if( szBuffer )    {      char* ptr = szBuffer;      if ( *ptr == g_chVer )//Версия      {        ptr++;        FILETIME* F = (FILETIME*)ptr;        if ( ::EqAttr( file[0], F[0] ) )//Время последнего изменения          if ( ::EqAttr( file[1], F[1] ) )//Время последнего изменения            if ( ::EqAttr( file[2], F[2] ) )//Время последнего изменения              if ( ::EqAttr( file[3], F[3] ) )//Время последнего изменения                if ( ::EqAttr( file[4], F[4] ) )//Время последнего изменения                {                  ptr += 5*sizeof(FILETIME);                  mHoney.Read( ptr );                  mMnemo.Read( ptr );                  mIndex.Read( ptr );                  mChars.Read( ptr );                  mAlmGr.Read( ptr );                  bRead = false;                  bReturn = true;                }      }    }  }  // Вс¸ прочитано  tKeyAlmGr_Desc KeyAlmGr_Desc;  if( bRead )  {    mChars.AddObj("");// Пустышка для обломов с именем    if( BuildHoneywellList() )    {      if( Load( szXml ) == 0 )        bReturn = Parser();      else      {        _makepath_s( szXml, NULL, rsuGetAcyPath(), "assoc_disp", "csv" );        bReturn = ReadAndParseAssoc_disp( szXml );      }      ////AlmGrp      if ( 1 )      {        //char szAlmGrXml[_MAX_PATH];        //_makepath_s( szAlmGrXml, NULL, rsuGetAcyPath(), "Alarm Groups.cnf", "xml" );        tinyxml2::XMLDocument doc;        tinyxml2::XMLError err = doc.LoadFile( szAlmGrXml );        ASS( tinyxml2::XML_SUCCESS==err );        if ( tinyxml2::XML_SUCCESS==err )        {          bReturn = true;          tinyxml2::XMLElementPtr root = doc.RootElement();          LPCSTR element_name = root->Name();          ASSD( !strcmp( element_name, "MultiBlock"));          LPCSTR almgr = "";          LPCSTR desc = "";          LPCSTR container = "";          //tKeyAlmGr_Desc KeyAlmGr_Desc;          for( tinyxml2::XMLElementPtr Block = root->FirstChildElement(); Block != NULL; Block = Block->NextSiblingElement() )          {            LPCSTR element_name = Block->Name();            if ( !strcmp( element_name, "Block") )            {              for( tinyxml2::XMLElementPtr BlockDef = Block->FirstChildElement(); BlockDef != NULL; BlockDef = BlockDef->NextSiblingElement() )              {                LPCSTR element_name = BlockDef->Name();                if ( !strcmp( element_name, "BlockDef") )                {                  for( tinyxml2::XMLElementPtr Ref = BlockDef->FirstChildElement(); Ref != NULL; Ref = Ref->NextSiblingElement() )                  {                    LPCSTR element_name = Ref->Name();                    if ( !strcmp( element_name, "BlockName") )                      almgr = Ref->GetText();                    if ( !strcmp( element_name, "Container") )                      container = Ref->GetText();                    if ( !strcmp( element_name, "Reference") )                    {                      LPCSTR name = Ref->GetText();											UINT N = mHoney.Count();											UINT hName = INVALID_UNI;											for ( UINT i=0; i<N; ++i )											{												const char* dll = mHoney.Name(i);												if ( !strnicmp(dll, name, strlen(name)) )													if ( (dll[strlen(name)] != '.' && dll[strlen(name)] != '\0') )														continue;													else														hName = i;												else													continue;												THoneyWell* item = mHoney.Data( UINT(hName) );												// Проходим по списку аларменных групп												char* temp = mChars.Temp(almgr);												ParserAlmGr(item,temp);												if ( container ) 													if ( strcmp( container, "" ) )													{														temp = mChars.Temp(container);														ParserAlmGr(item,temp);													}											}                    }                    else                      continue;                  }                }                else if (!strcmp( element_name, "Parameters"))                {                  for( tinyxml2::XMLElementPtr Parameter = BlockDef->FirstChildElement(); Parameter != NULL; Parameter = Parameter->NextSiblingElement() )                  {                    tinyxml2::XMLElementPtr ParamName = Parameter->FirstChildElement();                    LPCSTR element_name = ParamName->GetText();                    if ( !strcmp( element_name, "DESC") )                    {                      ParamName = ParamName->NextSiblingElement();                      desc = ParamName->GetText();                      char buff[64] = {};                      n_RSUs::TrimLeft( desc, '"' );                      memcpy(buff, desc, sizeof(buff) );                      n_RSUs::TrimRightToLeft( buff+strlen(buff)-1, '"' );                      KeyAlmGr_Desc.insert( tKeyAlmGr_Desc::value_type(almgr,buff) );                    }                  }                }                else                {                  continue;                }              }            }            else              continue;            container = "";          }          //          UINT nHoney = mHoney.Count();          UINT nAlmGr = mAlmGr.Count();          if ( nHoney == 0 ) return false;          if ( nAlmGr == 0 ) return false;          // Привязка объектов Honeywell к аларменным группам          TAlmGr* list = mAlmGr.Items();          for ( UINT n = 0; n < nAlmGr; n++ )            ParserAlmGr(list[n],n,KeyAlmGr_Desc);          }        //      }      ////    }    //    if ( bReturn )    {      HFILE hFile = _lcreat( szBin, 0 );      if ( hFile != HFILE_ERROR )      {        _hwrite( hFile, &g_chVer, 1 );        _hwrite( hFile, (char*)&file[0].ftLastWriteTime, sizeof(FILETIME) );        _hwrite( hFile, (char*)&file[1].ftLastWriteTime, sizeof(FILETIME) );        _hwrite( hFile, (char*)&file[2].ftLastWriteTime, sizeof(FILETIME) );        _hwrite( hFile, (char*)&file[3].ftLastWriteTime, sizeof(FILETIME) );        _hwrite( hFile, (char*)&file[4].ftLastWriteTime, sizeof(FILETIME) );        mHoney.Write( hFile );        mMnemo.Write( hFile );        mIndex.Write( hFile );        mChars.Write( hFile );        mAlmGr.Write( hFile );        _lclose( hFile );      }    }  }  return bReturn;}// Построение списка интересующих объектов,// исходя уже из готового списка в Names.dat.bool SAlarmsW::BuildHoneywellList(){  DWORD ID = 0;  CBase* pBase;  LPCSTR pName;  int numb = 0;  size_t nCount = 0;  while( g_Objs.WhileBase( ID, id_Unknown, &pBase, &pName, &numb ) )  {    if ( pBase == NULL )      continue;    if ( !IsAlarm(pBase->ID_CLASS) )      continue;    ++nCount;  }  ID = 0;  while( g_qbIOs.WhileBase( ID, id_Unknown, &pBase, &pName, &numb ) )  {    if ( pBase == NULL )      continue;    if ( !IsAlarm(pBase->ID_CLASS) )      continue;    ++nCount;  }  mHoney.Realloc( nCount );  ID = 0;  while( g_Objs.WhileBase( ID, id_Unknown, &pBase, &pName, &numb ) )  {    if ( pBase == NULL )      continue;    if ( !IsAlarm(pBase->ID_CLASS) )      continue;    int nb = -1;    UINT hName = mHoney.Find( pName, &nb );    ASS( hName == INVALID_UNI );    if ( hName != INVALID_UNI )      continue;    hName = mHoney.Add(pName,nb);    ASS( INVALID_UNI!=hName );    THoneyWell &h = *mHoney.Data( hName );    h.ID_CLASS = pBase->ID_CLASS;  }  ID = 0;  while( g_qbIOs.WhileBase( ID, id_Unknown, &pBase, &pName, &numb ) )  {    if ( pBase == NULL )      continue;    if ( !IsAlarm(pBase->ID_CLASS) )      continue;    int nb = -1;    UINT hName = mHoney.Find( pName, &nb );    ASS( hName == INVALID_UNI );    if ( hName != INVALID_UNI )      continue;    hName = mHoney.Add(pName,nb);    ASS( INVALID_UNI!=hName );    THoneyWell &h = *mHoney.Data( hName );    h.ID_CLASS = pBase->ID_CLASS;  }  nCount = mHoney.Count();  return nCount != 0;}//bool SAlarmsW::ReadAndParseAssoc_disp( LPCSTR pszCsv ){  FILE *pFile = NULL;  errno_t res = fopen_s( &pFile, pszCsv, "rt" );  if( res )    return false;  KAutoCloser<FILE*> file( pFile, fclose );  char szTmp[256] = {};  while( fgets( szTmp, _countof(szTmp)-1, pFile ) )  {    for( LPSTR p = szTmp; *p; ++p )    {      if( *p<=13 )      {        *p = 0;        break;      }    }    if( !szTmp[0] )      continue;    char tagAndMnemos[2][256] = {};    int C = SplitString( szTmp, tagAndMnemos, ';', false );    if( 2!=C )      continue;    char spltTag[10][256] = {};    char szFullTag[256] = {};    C = SplitString( tagAndMnemos[0], spltTag, '.', false );    if( C<2 )      continue;    sprintf_s( szFullTag, "%s.%s", spltTag[0], spltTag[1] );    UINT hName = mHoney.Find( szFullTag );    if ( hName != INVALID_UNI )    {      THoneyWell* item = mHoney.Data( UINT(hName) );      // Проходим по списку мнемосхем      char* temp = mChars.Temp(tagAndMnemos[1]);      Parser(item,temp);    }  }  //  UINT nHoney = mHoney.Count();  UINT nMnemo = mMnemo.Count();  if ( nHoney == 0 )     return false;  if ( nMnemo == 0 )    return false;  // Самая длительная операция  // Привязка объектов Honeywell к мнемосхемам  TMnemoSxem* list = mMnemo.Items();  for ( UINT n = 0; n < nMnemo; n++ )    Parser(list[n],n);  return true;}// Построение списка объектов Honeywellbool SAlarmsW::Parser(){  HXML hHtml = FindItem( INVALID_HXML, "htmls" );  if ( hHtml == INVALID_HXML )    return false;  HXML hItem = FindItem( hHtml, "html" );  if ( hItem == INVALID_HXML )    return false;  //  while ( hItem != INVALID_HXML )  {    const char* name = Param( hItem, "tag" );    const char* mnemo = Param( hItem, "mnemo" );    if ( name && mnemo && *name && *mnemo )    {      UINT hName = mHoney.Find( name );      if ( hName != INVALID_UNI )      {        THoneyWell* item = mHoney.Data( UINT(hName) );        // Проходим по списку мнемосхем        char* temp = mChars.Temp(mnemo);        Parser(item,temp);      }    }    //    hItem = NextItem( hItem );  }  //  UINT nHoney = mHoney.Count();  UINT nMnemo = mMnemo.Count();  if ( nHoney == 0 ) return false;  if ( nMnemo == 0 ) return false;  // Самая длительная операция  // Привязка объектов Honeywell к мнемосхемам  TMnemoSxem* list = mMnemo.Items();  for ( UINT n = 0; n < nMnemo; n++ )    Parser(list[n],n);  //  return true;}//// Построение списка мнемосхем у объекта Honeywellvoid SAlarmsW::Parser(THoneyWell* item,char* ptr){  if( item->m_nCount )    return;  ::CharUpper(ptr);  item->m_nPlace = mIndex.Count();  while ( 1 )  {    char* p = strchr( ptr, ',' );    if ( p ) *p = 0;    //    char* s = ptr;    while ( *s ) { if ( *s == '\\' ) *s = '/'; s++; }    //    UINT hName = mMnemo.Add( ptr );    ASS( hName != INVALID_UNI );    if ( hName != INVALID_UNI )    {      mIndex.Add(UINT(hName));      item->m_nCount++;    }    //    if ( p )      ptr = p+1;    else      break;  }}//void SAlarmsW::Parser(TMnemoSxem& sxem,UINT N){  LPCSTR name = mMnemo.mChar.Name( sxem.m_nName );  sxem.m_nPlace = mIndex.Count();  UINT nHoney = mHoney.Count();  THoneyWell* item = mHoney.Items();  for ( UINT n = 0; n < nHoney; n++ )  {    UINT M = item[n].m_nPlace;    for ( UINT m = 0; m < item[n].m_nCount; m++, M++ )    {      if ( N == mIndex.W(M) )      {        mIndex.Add(n);        sxem.m_nCount++;      }    }  }}// Типы, у которых есть сигнализацияinline bool SAlarmsW::IsAlarm( UINT nType ){  switch( nType )  {  // Control Builder  case id_DATAACQ:  case id_PID:  case id_DEVCTL:  case id_FLAG:  case id_DIGACQ:    return true;  // Quick Builder  case id_QBANALOG:  case id_QBSTATUS:    return true;  }  return false;}//void SAlarmsW::HIALM( CAlarmBase* pObj, BYTE nEHLN, BYTE nType ){  static SValueDef* mTp[id_AcyMax] = {};  static SValueDef* mPr[id_AcyMax] = {};  static BYTE       bIn[id_AcyMax] = {};  //  DWORD n = pObj->ID_CLASS;  if( !IsAcy( n ) )    return;  //  if( bIn[n] == 0 )  {    mTp[n] = NameToValueH( n, "HIALM.TYPE" );    mPr[n] = NameToValueH( n, "HIALM.PR"   );    if ( mTp[n] == NULL || mPr[n] == NULL )      bIn[n] = 2;    else if ( mTp[n]->eVal != enumValueChr ||   mPr[n]->eVal != enumValueChr )      bIn[n] = 2;    else      bIn[n] = 1;// Вс¸ нормально    ASS( bIn[n] != 2 );  }  // что-то не срослось  if ( bIn[n] == 2 )    return;  //  BYTE* tp = (BYTE*)( (char*)(pObj) + mTp[n]->dwShift );  BYTE* pr = (BYTE*)( (char*)(pObj) + mPr[n]->dwShift );  pr[0] = nEHLN;  if ( pr[0] == 0 )// Вошло в норму    tp[0] = 0;  else  {    switch ( n )    {    case id_DATAACQ : DATAACQ( tp[0], nType ); break;    case id_PID     : PID    ( tp[0], nType ); break;    case id_PIDFF   : PID    ( tp[0], nType ); break;    case id_DEVCTL  : DEVCTL ( tp[0], nType ); break;    case id_FLAG    : FLAG   ( tp[0], nType ); break;    case id_DIGACQ  : DIGACQ   ( tp[0], nType ); break;    default:      LogMsg( "Неподдерживаемый тип сигнализации" );    };  }  //}////ENUM_S(HIALM,TYPE,"High Alarm Flag",//NONE,BadPv,PvHiHi,PvLoLo,PvHi,PvLo,PosRoc,NegRoc)void SAlarmsW::DATAACQ( BYTE& tp, BYTE nType ){  switch ( nType )  {  case A_BP : tp = 1; break;//Bad Process Variable  case A_2HP: tp = 2; break;//High High PV  case A_2LP: tp = 3; break;//Low Low PV  case A_HP : tp = 4; break;//High Process Variable  case A_LP : tp = 5; break;//Low Process Variable  case A_RCp: tp = 6; break;//Positive Rate of Change  case A_RCm: tp = 7; break;//Negative Rate of Change  default:    LogMsg( "Неподдерживаемая сигнализация DATAACQ" );  };}////ENUM_S(HIALM,TYPE,"High Alarm Flag",//NONE,BadCtl,SafetyIlock,DevHi,DevLo,AdvDev,OpHi,OpLo,PredHi,PredLo)void SAlarmsW::PID( BYTE& tp, BYTE nType ){  switch ( nType )  {  case A_BC : tp = 1; break;//Bad Control Flag Alarm  case A_S10: tp = 2; break;//Safety Interlock Tripped, State 0  case A_HD : tp = 3; break;//High Deviation  case A_LD : tp = 4; break;//Low deviation  case A_ADV: tp = 5; break;//Advisory Deviation  case A_OH : tp = 6; break;//Output High alarm  case A_OL : tp = 7; break;//Output Low alarm  //case CHANGE : tp = 8; break;//CHANGE    //case A_X: tp = 8; break;// PredHi ?????    //case A_X: tp = 9; break;// PredLo ?????  default:    LogMsg( "Неподдерживаемая сигнализация PID" );  };}////ENUM_S(HIALM,TYPE,"High Alarm Type",//NONE,BadPv,OffNormal,CmdDisagree,CommandFail,UnCommanded,SafetyIlock,Oride0Ilock,Oride1Ilock,Oride2Ilock)void SAlarmsW::DEVCTL( BYTE& tp, BYTE nType ){  switch ( nType )  {  case A_BP : tp = 1; break;//Bad Process Variable  case A_OFN: tp = 2; break;//Off Normal  case A_FB : tp = 3; break;//Digital Feedback (Command Disagree)  case A_CMF: tp = 4; break;//Command Fail  case A_UNC: tp = 5; break;//Uncommanded Change  case A_S10: tp = 6; break;//Safety Interlock Tripped, State 0  case A_OV0: tp = 7; break;//Interlock Tripped, State 0  case A_OV1: tp = 8; break;//Interlock Tripped, State 1  case A_OV2: tp = 9; break;//Interlock Tripped, State 2  default:    LogMsg( "Неподдерживаемая сигнализация DEVCTL" );  };}////ENUM_S(HIALM,TYPE,"High Alarm Flag",//NONE,OFFNRMLALM)void SAlarmsW::FLAG( BYTE& tp, BYTE nType ){  switch ( nType )  {  case A_OFN: tp = 1; break;//Off Normal  default:    LogMsg( "Неподдерживаемая сигнализация FLAG" );  };}//void SAlarmsW::DIGACQ( BYTE& tp, BYTE nType ){  switch ( nType )  {  case A_OFN: tp = 1; break;//Off Normal  default:    LogMsg( "Неподдерживаемая сигнализация FLAG" );  };}// Построение списка аларменных групп у объекта Honeywellvoid SAlarmsW::ParserAlmGr(THoneyWell* item,char* ptr){  /*UINT hExistAlmGr = mAlmGr.Find( ptr );  if( item->m_nCountAlmGr && hExistAlmGr != INVALID_UNI ) //Проверка: Внесена ли уже аларменная группа в список    return;  //::CharUpper(ptr);  item->m_nPlace = mIndex.Count();  while ( 1 )  {    char* p = strchr( ptr, ',' );    if ( p ) *p = 0;    //    char* s = ptr;    while ( *s ) { if ( *s == '\\' ) *s = '/'; s++; }    //    UINT hName = mAlmGr.Add( ptr );    ASS( hName != INVALID_UNI );    if ( hName != INVALID_UNI )    {      mIndex.Add(UINT(hName));      item->m_nCountAlmGr++;    }    //    if ( p )      ptr = p+1;    else      break;  }*/  ////////////////////////////////  UINT hExistAlmGr = mAlmGr.Find( ptr );  if( hExistAlmGr == INVALID_UNI ) //Проверка: Внесена ли уже аларменная группа в список    hExistAlmGr = mAlmGr.Add( ptr );  if( item->m_nCountAlmGr )  {    if ( hExistAlmGr != INVALID_UNI )    {      item->indexplace[item->m_nCountAlmGr] = mIndex.Add(UINT(hExistAlmGr));      item->m_nCountAlmGr++;      if ( item->m_nCountAlmGr > 63 )        ASSD(0);    }  }  else  {    item->m_nPlace = mIndex.Count();    item->indexplace[item->m_nCountAlmGr] = mIndex.Add(UINT(hExistAlmGr));    item->m_nCountAlmGr++;  }}//void SAlarmsW::ParserAlmGr(TAlmGr& agr,UINT N, tKeyAlmGr_Desc& AlmGrDesc){  LPCSTR name = mAlmGr.mChar.Name( agr.m_nName );  tKeyAlmGr_Desc::iterator it = AlmGrDesc.find( name );  if( AlmGrDesc.end()!=it )    strcpy(agr.description,it->second.c_str());  agr.m_nPlace = mIndex.Count();  UINT nHoney = mHoney.Count();  THoneyWell* item = mHoney.Items();  for ( UINT n = 0; n < nHoney; n++ )  {    //UINT M = item[n].m_nPlace;    for ( UINT m = 0; m < item[n].m_nCountAlmGr; m++ )    {      if ( N == mIndex.W(item[n].indexplace[m]) )      {        mIndex.Add(n);        agr.m_nCount++;      }    }  }}//