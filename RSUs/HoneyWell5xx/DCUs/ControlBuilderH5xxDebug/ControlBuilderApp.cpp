#include "stdafx.h"#include "ControlBuilderH5xxDebug.h"#include "ControlBuilderApp.h"#include "ControlBuilderSimpleFrame.h"#include "ControlBuilderDoc.h"#include "ControlBuilderView.h"#include <MemoryServiceH5xx.h>#include <rsuErr.h>#include "Dial_Value.h"#include <rsuPaths.h>#include <macros/AutoCloser.h>// KControlBuilderAppIMPLEMENT_DYNCREATE(KControlBuilderApp, CWinThread)KControlBuilderApp::KControlBuilderApp(): m_pDocManager( NULL ){}KControlBuilderApp::~KControlBuilderApp(){}BOOL KControlBuilderApp::InitInstance(){	// TODO:  perform and per-thread initialization here	return TRUE;}int KControlBuilderApp::ExitInstance(){	// TODO:  perform any per-thread cleanup here	return CWinThread::ExitInstance();}BEGIN_MESSAGE_MAP(KControlBuilderApp, CWinThread)END_MESSAGE_MAP()// KControlBuilderApp message handlersvoid KControlBuilderApp::Open( IControlBuilder *pcb, SH_Module * entryModule, LPCSTR pszSelectName ){  AFX_MANAGE_STATE(AfxGetStaticModuleState());  AFX_MODULE_THREAD_STATE* pState = AfxGetModuleThreadState();  CWinThread* pThread = pState->m_pCurrentWinThread;  if( !pState->m_pCurrentWinThread )    pState->m_pCurrentWinThread = this;  if( !m_pDocManager )  {    void *pmem = KMemoryServiceH5xx::Instance().NewMem( sizeof(*m_pDocManager) );    if( !pmem )      return;    m_pDocManager = new (pmem ) CDocManager;    if( !m_pDocManager )      return;    pmem = KMemoryServiceH5xx::Instance().NewMem( sizeof(CMultiDocTemplate) );    if( !pmem )      return;    /*HINSTANCE ht = GetModuleHandle("ControlBuilderH5xxDebug");    if( !ht )      return;    if( !m_hInstance )      m_hInstance = ht;*/    //ASSERT( m_hInstance==ht );    CMultiDocTemplate* pDocTemplate = new (pmem) CMultiDocTemplate( IDR_CONTROLBUILDER,                                                                    RUNTIME_CLASS(KControlBuilderDoc),                                                                    RUNTIME_CLASS(KControlBuilderSimpleFrame),                                                                    RUNTIME_CLASS(KControlBuilderView));    m_pDocManager->AddDocTemplate(pDocTemplate);  }  POSITION pos = m_pDocManager->GetFirstDocTemplatePosition();  CDocument* pOpenDocument = NULL;  /*while( pos != NULL )  {    CDocTemplate* pTemplate = (CDocTemplate*)m_pDocManager->GetNextDocTemplate(pos);    ASSERT_KINDOF(CDocTemplate, pTemplate);    POSITION pos1 = pTemplate->GetFirstDocPosition();    if( pos1 )    {      pOpenDocument = pTemplate->GetNextDoc( pos1 );      if( pOpenDocument && pOpenDocument->IsKindOf( RUNTIME_CLASS(KControlBuilderDoc)) )        break;      else        pOpenDocument = NULL;    }  }*/  if( !pOpenDocument )  {    char Path[_MAX_PATH] = { 0 };    m_pDocManager->OnFileNew();    pos = m_pDocManager->GetFirstDocTemplatePosition();    while (pos != NULL )    {      CDocTemplate* pTemplate = (CDocTemplate*)m_pDocManager->GetNextDocTemplate(pos);      ASSERT_KINDOF(CDocTemplate, pTemplate);      POSITION pos1 = pTemplate->GetFirstDocPosition();      while( pos1 )      {        pOpenDocument = pTemplate->GetNextDoc( pos1 );        if( pOpenDocument && pOpenDocument->IsKindOf( RUNTIME_CLASS(KControlBuilderDoc)) )        {          KControlBuilderDoc* pDoc = (KControlBuilderDoc*)pOpenDocument;          if( pDoc->m_entryModule )            continue;          pDoc->m_entryModule = entryModule;          pDoc->m_pcb = pcb;          if( pszSelectName )            pDoc->m_szFirstSelect = pszSelectName;          pDoc->SetTitle( entryModule->BlockName );          pos = pOpenDocument->GetFirstViewPosition();          CView *pView = pOpenDocument->GetNextView( pos );          if( pView )          {            CFrameWnd *pFr = pView->GetParentFrame();            if( pFr )            {              pFr->SendMessageToDescendants( WM_USER, 0, 0, TRUE, TRUE);            }          }          pos = NULL;          break;        }        else          pOpenDocument = NULL;      }    }  }  /*if( pOpenDocument )  {    pos = pOpenDocument->GetFirstViewPosition();    CView *pView = pOpenDocument->GetNextView( pos );    if( pView )    {      CFrameWnd *pFr = pView->GetParentFrame();      if( pFr )      {        if( !pFr->IsWindowVisible() )          pFr->ShowWindow( SW_SHOW );        pFr->SetActiveWindow();      }    }  }*/}CFrameWnd* KControlBuilderApp::IsAnyOpen( SH_Module *pEntry ){  if( !m_pDocManager )    return FALSE;  POSITION pos = m_pDocManager->GetFirstDocTemplatePosition();  CDocument* pOpenDocument = NULL;  while( pos != NULL )  {    CDocTemplate* pTemplate = (CDocTemplate*)m_pDocManager->GetNextDocTemplate(pos);    ASSERT_KINDOF(CDocTemplate, pTemplate);    POSITION pos1 = pTemplate->GetFirstDocPosition();    if( pos1 )    {      pOpenDocument = pTemplate->GetNextDoc( pos1 );      if( pOpenDocument && pOpenDocument->IsKindOf( RUNTIME_CLASS(KControlBuilderDoc)) )      {        KControlBuilderDoc* pDoc = (KControlBuilderDoc*)pOpenDocument;        if( pEntry==pDoc->m_entryModule )          break;      }      pOpenDocument = NULL;    }  }  if( pOpenDocument )  {    pos = pOpenDocument->GetFirstViewPosition();    CView *pView = pOpenDocument->GetNextView( pos );    if( pView )    {      CFrameWnd *pFr = pView->GetParentFrame();      if( pFr )      {        return pFr;        /*if( !pFr->IsWindowVisible() )          pFr->ShowWindow( SW_SHOW );        pFr->SetActiveWindow();*/      }    }  }  return FALSE;}class KThreadSupport{  struct S  {    HANDLE hThread;    KControlBuilderApp *pApp;  }_array[20];public:  KThreadSupport()  {    ZeroMemory( _array, sizeof(_array) );  }  KControlBuilderApp *Get()  {    HANDLE cur = GetCurrentThread();    int i = 0;    for( i; i<_countof(_array); ++i )    {      if( !_array[i].hThread )        break;      if( cur==_array[i].hThread )        return _array[i].pApp;    }    ASS( i<_countof(_array) );    _array[i].hThread = cur;    _array[i].pApp = new KControlBuilderApp;    return _array[i].pApp;  }}static s_controller;bool ShowDebugWnd( SH_Module *pRoot, IControlBuilder *pCB, char * pszName, LPCSTR pszObjName, void * pData, bool bEnternalCall ){  KControlBuilderApp *pApp = s_controller.Get();  if( !pApp )    return false;  HINSTANCE ht = GetModuleHandle("ControlBuilderH5xxDebug");  AFX_MANAGE_STATE(AfxGetStaticModuleState());  AFX_MODULE_THREAD_STATE* pState = AfxGetModuleThreadState();  CWinThread* pThread = pState->m_pCurrentWinThread;  if( !pState->m_pCurrentWinThread )    pState->m_pCurrentWinThread = pApp;  KBmBase *blk = NULL;  CFrameWnd* pFr = NULL;  int nOpenWnd = 0;  if( !pRoot )    return false;  if( !strcmp(pszName, pszObjName) )    nOpenWnd |= 1;  else  {    blk = pRoot->QuickFindObj( pszName, 1 );    if( blk && !pFr && !bEnternalCall )      nOpenWnd |= 2;  }  if(pData == NULL)  {    if( nOpenWnd )      pApp->Open( pCB, pRoot, pszName );    if( 1==nOpenWnd )      return true;  }    if( !blk )    return false;  if( !pFr )    pFr = pApp->IsAnyOpen( pRoot );  if( pFr )  {    if( !pFr->IsWindowVisible() )      pFr->ShowWindow( SW_SHOW );    pFr->SetActiveWindow();  }  KHParamInfo params[10000], paramsPref[50];  ZeroMemory( params, sizeof(params) );  ZeroMemory( paramsPref, sizeof(paramsPref) );  int nCur = 0, prefC=0;  blk->GetFields( params, nCur, _countof(params) );  ASSD( nCur<_countof(params) );  if( !nCur )    return false;  char szFilterParams[50][32] = { 0 };  int nFilterParams = 0;  char szTmp[256];  char szCompNamePlType[_MAX_PATH] = { 0 };  strcpy_s( szCompNamePlType, "INI\\Params\\" );  DWORD l = (DWORD)strlen(szCompNamePlType);  DWORD size = _countof(szCompNamePlType) - l;  if( GetComputerName( &szCompNamePlType[l], &size) )  {    strcat_s( szCompNamePlType, "_" );    strcat_s( szCompNamePlType, blk->TypeName );    _makepath_s( szTmp, NULL, rsuGetProjectPath(), szCompNamePlType, "csv" );    FILE *pFile = NULL;    errno_t res = fopen_s( &pFile, szTmp, "rt" );    if( !res )    {      KAutoCloser<FILE*> file( pFile, fclose );      while( fgets( szTmp, _countof(szTmp)-1, pFile ) )      {        for( LPSTR p = szTmp; *p; ++p )        {          if( *p<=13 )          {            *p = 0;            break;          }        }        if( !szTmp[0] )          continue;        strcpy_s( szFilterParams[nFilterParams], szTmp );        ++nFilterParams;        if( nFilterParams==_countof(szFilterParams) )          break;      }    }  }  if( nFilterParams )  {    for( int f=0; f<nFilterParams; ++f )    {      for( int a=0; a<nCur; ++a )      {        KHParamInfo &testParam = params[a];        if( !testParam.VarName )          continue;        if( _strcmpi(testParam.VarName, szFilterParams[f]) )          continue;        CopyMemory( &paramsPref[prefC], &testParam, sizeof(paramsPref[prefC]) );        ZeroMemory( &params[a], sizeof(testParam) );        ++prefC;        break;      }      if( prefC==_countof(paramsPref) )        break;    }    /*if( prefC )    {      for( int a=0; a<nCur; ++a )      {        KHParamInfo &testParam = params[a];        if( testParam.VarName )          continue;        MoveMemory( &params[a], &params[a+1], sizeof(params[a])*(nCur-a) );      }      MoveMemory( &params[prefC], &params[0], sizeof(params[0])*(nCur-prefC) );      CopyMemory( &params[0], &paramsPref[0], sizeof(params[0])*prefC );    }*/  }  CDial_Value * pDlg = NULL;	  if(pData == NULL)  {    pDlg = new CDial_Value;    if( pDlg == NULL )      return false;    if( !pDlg->Create( CDial_Value::IDD, pFr ) )    {      delete pDlg;      return false;    }  }  if(pDlg)  {    sprintf_s( szTmp, "%s[%s]", pszName, blk->TypeName );    pDlg->SetWindowText( szTmp );    pDlg->ShowWindow( SW_SHOW );    if( prefC )      pDlg->SetVarList( prefC, paramsPref );    else      pDlg->SetVarList( nCur, params );  }  return true;}