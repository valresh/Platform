#include "stdafx.h"#include "ControlBuilderView.h"#include "../../../HoneyWell4xx/DCUs/CnfReader/CnfReader.h"#include <InlineGDI.h>#include <algorithm>#include "ControlBuilderH5xxDebug.h"using namespace std;IMPLEMENT_DYNCREATE(KControlBuilderView, CScrollView)KControlBuilderView::KControlBuilderView(): m_hBitmap(NULL), m_fScale(1.), m_curSize( 100, 100 ), m_bFirstDraw( true ), m_ptMouse(-1,-1){}KControlBuilderView::~KControlBuilderView(){  if( m_hBitmap )     ::DeleteObject( m_hBitmap );}KControlBuilderDoc* KControlBuilderView::GetDocument(){  ASSERT(m_pDocument->IsKindOf(RUNTIME_CLASS(KControlBuilderDoc)));  return (KControlBuilderDoc*)m_pDocument;}BEGIN_MESSAGE_MAP(KControlBuilderView, CScrollView)  ON_MESSAGE( WM_USER, OnWmUser)  ON_WM_ERASEBKGND()  ON_WM_TIMER()  ON_WM_MOUSEWHEEL()  ON_WM_KEYDOWN()  ON_WM_LBUTTONDOWN()  ON_WM_LBUTTONUP()  ON_WM_MOUSEMOVE()  ON_WM_RBUTTONUP()  ON_WM_MBUTTONDOWN()END_MESSAGE_MAP()// KControlBuilderView drawingvoid KControlBuilderView::OnInitialUpdate(){	CScrollView::OnInitialUpdate();	SetScrollSizes( m_curSize );  LOGFONT lf;  memset( &lf, 0, sizeof(lf) );  lf.lfCharSet = DEFAULT_CHARSET;  lf.lfHeight  = -12;  lf.lfWeight  = FW_BOLD;  strcpy_s( lf.lfFaceName, "Courier New" );  m_fontName.CreateFontIndirect(&lf);  lf.lfHeight  = -10;  lf.lfWeight  = FW_NORMAL;  m_fontType.CreateFontIndirect(&lf);  strcpy_s( lf.lfFaceName, "Arial" );  m_fontParams.CreateFontIndirect(&lf);  lf.lfHeight  = -8;  m_fontRefM.CreateFontIndirect(&lf);  SetTimer( 500, 500, NULL );}void KControlBuilderView::SetScrollSizes( SIZE size){  CSize sz = CSize(int(m_fScale*size.cx),int(m_fScale*size.cy));  __super::SetScrollSizes(MM_TEXT, sz);}// KControlBuilderView diagnostics#ifdef _DEBUGvoid KControlBuilderView::AssertValid() const{	CScrollView::AssertValid();}void KControlBuilderView::Dump(CDumpContext& dc) const{	CScrollView::Dump(dc);}#endif //_DEBUG// KControlBuilderView message handlersBOOL KControlBuilderView::OnEraseBkgnd(CDC* pDC){  return FALSE;//CScrollView::OnEraseBkgnd(pDC);}void KControlBuilderView::OnTimer(UINT_PTR nIDEvent){  Invalidate( FALSE );  CScrollView::OnTimer(nIDEvent);}void KControlBuilderView::ChangeScale( BOOL bInc ){  CPoint pos = GetDeviceScrollPosition();  CRect rect;  GetWindowRect( &rect );  ScreenToClient( &rect );  CPoint centre = rect.CenterPoint();  CPoint pt;  pt.x = (int)(double(centre.x+pos.x)/m_fScale);  pt.y = (int)(double(centre.y+pos.y)/m_fScale);  SIZE szDoc = m_curSize;//{ rect.Width(), rect.Height() };  if( bInc )  {    if ( m_fScale < 10 )     {      m_fScale += 1.0;       SetScrollSizes( szDoc );       Invalidate(false);    }  }  else  {    if ( m_fScale > 1. )    {      m_fScale -= 1.0;      SetScrollSizes( szDoc );      Invalidate(false);    }  }  pt.x = (LONG)(pt.x * m_fScale);  pt.y = (LONG)(pt.y * m_fScale);  centre.x *= -1;  centre.y *= -1;  pt.Offset( centre );  ScrollToPosition( pt );}BOOL KControlBuilderView::OnMouseWheel(UINT nFlags, short zDelta, CPoint pt){  if( zDelta>0 )    ChangeScale( TRUE );  else    ChangeScale( FALSE );  return __super::OnMouseWheel(nFlags, zDelta, pt);}void KControlBuilderView::OnKeyDown(UINT nChar, UINT nRepCnt, UINT nFlags){  switch ( nChar )  {  case VK_ADD:  case VK_OEM_PLUS:     ChangeScale(TRUE);    break;  case VK_SUBTRACT:  case VK_OEM_MINUS:    ChangeScale( FALSE );    break;  }  __super::OnKeyDown(nChar, nRepCnt, nFlags);}LRESULT KControlBuilderView::OnWmUser( WPARAM wParam, LPARAM lParam ){  m_curSize.cx = 100;  m_curSize.cy = 100;  SetScrollSizes( m_curSize );  m_module = NULL;  m_bFirstDraw = true;#ifdef _DEBUG  m_fScale = 1.;#else  m_fScale = 1.;#endif  if( !wParam || !lParam )    return 0L;  LPCSTR pPath = GetDocument()->GetFile();  if( !pPath || !*pPath )    return 0L;  m_blks.clear();  m_probConnects.clear();  KCnfReader reader;  int r = reader.ReadCnf( pPath, IBlock::emrSymbolAttrs | IBlock::emrConVertexs | IBlock::emrParameters );  m_module = reader.SortFor( string(GetDocument()->GetBlkName() ) );  bool bc = m_module->IsCoordSame();  CRect res( m_module->GetCoord() );  m_blks.resize( m_module->m_EmbBlocks.size() );  size_t i = 0;  for( tBlocksCont_::iterator it=m_module->m_EmbBlocks.begin(), end(m_module->m_EmbBlocks.end()); it!=end; ++it, ++i )  {    tBlockPtr_ blk = *it;    CRect n = blk->GetCoord();    res.left = __min( res.left, n.left );    res.right = __max( res.right, n.right );    res.top = __min( res.top, n.top );    res.bottom = __max( res.bottom, n.bottom );    KBlkDraw &bd = m_blks.at(i);    bd.m_blk = blk;    LPCSTR p = strchr( blk->BlockName.c_str(), '.');    if( p )      ++p;    else      p = blk->BlockName.c_str();    bd.m_ShowName = p;    CRect boundRect = blk->GetCoord();    CRect bodyRect = blk->GetBlkBodyCoord();    bool checkInputForInverse = false;    if( !strcmp(blk->tnNamespace.c_str(),"LOGIC") )      checkInputForInverse = true;    ns_AttrType::eAttrType ios[] = { ns_AttrType::InputTop, ns_AttrType::InputLeft, ns_AttrType::OutputBottom, ns_AttrType::OutputRight };    for ( int a=0; a<_countof(ios); ++a )    {      LONG index = 0;      for( tSymbolAttrPtr_ attr = blk->GetSymbAttr( ios[a], index ); attr; attr = blk->GetSymbAttr( ios[a], ++index ) )      {        LONG d = 0;        LONG order = attr->AttrOrder;        KBlkDraw::SPin pin;        LPCSTR pName = strchr( attr->ParamName.c_str(), '.' );        SCon con;        con.myName = attr->ParamName;        if( pName )        {          ++pName;          LPCSTR p2 = strchr( pName, '.');          if( p2 )            pName = p2 + 1;        }        else          pName = attr->ParamName.c_str();        strcpy_s( pin.name, pName );        if( checkInputForInverse )        {          if( !_strnicmp(pName,"IN[",3) )          {            char INPTINVSTS[16];            sprintf_s( INPTINVSTS, "INPTINVSTS%s", pName+2 );            for( tParamsCont_::iterator v=blk->Parameters.begin(), e(blk->Parameters.end()); v!=e; ++v )            {              tParamsCont_::value_type &p = *v;              if( p.first!=INPTINVSTS )                continue;              if( p.second=="ON" )                pin.bInverse = true;              break;            }          }        }		LONG h = 0;        switch( ios[a] )        {        case ns_AttrType::InputTop:          d = order * 24 + 24;          pin.coord.SetRect( bodyRect.left + d, bodyRect.top, bodyRect.left + d, boundRect.top );          bd.Pins[KBlkDraw::top].push_back( pin );          con.bInput = true;          con.coord.SetRect( bodyRect.left + d, boundRect.top, bodyRect.left + d, boundRect.top );          break;        case ns_AttrType::OutputBottom:          d = order * 24 + 24;          pin.coord.SetRect( bodyRect.left + d, bodyRect.bottom, bodyRect.left + d, boundRect.bottom );          bd.Pins[KBlkDraw::bottom].push_back( pin );          con.bInput = false;          h = order * 12;          con.coord.SetRect( bodyRect.left + d, boundRect.bottom-h, bodyRect.left, boundRect.bottom-h );          break;        case ns_AttrType::InputLeft:          d = (order * 24 + 24);          pin.coord.SetRect( boundRect.left, bodyRect.top + d, bodyRect.left, bodyRect.top + d );          bd.Pins[KBlkDraw::left].push_back( pin );          con.bInput = true;          con.coord.SetRect( boundRect.left, bodyRect.top + d, boundRect.left, bodyRect.top + d );          break;        case ns_AttrType::OutputRight:          d = (order * 24 + 24);          pin.coord.SetRect( bodyRect.right, bodyRect.top + d, boundRect.right, bodyRect.top + d );          bd.Pins[KBlkDraw::right].push_back( pin );          con.bInput = false;          con.coord.SetRect( boundRect.right, bodyRect.top + d, boundRect.right, bodyRect.top + d );          break;        }        m_probConnects.push_back( con );      }    }    bd.Prepare0( GetDocument()->m_pcb, GetDocument()->GetBlkName() );  }  m_curSize.cx = (LONG)(res.right * 1.5);  m_curSize.cy = (LONG)(abs( res.bottom ) * 1.5);  SetScrollSizes( m_curSize );  return 0L;}// Получение дескриптора битмапаbool _GiveBitmap(HDC hDC,int nWidth,int nHeight,HBITMAP& hBitmap){  bool bReturn = false;  if ( hBitmap != NULL )  {    BITMAP info;    ::GetObject( hBitmap, sizeof(BITMAP), &info);    if ( info.bmWidth < nWidth || info.bmHeight < nHeight )    {      ::DeleteObject(hBitmap);      hBitmap = NULL;    }  }  //  if ( hBitmap == NULL )  {    bReturn = true;    hBitmap = ::CreateCompatibleBitmap( hDC, nWidth, nHeight );  }  return bReturn;}void KControlBuilderView::OnDraw(CDC* pDC){  CDocument* pDoc = GetDocument();  // TODO: add draw code here  CRect rect;  GetClientRect( rect );  CDC cdc;  if ( cdc.CreateCompatibleDC( pDC ) )  {    _GiveBitmap( *pDC, rect.Width(), rect.Height(), m_hBitmap );    if ( m_hBitmap != NULL )    {      HBITMAP hOldBmp = (HBITMAP)::SelectObject( cdc, m_hBitmap );      {        CMyBrush brush( cdc, RGB(255,255,255));        brush.Rect( rect );      }      Draw( cdc );      CPoint pt = GetDeviceScrollPosition();      pDC->BitBlt( pt.x, pt.y, rect.Width(), rect.Height(), &cdc, 0, 0, SRCCOPY );      //      ::SelectObject( cdc, hOldBmp );    }    cdc.DeleteDC();  }}void KControlBuilderView::Draw( CDC& dc ){  if( !m_module )    return;  XFORM xT;  int nGraph = dc.SetGraphicsMode( GM_ADVANCED );  dc.GetWorldTransform( &xT );  //dc.SetViewportExt( 0, m_curSize.cy );  int prevMM = dc.SetMapMode( MM_TEXT );  CPoint pt = GetDeviceScrollPosition();  FLOAT dx = -FLOAT(pt.x);  FLOAT dy = -FLOAT(pt.y);  XFORM xf = {FLOAT(m_fScale),0,0,FLOAT(m_fScale),dx,dy};  dc.SetWorldTransform( &xf );  int nMode = dc.SetBkMode( TRANSPARENT );  COLORREF color = dc.SetTextColor( 0 );  for( tBlkDrawCont_::iterator it=m_blks.begin(), end(m_blks.end()); it!=end; ++it )  {    DrawBlock( dc, *it );  }  if( m_bFirstDraw )  {    for( tAllConnects_::iterator it=m_probConnects.begin(), end(m_probConnects.end()); it!=end; ++it )    {      SCon &test = *it;      char name[40], field[20];      LPCSTR s = test.myName.c_str();      LPCSTR p = strchr( s, '.' );      LPCSTR p2 = strchr( p+1, '.');      if( p2 )        p = p2+1;      strncpy_s( name, s, p-s-1 );      strcpy_s( field, p);      GetDocument()->m_pcb->SelectAllConnections( name, field, test.bInput, test.otherNames );      KKK();    }    tAllConnects_::iterator it=m_probConnects.begin();    while( it!=m_probConnects.end() )    {      if( (*it).otherNames.empty() )      {        it = m_probConnects.erase( it );        continue;      }      ++it;    }  }  m_bFirstDraw = false;  for( tAllConnects_::iterator it=m_probConnects.begin(), end(m_probConnects.end()); it!=end; ++it )  {    COLORREF clr = RGB(200,200,200);    CMyFont fparm( dc, m_fontRefM, clr );    //CMyPen pen( dc, clr, 1);    SCon &con = *it;    LONG y = con.coord.top;    for( vector<string>::iterator v=con.otherNames.begin(), e(con.otherNames.end()); v!=e; ++v, y+=8 )    {      string &ref = *v;      if( con.bInput )        dc.SetTextAlign( TA_RIGHT | TA_BOTTOM );      fparm.Text( con.coord.left, y, ref.c_str() );    }  }  dc.SetTextColor( color );  dc.SetBkMode( nMode );  //dc.SetViewportOrg( 0, 0 );  dc.SetMapMode( prevMM );  dc.SetWorldTransform( &xT );  dc.SetGraphicsMode( nGraph );}void KControlBuilderView::DrawBlock( CDC& dc, KBlkDraw &blk ){  CRect boundRect = blk.m_blk->GetCoord();  CRect bodyRect = blk.m_blk->GetBlkBodyCoord();  if( 0 )  {    CMyPen pen( dc, RGB(0,0,255), 1 );    dc.Rectangle( &boundRect );  }  {    bodyRect.InflateRect( 1, 1, 0, 0 );    dc.Rectangle( &bodyRect );  }  bodyRect = blk.m_blk->GetBlkBodyCoord();  dc.FillSolidRect( &bodyRect, RGB(222,222,222) );  dc.Draw3dRect( &bodyRect, RGB(255,255,255), RGB(128,128,128) );  {    CMyFont fname( dc, m_fontName, 0 );    if( m_bFirstDraw )    {      RECT calc;      fname.Calc( calc, blk.m_ShowName.c_str() );      blk.m_pntClass.x = blk.m_pntName.x = bodyRect.right - 10;      blk.m_pntClass.y = blk.m_pntName.y = bodyRect.top + calc.bottom;      blk.m_pntClass.y += calc.bottom;    }    dc.SetTextAlign( TA_BOTTOM | TA_RIGHT );    fname.Text( blk.m_pntName.x, blk.m_pntName.y, blk.m_ShowName.c_str() );    CMyFont ftype( dc, m_fontType, 0 );    dc.SetTextAlign( TA_BOTTOM | TA_RIGHT );    ftype.Text( blk.m_pntClass.x, blk.m_pntClass.y, blk.m_blk->ClassName.c_str() );  }  blk.DrawPins( dc, m_fontParams );  CMyPen penС( dc, 0, 1 );  for( tConnectionsCont_::iterator it=blk.m_blk->Connections.begin(), end(blk.m_blk->Connections.end()); it!=end; ++it )  {    tConnectionsCont_::value_type &con = *it;    if( con.path.empty() )      continue;    SConnection::tVerCont_::iterator iv = con.path.begin(), endv(con.path.end());    dc.MoveTo( *iv );    ++iv;    for( iv; iv!=endv; ++iv )    {      dc.LineTo( *iv );    }    if( m_bFirstDraw )    {        SCon test[2];        if( con.GraphicalForm==ns_GraphicalForm::IG )        {          test[0].bInput = true;          test[0].myName = con.InputEnd;        }        else if( con.GraphicalForm==ns_GraphicalForm::BG )        {          test[0].bInput = true;          test[0].myName = con.InputEnd;          test[1].bInput = false;          test[1].myName = con.OutputEnd;        }        else if( con.GraphicalForm==ns_GraphicalForm::OG )        {          test[0].bInput = true;          test[0].myName = con.InputEnd;          test[1].bInput = false;          test[1].myName = con.OutputEnd;        }        else          ASSD(0);        for( int i=0; i<_countof(test); ++i )        {          if( test[i].myName.empty() )            continue;          tAllConnects_::iterator itd = std::remove_if( m_probConnects.begin(), m_probConnects.end(), test[i] );          if( m_probConnects.end()!=itd )            m_probConnects.erase( itd, m_probConnects.end() );        }      KBlkDraw::SRefToOtherBlock ref;      switch( con.ConnectionForm )      {      case ns_ConnectionForm::NamedConnVisible:        if( con.GraphicalForm==ns_GraphicalForm::IG )        {          CMyFont fparm( dc, m_fontParams, 0 );          SConnection::tVerCont_::reverse_iterator rit = con.path.rbegin();          CPoint pnt = *rit;          if( con.path.size() > 1 )            ++rit;          CPoint pntN = *rit;          fparm.Calc( ref.rect, con.OutputEnd.c_str() );          ref.rect.MoveToXY( pnt.x, pnt.y );          if( pntN.x > pnt.x )            ref.rect.OffsetRect( -ref.rect.Width(), 0 );          ref.rect.InflateRect( 2, 2 );          ref.text = con.OutputEnd;          ref.bInput = true;          blk.references.push_back( ref );        }        else if( con.GraphicalForm==ns_GraphicalForm::OG )        {          CMyFont fparm( dc, m_fontParams, 0 );          SConnection::tVerCont_::reverse_iterator rit = con.path.rbegin();          CPoint pnt = *rit;          if( con.path.size() > 1 )            ++rit;          CPoint pntN = *rit;          fparm.Calc( ref.rect, con.OutputEnd.c_str() );          ref.rect.MoveToXY( pnt.x, pnt.y );          if( pntN.x > pnt.x )            ref.rect.OffsetRect( -ref.rect.Width(), 0 );          ref.rect.InflateRect( 2, 2 );          ref.text = con.OutputEnd;          ref.bInput = true;          blk.references.push_back( ref );        }        else          ASSD(0);        break;      }    }  }  blk.DrawReferences( dc, m_fontParams );}void KControlBuilderView::OnLButtonDown(UINT nFlags, CPoint point){  CPoint ptOffset = GetDeviceScrollPosition();  POINT pt;  pt.x = int( (point.x+ptOffset.x)*m_fScale);  pt.y = int( (point.y+ptOffset.y)*m_fScale);  m_ptMouse = point;  CScrollView::OnLButtonDown(nFlags, point);}void KControlBuilderView::OnLButtonUp(UINT nFlags, CPoint point){  POINT pt;  ::GetCursorPos( &pt );  ScreenToClient( &pt );  CPoint ptOffset = GetDeviceScrollPosition();  pt.x = (int)(double(pt.x+ptOffset.x)/m_fScale);  pt.y = (int)(double(pt.y+ptOffset.y)/m_fScale);  m_ptMouse.x = -1;  m_ptMouse.y = -1;  CScrollView::OnLButtonUp(nFlags, point);}void KControlBuilderView::OnMouseMove(UINT nFlags, CPoint point){  if( nFlags & MK_LBUTTON && m_ptMouse.x > 0 )  {    int dx = point.x-m_ptMouse.x;    int dy = point.y-m_ptMouse.y;    if ( !(dx == 0 && dy == 0) )    {      m_ptMouse = point;      CPoint ptOffset = GetDeviceScrollPosition();      CPoint t = ptOffset;      ptOffset.x -= dx;      ptOffset.y -= dy;      //      CSize sz = GetTotalSize();      ptOffset.x = max( ptOffset.x, 0 );      ptOffset.y = max( ptOffset.y, 0 );      ptOffset.x = min( ptOffset.x, sz.cx );      ptOffset.y = min( ptOffset.y, sz.cy );      //      ScrollToPosition(ptOffset);    }  }  CScrollView::OnMouseMove(nFlags, point);}void KControlBuilderView::OnRButtonUp(UINT nFlags, CPoint point){  CPoint ptOffset = GetDeviceScrollPosition();  POINT pt = { 0, 0};  pt.x = int( (point.x+ptOffset.x)*m_fScale);  pt.y = int( (point.y+ptOffset.y)*m_fScale);  for( tBlkDrawCont_::iterator it=m_blks.begin(), end(m_blks.end()); it!=end; ++it )  {    CRect bodyRect = (*it).m_blk->GetBlkBodyCoord();    if( bodyRect.PtInRect(pt) )    {      KKK();      GetDocument()->m_pcb->ShowACSObject( NULL, (LPSTR)it->m_blk->BlockName.c_str(), NULL, true );      break;    }  }  CScrollView::OnRButtonUp(nFlags, point);}BOOL KControlBuilderView::PreCreateWindow(CREATESTRUCT& cs){  cs.style |= (CS_VREDRAW | CS_HREDRAW);  return CScrollView::PreCreateWindow(cs);}void KControlBuilderView::OnMButtonDown(UINT nFlags, CPoint point){  // TODO: Add your message handler code here and/or call default  //__super::OnMButtonDown(nFlags, point);}