#include "stdafx.h"#include "Trans.h"#include <macros/csvrow.h>using namespace std;using namespace ns_translator;struct CSysID{  LPSTR Name;  eYToken_value ID;};static CSysID SysID[] = {  "ALIAS", eytALIAS,   "IF", eytIF,   "THEN", eytTHEN,   "ELSE", eytELSE,   "ELSEIF", eytELSEIF,  "ENDIF", eytEND_IF,   "INTEGER", eytINTEGERkeyword,  "DOUBLE", eytDOUBLEkeyword,  "FLOAT", eytDOUBLEkeyword,  "END", eytEND,  "OR", eytOR,  "AND", eytAND,  "NOT", eytNOT,  "PROGRAM", eytPROGRAM,  "#DEFINE", eytDEFINE,  "SWITCH", eytSWITCH,  "CASE", eytCASE,  "OTHERWISE", eytOTHERWISE,  "END SWITCH", eytEND_SWITCH,   "EXIT", eytEXIT,  "MOD", eytMOD,  "DIM", eytDIM,};struct SNameByName{  LPCSTR pN1;  LPCSTR pN2;};static SNameByName constVals[] ={  {"TRUE", "1"},  {"FALSE", "0"},  {"IsGood", "192"},  {"IsBad", "0"},  {"NAN", "NAN"},  {"+INF", "+INF"},  {"-INF", "-INF"},};//////////////////////////////////////////////////////////////////////////KAaTranslator::KAaTranslator( KBmBase *pOwner ): m_pLineStream( NULL ), m_pFullStream( NULL), m_bIsLogicOperation( FALSE ), m_bUnderComment( false ), m_pOwner( pOwner ){  Reset();}KAaTranslator::~KAaTranslator(void){}void KAaTranslator::Reset(){  memset( Stack, 0xFF, sizeof( Stack ) );  PosSt = 0;   memset( OP, 0xFF, sizeof( OP ) );  kOP = 0;  memset( nFunction, 0xFF, sizeof( nFunction ) );  kFunction = 0;   m_bIsLogicOperation = FALSE;}eYToken_value KAaTranslator::GetTokenFromLine(){  if( !Parm.skipShiftTokHistory )  {    MoveMemory( &Parm.prevTok[1], &Parm.prevTok[0], (_countof(Parm.prevTok)-1)*sizeof(Parm.prevTok[0]) );    Parm.prevTok[0] = Parm.curTok;  }  Parm.curTok = GetToken( *m_pLineStream );  Parm.skipShiftTokHistory = 0;  return Parm.curTok;}void KAaTranslator::Translate( std::istream &str, bool bAction ){  Parm.InitAll( bAction );  m_pFullStream = &str;  try  {    NextTokens();  }  catch( KThrow e )  {    Var.clear();    kOP = 0;    kFunction = 0;    return;  }  char ch = 1;  if( m_pLineStream )    ASS( !m_pLineStream->get(ch) );  ASS( !m_pFullStream->get(ch) );  m_pFullStream = NULL;  m_pLineStream = NULL;  ASS( !PosSt );  ASS( !kFunction );}void KAaTranslator::AnalizeLine( std::string &szLine ){  Trim( szLine, ' ');  if( szLine.empty() )    return;  istream *pPrevLineStream = m_pLineStream;  stringstream lineStr;  m_pLineStream = &lineStr;  lineStr << szLine;  while( lineStr )  {    eYToken_value tok = GetTokenFromLine();    if( !AnalizeTok( tok, m_szValue ) )    {      m_pLineStream = pPrevLineStream;      Parm.tokAfterAnalize = unknown;      return;    }    Parm.tokAfterAnalize = tok;  }  if( !Parm.skipEndLine )  {    while( PosSt > 0 )    {      KMethodOnDestroy<STokenElement> C(Stack[PosSt-1], &STokenElement::Reset );      PosSt--;      AddToOP( OP_Operation, (STokenElement)C );      Parm.Init();    }  }  m_pLineStream = pPrevLineStream;}void KAaTranslator::NextTokens( bool bOne /*= false*/ ){  KTHROW( m_pFullStream );  if( m_pLineStream )  {    while( *m_pLineStream )    {      eYToken_value tok = GetTokenFromLine();      if( !AnalizeTok( tok, m_szValue ) )      {        Parm.tokAfterAnalize = unknown;        return;      }      if( bOne )        return;    }  }  string szTmp;  while( GetLine( *m_pFullStream, szTmp) )  {        TrimLeft( szTmp, ' ' );    TrimLeft( szTmp, '\t' );    if( m_bUnderComment )    {      string::size_type pos = szTmp.find('}');      if( string::npos!=pos )        szTmp.erase( 0, pos+1), m_bUnderComment = false;    }    if( szTmp.length() && '{'==szTmp.at(0) )    {      ASS( !m_bUnderComment );      m_bUnderComment = true;    }    if( m_bUnderComment )    {      string::size_type pos = szTmp.find('}');      if( string::npos!=pos )        szTmp.erase( 0, pos+1), m_bUnderComment = false;    }    if( !m_bUnderComment )      AnalizeLine( szTmp );  }}std::istream &KAaTranslator::GetLine( std::istream &_ist, std::string &str ){  string szTmp;  str.clear();  while( getline( _ist, szTmp) )  {    bool cont = false;    string::size_type pos = szTmp.rfind( "//" );    if( string::npos!=pos )    {      string::size_type l = szTmp.length();      if( (l-pos)>=2 )      {        szTmp.erase( pos );        cont = true;      }    }    str.append( szTmp );    if( !cont )      break;  }  return _ist;}bool KAaTranslator::IsConst( eYToken_value &tok, std::string &szVal ){  for( int i=0; i<_countof(constVals); i++ )  {    if( !_strcmpi(szVal.c_str(), constVals[i].pN1) )    {      szVal = constVals[i].pN2;      tok = eytNUMBER;      return true;    }  }  return false;}bool KAaTranslator::IsSys( eYToken_value &tok, std::string &szVal ){  for( int i=0; i<_countof(SysID); i++ )  {    if( !_strcmpi(szVal.c_str(), SysID[i].Name) )    {      tok = SysID[i].ID;      if( !m_bIsLogicOperation )      {        switch( tok )        {        case eytAND:          tok = eytBITAND;          break;        case eytOR:          tok = eytBITOR;          break;        }      }      return true;    }  }  return false;}void KAaTranslator::GetResult( int &kVar, int &kop ){  kVar = (int)Var.size();  kop = kOP;}void KAaTranslator::GetResult( KVar *var, SOPElement *op ){  if( !Var.empty() && var )    memmove_s( var, sizeof(*var)*Var.size(), &Var[0], sizeof(*var)*Var.size() );  if( kOP && op )    memmove_s( op, sizeof(*op)*kOP, OP, sizeof(*op)*kOP );}