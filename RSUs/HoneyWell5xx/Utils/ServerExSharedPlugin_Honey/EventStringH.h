#pragma once#include <EventString.h>#include <Common.h>class KEventString : public CEventString{public:  KEventString(void);  ~KEventString(void);  bool Flt( SSendToModel& send, float * v, SValueDef* def, LPCTSTR szName, LPCTSTR szComm, CBase* pBase );  bool Dbl( SSendToModel& send, double* v, SValueDef* def, LPCTSTR szName, LPCTSTR szComm, CBase* pBase );  bool Int( SSendToModel& send, int* v, SValueDef* def, LPCTSTR szName, LPCTSTR szComm, CBase* pBase );  bool Chr( SSendToModel& send, char* v, SValueDef* def, LPCTSTR szName, LPCTSTR szComm, CBase* pBase );  bool Str( SSendToModel& send, char* v, SValueDef* def, LPCTSTR szName, LPCTSTR szComm, CBase* pBase );  bool Bol( SSendToModel& send, bool* v, SValueDef* def, LPCTSTR szName, LPCTSTR szComm, CBase* pBase );  int Put( SSendToModel& send, CBase* pBase );};